import { GLUE_TAB, TIP_FULL } from "./geometry.mjs";

/**
 * 맞뚜껑 (상하 텍 클로저)
 *   상하 대칭. 한쪽 날개 = 전폭 뚜껑패널(D) + 좁은 텍 혀(**18.0** — 목형 5벌 산포의 상한)
 *   netH = H + 2·(D + 18.0) = H + 2D + 36
 *
 * ① 뚜껑패널 = D 정확 — 칼선 PDF 2건이 세로 접는선의 y 범위로 직접 확인했다
 *    (아트워크에 오염되지 않는 측정법. verify-imposition.mjs ③ 참조)
 *      바이오머   150×15×150   몸통 성분 180.0 = H+2D  (y 31.66~211.66)
 *      더파이러츠 190×68×280   몸통 성분 416.0 = H+2D  (x 42.06~572.36)
 *    종전 주석의 「뚜껑 66.5 ≈ D−3(여유)」은 칼선-07 접는선 눈대중 판독이었고
 *    그 합이 320.0 ≠ 실측 321.0 이었다. 위 2건이 D 정확을 확정했으니 폐기한다.
 *
 * ② ★ 텍 혀는 상수가 아니다 — 목형마다 15.0~18.0 으로 흩어진다 (26-08-07 재조사)
 *    혀 = (netH실측 − (H+2D)) / 2 로 역산한 실측 4건 / 목형 3벌:
 *
 *      샘플        W×D×H          netH실측  몸통 H+2D   혀     근거 강도
 *      바이오머    150×15×150      210.0     180.0     15.00  PDF 성분 직접 실측
 *      더파이러츠  190×68×280      452.0     416.0     18.00  PDF 성분 직접 실측
 *      칼선-07     150×69.5×149    321.0     288.0     16.50  netH 만 실측 → 역산
 *      칼선-08     150×69.5×219    391.0     358.0     16.50  같은 목형족(D 동일)
 *
 *    혀 상수 후보별 netH 오차 (바이오머 / 더파이러츠 / 칼선-07 / 08):
 *      t=15.0   +0.0  −6.0  −3.0  −3.0   MAE 3.00mm
 *      t=16.5   +3.0  −3.0  ±0.0  ±0.0   MAE 1.50mm  ← 종전 채택 (26-08-19 폐기)
 *      t=18.0   +6.0  ±0.0  +3.0  +3.0   MAE 3.00mm  ← **채택**. 음수(=부족=겹침)가 0 이다
 *    ★ MAE 로 고르면 16.5 가 이긴다. 그런데 이 프로젝트의 판정 기준은 MAE 가 아니다 —
 *      「작으면 판에서 겹친다(물리적으로 불가능) · 크면 up 이 줄 뿐」이라 **음수 잔차가
 *      금지**다(verify-imposition 잔차 게이트 머리말). t=16.5 의 −3.0 이 그 금지 항목이고
 *      실제로 510mm² 겹침을 만들고 있었다. 아래 ★ 26-08-18 A/B 참조.
 *
 *    ★ 26-08-16 정정 — 위 「목형 3벌 평균과 정확히 일치」는 이제 **사실이 아니다.**
 *      DXF 2건이 들어와 목형 5벌이 됐고 혀 = (netH−(H+2D))/2 는 11.0 / 15.0 / 16.5 /
 *      16.5 / 17.0 / 18.0 으로 흩어진다 (T002-75x35x143 · 바이오머 · 칼선-07 · 칼선-08 ·
 *      A004A-152x56x200 · 더파이러츠). 평균 15.7 · 산포 ±3.5mm.
 *      **16.5 를 바꿀 근거는 여전히 없다**(중앙값이고 최소 MAE) — 하지만 「평균과 일치」로
 *      읽고 안심하지 마라. 이 주석을 안 고쳤더니 다음 사람이 15.0 으로 낮추는 실험을
 *      반복했다. 목형별 상수가 유일하게 모순 없는 설명이고, 판지 두께는 PDF 에서 측정
 *      불가라 요인을 특정할 수 없다(H·D·W 어느 것으로도 설명 안 된다 — 아래 ⚠ 참조).
 *      ⚠ 더파이러츠 실측 뚜껑 86.01 vs 공식 84.5 = −1.51mm 로 **작다.** clearance 0.5mm
 *        보다 크므로 목형 절반에서 겹침 위험이 상수로 깔려 있다. t=18.0 으로 올리면
 *        전 목형 안전측이 되지만 netH 가 +3mm 늘어 up 이 줄고 바이오머는 +6mm 과대가
 *        된다 — 계량하지 않았으니 채택 전에 A/B 가 필요하다.
 *
 *      ★ 26-08-18 — 그 A/B 를 **돌렸다. 위험은 실재하고 t=18.0 이 그것을 없앤다.**
 *        「위험」이 추측이 아니라는 것부터: 실측 프로파일(위 12.99/29.99… 대신
 *        더파이러츠 실측 [35,86,35,0])을 앱이 고른 배치에 얹고 격자 0.3mm 로 재면
 *          t=16.5  더파이러츠 **510.48mm²** 겹침 (46전지·국전·하전지 2up 2×1↺ 각각)
 *          t=18.0  **0.00mm²**
 *        치르는 값 (직접 실행):
 *          맞뚜껑 원시 up  1/2 → 1/2   앱 up(verify-net) 1/2 → 1/2   **불변**
 *          바이오머 netH 213.0 → 216.0 (실측 210.0 대비 +3 → +6, 과대 = 안전측)
 *          더파이러츠 netH 449.0 → 452.0 (실측 452.0 과 **정확히 일치**)
 *          ⚠ verify-net 맞뚜껑 세로 MAE 0.0 → 3.0mm (칼선-07/08 이 둘 다 +3.0 과대)
 *
 *      ★★ 26-08-19 — **적용했다.** t = 16.5 → 18.0.
 *        미룬 이유가 「날개 프로파일 과제의 범위 밖」이었는데, 그 사이 겹침 게이트의
 *        진실이 모델 재합성에서 **원본 칼선 PDF** 로 바뀌면서 같은 위험이 다시,
 *        더 크게 찍혔다 — 더파이러츠 **517.87mm² @46전지 2up 2×1↺** (t=16.5).
 *        예산 600mm² 로 덮어 두는 것은 「크게 그려라」 정책을 이 한 자리에서만
 *        어기는 것이고, 그 자리가 하필 **물리적으로 불가능한 판**이다. 정책이 이긴다.
 *        A/B 재실행(26-08-19, PDF 진실 기준): 더파이러츠 517.87 → **0.00mm²**,
 *        바이오머 0.00 → 0.00 (불변), 잔차 netH −3.00 → **0.00**.
 *        ⚠ 잃는 것은 위에 적은 그대로다 — verify-net 맞뚜껑 세로 MAE 0.0 → 3.0mm.
 *          칼선-07/08 이 둘 다 **+3.0 과대** = 안전측이라 방향이 옳다. 점수는
 *          떨어진 대로 적는다(ARCHITECTURE §7 갱신규칙 2).
 *
 *      ★★★ 26-08-19 독립 재측정 — 위 A/B 를 **믿지 않고 다시 쟀다.** 결론은 같다.
 *        ⓐ 혀 18.0 을 공식이 아니라 도면에서 다시 확인했다. 원본 PDF 폴리라인만 쓰고
 *          (healing·래스터 없이) 세로 크리스로 패널을, 「간격이 H 에 가장 가까운 가로선
 *          두 개」로 몸통을 특정했다:
 *            더파이러츠  몸통 y 86.00~366.00 = 280.00 (=H, Δ0.00) · 위 띠 = **86.00** = D+18.0
 *            바이오머    몸통 y 30.00~180.00 = 150.00 (=H, Δ0.00) · 위 띠 = **30.00** = D+15.0
 *          즉 15.0 / 18.0 산포는 측정법을 바꿔도 그대로다. 상한 18.0 이 맞다.
 *        ⓑ t=16.5 로 되돌려 게이트를 실제로 돌렸다(원본 PDF 진실 기준):
 *            더파이러츠 **516.39mm² @46전지 2up 2×1↺** ★신규 · 잔차 최소 **−3.00**
 *            verify-imposition 실측대조 24/31 · **EXIT=1** (게이트가 실패로 잡는다)
 *          t=18.0 으로 되돌리면 0.00mm² · 잔차 0.00 · 26/31 · EXIT=0.
 *          ⚠ 위 줄의 「517.87」은 재현되지 않았다 — 내 재실행값은 **516.39** 다.
 *            1.48mm² 차이는 그 사이 healSegs 의 「끝점끼리 1.0mm 이하 다리」 규칙이
 *            들어와 실물이 조금 커진 것으로 보인다(그 규칙은 도형을 크게만 만든다).
 *            어느 값이든 결론은 같으니 숫자만 내가 실제로 잰 것으로 고쳐 둔다.
 *        ⓒ 치르는 값도 t 만 갈아 A/B 했다 — **up 은 정말 불변이다**:
 *            verify-net up 일치 6/10 → 6/10 · 비폴리곤 6/6 → 6/6
 *            맞뚜껑A guk2 224×199→224×202 둘 다 6up✓ · 맞뚜껑B 4x62 둘 다 2up
 *            움직인 것은 맞뚜껑 세로 MAE 0.0 → 3.0mm 하나뿐이고 방향은 +3.0 과대(안전)다.
 *
 *    ★ 26-08-07 재실측 — 추출기를 직접 돌려 「세로 접는선 span」으로 다시 쟀다
 *      (verify-imposition ③ 과 같은 방법. 가로 크리스는 1mm 안쪽에 또 하나가 있어
 *       몸통을 414 로 오독한다 — 몸통은 반드시 세로 접는선으로 특정해라):
 *        바이오머   세로선 x=196.03·346.03  y 31.66~211.66 = 180.00 (=H+2D)
 *                   bbox y 16.66~226.66 = 210.00  → 혀 위 15.00 / 아래 15.00
 *        더파이러츠 세로선 x=315.06·505.06  y 30.96~446.96 = 416.00 (=H+2D)
 *                   bbox y 12.96~464.96 = 452.00  → 혀 위 18.00 / 아래 18.00
 *      위·아래 대칭 오차가 두 건 다 0.00 이다. 혀 15.0 vs 18.0 은 측정 잡음이 아니다.
 *
 *    ⚠ 순환 근거 주의 — `geometry.mjs:28-29` 의 「전체 213.0 / 449.0」 은 실측이 아니라
 *      **이 공식 H+2(D+16.5) 의 출력**이다. 같은 줄의 몸통(150.0/180.0 · 280.0/416.0)은
 *      진짜 실측이라 「전체」도 실측으로 읽히지만, 실측 netH 는 210.0 / 452.0 이고
 *      차이가 정확히 −3.0 / +3.0 — t=16.5 의 두 잔차 그 자체다. 그 줄로 16.5 를
 *      검증하면 공식이 공식을 확인하는 순환이 된다. 대조는 verify-pdf §A 오라클로 해라.
 *
 *    ⚠ 바이오머 netH 210.0(=혀 15.0)을 그대로 반영해 t=15.0 으로 바꾸면 오차가
 *      **2배가 된다.** 같은 추출기·같은 측정법의 더파이러츠가 18.0 이라 서로 상쇄되지
 *      않는다. 샘플 1건에 맞추는 것이므로 하지 마라 — 실행해서 확인했다(아래 ③).
 *    ⚠ D 의존으로도 설명이 안 된다. 목형 3벌 회귀는 혀 = 0.0413D + 14.40 (잔차 0.8mm)
 *      이지만, D 가 1.5mm 밖에 안 다른 더파이러츠(68→18.0)와 칼선-07(69.5→16.5)의
 *      국소 기울기가 **−1.00** 으로 부호가 반대다. 즉 D 기울기는 목형 산포(±1.5mm)에
 *      묻혀 있고 분리 불가다. 상수 유지가 정직한 선택이다.
 *
 * ③ A/B 실행 결과 (t=16.5 → 15.0, 11개 스위트 전부 실행)
 *    움직인 점수: **없다.** verify-net 판걸이 7/10 · 비폴리곤 6/6 · verify-total 24/26 ·
 *    verify-r 37/39 · verify-print 19/20 · scorecard2 10/15 · scorecard-nest 7/10 ·
 *    scorecard-unit 7/12 · verify-dieline 360/360+36/36 · verify-pdf 66/68 전부 동일.
 *    나빠진 것 1개: verify-net 맞뚜껑 세로 평균절대오차 **0.0mm → 3.0mm**.
 *    netH 가 3mm 줄어 맞뚜껑A 199→196 · B 223→220 이 되지만 up 은 6/6 · 2 그대로다.
 *    ⚠ verify-pdf 가 안 움직이는 것은 안전 신호가 아니다 — 그 스위트는 PDF 추출기를
 *      채점하고 이 공식을 읽지 않는다. 맞뚜껑 netH 의 유일한 점수판은 verify-net 이다.
 *    26-08-07 13개 스위트로 재실행해 위 결과를 그대로 재현했다. 추가로 확인된 것 1개:
 *    verify-interlock 은 맞뚜껑 2행의 「필요 겹침」 수치만 따라 움직이고(A 30→28 ·
 *    B 37→35) up 7/10 은 불변이다 — 점수가 아니라 파생 표시값이다.
 *
 * ④ 삼면접착 아래 띠 D+15 와 같은 규칙인가 — **구조는 같고 상수는 다르다**
 *    같은 것: 띠 = [전폭 패널 D] + [좁은 텍 혀] 라는 분해. 삼면접착도 PDF 성분이
 *      혀를 따로 떼어 낸다(소스코 몸통 208.5 + 혀 15.0 = 223.5, 아래 띠 57.9 ≈ D+15).
 *      ①에서 맞뚜껑 뚜껑패널이 D 정확으로 확인됐으므로 분해 자체는 우연이 아니다.
 *    다른 것: 혀 값. 삼면접착은 D 36~46 에서 3벌 전부 정확히 15.00 (산포 0) 인데
 *      맞뚜껑은 15.0~18.0 으로 흩어진다. 삼면접착 자동바닥 혀는 잠금 길이가 목형
 *      표준으로 고정된 값이고, 맞뚜껑 텍 혀는 아가리에 끼우는 마찰 길이라 설계자
 *      재량이 남는다 — 구조와 무관하다. 즉 바이오머의 15.0 이 삼면접착 15.0 과
 *      같은 것은 **값 수준에서는 우연**이다. 두 상수를 묶지 마라.
 *
 * ⑤ 폐기된 공식 (지우지 마라 — 「왜 이 공식이 아닌가」를 잃는다)
 *    D/2+20 / D/2+15 → 칼선-07/08 두 건 모두 −67.5mm 부족. 원본은 verify-net.mjs OLD.
 *    ✓ 맞뚜껑A 92×13×140 국2: 224×199 → 회전 3×2 = 6up (견적서 6up 일치)
 *      종전 공식(224×258)으로는 4up 밖에 안 나왔다.
 */
export default {
  id: "tuck_both", label: "맞뚜껑 (상하 텍 클로저)", labelJa: "差込み蓋（上下）", polygon: true,

  netSize(W, D, H, { hangTab = 0 } = {}) {
    // 혀 18.0 = 목형 5벌 실측 산포(11.0 / 15.0 / 16.5 / 16.5 / 17.0 / 18.0)의 **상한**.
    // 평균(15.7)도 중앙값(16.5)도 아니다 — 부족은 겹침이고 과대는 up 감소라 상한이 정답이다.
    // 근거·A/B·치르는 값은 위 ② 「★★ 26-08-19」 참조. 낮추려면 거기부터 읽어라.
    const topLid = D + 18.0, botFloor = D + 18.0;
    return { netW: 2 * (W + D) + GLUE_TAB, netH: H + topLid + botFloor,
             topLid, botFloor, glueTab: GLUE_TAB, hangTab };
  },

  // ⚠ 26-08-25 — **맞뚜껑에는 목형별 노브를 만들지 않았다.** 후보였던 「혀 t」(실측
  //   15.0 / 16.5 / 18.0)를 3값 전부로 계량했더니 verify-net 6 · scorecard-unit §A 6 ·
  //   scorecard2 8 · 앱 실경로 15건 7 로 **한 칸도 안 움직인다.** 죽은 손잡이는 만들지
  //   않는다 — 화면에 뜨는 순간 사용자가 「이걸 맞추면 값이 달라지겠지」라고 믿는다.
  //   (상한 18.0 봉투는 더파이러츠 510mm² 방어용이므로 위 netSize 상수로 그냥 남긴다.)
  //   opt 를 받는 것은 서명 통일 때문이다 — 모르는 노브는 무시한다.
  flaps(W, D, H, opt = {}) {
    const ns = this.netSize(W, D, H, opt);
    const dust = Math.min(0.43 * D + 7, ns.topLid * 0.85);   // 더스트는 뚜껑보다 낮다
    const bDust = Math.min(dust, ns.botFloor * 0.85);
    // ★ 26-08-16 — **스트레이트텍**으로 교정. 위·아래 뚜껑을 **같은 W 패널**(idx 1)이 든다.
    //   종전 주석은 「하단 뚜껑은 후면(3)에만 — 뚜껑이 2개면 맞물림 불가」였는데,
    //   그건 실측이 아니라 맞물림을 살리려는 **추론**이었고 실물은 역텍이 아니다.
    //   실측 2/2 (몸통 가로 접는선 기준 · 잔차 −0.01mm, 현행 idx 0..3 순서):
    //     바이오머   150×15×150   위 12.99 29.99 12.99 0.00 / 아래 13.01 30.01 13.01 0.01
    //     더파이러츠 190×68×280   위 34.99 85.99 34.99 0.00 / 아래 35.01 86.01 35.01 0.01
    //   두 건 다 위·아래 배열이 **완전히 같다**. 종전 역텍 모델은 더파이러츠에서
    //   잔차 **−86.01mm**(뚜껑이 있어야 할 자리를 빈 공간으로 그린다) · MAE 22.12mm →
    //   교정 후 0.996mm.
    //   ⚠ up 영향은 전부 **감소 방향**이다(도형이 커졌으므로 정상 = 안전측):
    //     맞뚜껑B 4x64 2→1 · ha2 6→4 · ha4 3→2 · 바이오머 4x64 2→1 · ha3 4→3.
    //     견적서 근거 2건(맞뚜껑A guk2 6up · 맞뚜껑B 4x62 4up)은 불변이다.
    //   ⚠ 더스트(dust)는 **바꾸지 않았다.** 실측 2건뿐이라 2점 정확적합(0.4151D+6.774)은
    //     검증 불가고, 현행식은 두 표본 모두 과대(안전측)다 — 유지가 정직하다.
    //     삼면접착의 D/2 규칙을 여기 갖다 쓰지 마라: 바이오머 D/2=7.5 vs 실측 13.0 이
    //     D/2 를 명확히 반증한다. 두 상수를 묶지 마라.
    // ★ 26-08-19 tipW: null → TIP_FULL. 맞뚜껑 텍 혀는 **패널 폭 그대로**다
    //   (바이오머 혀 148.6/W150 · 더파이러츠 188.6/W190 — geometry.TIP_FULL 주석에 근거).
    //   null(사선 기울기형)은 한쪽 9.9mm 를 깎아 실물보다 **작게** 그리고 있었다.
    //
    //   ⚠ 26-08-19 정정 — 종전 이 자리에 근거로 적혀 있던 「실물 겹침 15.58mm²」는
    //     **재현되지 않는다.** 이 작업트리에서 tipW 만 null 로 되돌려 실제로 돌렸다:
    //       실물 겹침 게이트 = 바이오머 0.00mm² · 더파이러츠 0.00mm²
    //                        (표준판형 114조합 신규 겹침 **0건**)
    //     즉 이 변경을 지키는 것은 겹침 게이트가 아니다. 틀린 숫자를 근거로 남기면
    //     다음 사람이 그 숫자를 찾다가 근거 전체를 못 믿게 된다 — 그래서 지운다.
    //   ★ 실제로 지키는 게이트는 **커버리지**다(verify-imposition, 26-08-19 신설).
    //     같은 되돌림에서 둘 다 ★신규로 뜨고 스위트가 EXIT=1 이 된다:
    //       바이오머   부족 553.0mm² · 최대연속 3.58mm  @x11.3 y17.0~20.6 위띠·측면1
    //       더파이러츠 부족 391.4mm² · 최대연속 14.29mm @x68.9 y430.2~444.5 아래띠·전면
    //     (돌연변이표 「맞뚜껑 tipW TIP_FULL → null」 행이 이 되돌림을 상시 감시한다.)
    //   ⚠ 대가: 맞뚜껑 날개가 전폭 사각형이 되어 그쪽 맞물림 공간이 사라진다.
    //     실측이 「혀가 거의 전폭」이라고 말하므로 그 맞물림은 애초에 없던 것이다.
    return { type: "tuck", tipW: TIP_FULL,
      top: [dust,  ns.topLid,   dust,  0],
      bot: [bDust, ns.botFloor, bDust, 0] };
  },
};
