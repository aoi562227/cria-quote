// ⚠ geometry.GLUE_TAB 을 **일부러 import 하지 않는다.** 십자의 netW 가산분은 공용값과
//   다르다(아래 CROSS_TAB). 예전처럼 GLUE_TAB 을 쓰면 니치어·칼선-10 이 부족(=겹침)이 된다.
// ★ 반면 GLUE_TAB_SHORT(0.7)는 **공용이 맞다** — 「탭이 붙는 D 패널이 0.70mm 짧다」는
//   목형 공통의 위상이고 geometry.xEdges 가 탭 폭을 netW 에서 역산할 때 쓰는 바로 그 값이다.
//   여기서 사본을 만들면 둘이 갈리는 순간 폴리곤 탭 폭이 netW 와 어긋난다.
import { GLUE_TAB_SHORT } from "./geometry.mjs";

/**
 * 십자조립 (크로스바텀)
 *
 * 종전: total_tuck = 7D/4 + 11 을 2점(D=70 · D=20) 회귀로 얻고
 *   topLid = botFloor = 7D/8 + 5.5 로 **반반 나눴다.**
 *     70×70×151.5 → 303.33×285(실측) → netH 283.25 (Δ−1.75)
 *     80×20×49    → 211×95(실측)      → netH  94.50 (Δ−0.50)
 *
 * ★ 26-08-16 — 「반반 분할」은 실측이 아니라 **가정**이었다는 것이 드러났다.
 *   총합만 회귀했고 분할 근거는 어디에도 없었는데, 두 값이 나란히 적혀 있어
 *   실측처럼 읽혔다. 위/아래를 따로 재니 **5/5 전부 비대칭**이다 (위가 아래의 1.55~2.28배):
 *
 *   ⚠ 아래 표의 괄호 값은 **적합도(MAE)가 아니라 잔차 부호**로 읽어라. 이 저장소 규약에서
 *     음수 = 모델이 실물보다 작다 = 판에서 겹친다 = 금지다(바로 아래 26-08-19 절 참조).
 *     그래서 「✓」는 잔차 ≥ 0 을 뜻하고, 음수 4칸이 이 파일을 고치게 만든 실패 목록이다.
 *
 *     표본                      D      위 띠    아래 띠   0.70D       1.086D+10.2
 *     니치어 240808 70×70×151.5   70     85.0    49.0    49.00 ✓      86.22 (+1.22 ✓)
 *     다빈기획 240805 190×100×109.5 100   119.2    70.0    70.00 ✓     118.80 (−0.40 ✗겹침)
 *     뉴로티엑스 240821 80×20×48.5  20     32.0    14.0    14.00 ✓      31.92 (−0.08 ✗겹침)
 *     C017-42x42x117 (칼선-09)    42     55.0    30.0    29.40 (−0.6 ✗) 55.81 (+0.81 ✓)
 *     C017-49x49x144 (칼선-10)    49.5   65.0    34.5    34.65 (+0.15 ✓) 63.96 (−1.04 ✗겹침)
 *
 *   ⟹ 종전 대칭 모델은 위 MAE 16.48mm · 아래 MAE 15.26mm 였다 — 20배 차이다.
 *     (MAE 는 「어느 모델이 더 가까운가」의 비교에만 쓴다. **채택 판정은 MAE 가 아니라
 *      잔차 부호**다 — 0.15mm 짜리 음수도 물리적으로 불가능한 판을 만든다.)
 *   netH 는 바뀐다. 종전 총합이 2점 회귀였고 이제 5점이다. 칼선 실측 netH 오차도
 *   MAE 2.77 → 1.47mm 로 내려간다(라벨 오류인 칼선-14 를 빼면 1.82 → 0.35mm).
 *   파일: C:/이예찬_업무/private/도면/견적용도면 (영업기밀 — 저장소 밖).
 *   DXF 2건(C017-*)은 칼선(SolidLayer)/오시선(DashLayer)이 레이어로 분리돼 있어
 *   접는선 판독에 추정이 안 들어간다 — 이번 5건 중 근거 강도가 가장 높다.
 *
 * ══════════════════════════════════════════════════════════════════
 *  ★★ 26-08-19 — 위 표의 **음수를 적합도로 읽고 있었다.** 그게 오독이다.
 * ══════════════════════════════════════════════════════════════════
 *  (−0.40)(−0.08)(−0.6)(−1.04) 는 「MAE 0.71 / 0.15 로 잘 맞는다」의 근거로 적혀 있었다.
 *  그런데 이 저장소의 규약에서 **음수 잔차 = 모델이 실물보다 작다 = 판에서 겹친다 =
 *  물리적으로 불가능한 판**이고, 그건 금지다(verify-imposition 잔차 게이트 머리말 ·
 *  verify-drag-shape 머리말). 자기 숫자를 규약과 다르게 읽고 있었던 것이다.
 *
 *  더 나쁜 것은 **cross 가 새 게이트 2개(실물 겹침·잔차)에 표본 0건**이었다는 점이다.
 *  이번 변경에서 netSize 를 고친 유일한 구조가 cross 인데 감시자가 없었다. 등록하니
 *  겹침 2건(다빈기획 36.5mm² · 칼선-10 100.2mm²)과 음수 잔차 4/5 가 즉시 나왔다.
 *  ⟹ 세 상수를 **안전 봉투**(5벌 전부 ≥ 실측)로 올린다. glue3 가 26-08-18 에 한 것과
 *    같은 조치이고, 값은 아래 각 상수 옆에 있다. up 은 줄어든다(= 견적이 비싸진다).
 *
 *  ★ 위 띠 **더스트**는 이번에 처음 실측했다 (종전 「실측 없음」)
 *    방법: 원본 PDF 의 세로 크리스로 패널 5칸을 특정(5벌 전부 4경계 검출)하고
 *          몸통 가로 접는선 밖 최원점을 0.25mm 세로 스캔.
 *          검산 — 이 파일이 이미 적어 둔 「아래 띠 D/W 비 32/49 · 68/70 · 14/14」가
 *          3/3 정확히 재현됐다. 즉 측정법이 이 표와 같은 것을 보고 있다.
 *
 *      표본        D      더스트 실측   종전 min(0.43D+7, …)   0.55D+5
 *      뉴로티엑스   20      15.5          15.60 (+0.10)          16.00 (+0.50)
 *      칼선-09     42      20.0          25.06 (+5.06)          28.10 (+8.10)
 *      칼선-10     49.5    23.5          28.29 (+4.79)          32.23 (+8.73)
 *      니치어      70      35.0          37.10 (+2.10)          43.50 (+8.50)
 *      다빈기획    100     59.0          50.00 (**−9.00**)      60.00 (+1.00)
 *
 *    ⟹ 종전 0.43D+7 은 큰 D 에서 **9mm 부족**했다. 5점 안전 봉투가 0.55D + 5 다.
 *    ⟹ 「70×70×151.5 의 −46% 가 더스트 상수 하나에 걸려 있다」는 지적의 답도 여기 있다:
 *      그 목형(니치어)의 더스트 실측은 **35.0** 이라 종전 37.10 도 실은 안전측이었다.
 *      즉 −46% 는 「근거 없이 크게 번 up」이 아니었다. 다만 **공식**은 다빈기획을
 *      못 덮으므로 봉투로 올려야 하고, 그 대가로 70×70 의 여유가 줄어든다.
 *      실측이 없어서 안전했던 게 아니라 실측해 보니 안전했던 것이다 — 이 구분이 중요하다.
 *
 *    ★★ 26-08-19 독립 재측정 — 위 더스트 5벌을 **믿지 않고 다시 쟀다.**
 *      다른 계측기로 쟀다: 원본 PDF 폴리라인의 **꼭짓점만** 쓰고 healing·래스터를
 *      안 거친다. 패널은 세로 크리스로, 몸통은 「간격이 H 에 가장 가까운 가로선 두 개」로
 *      특정했다(5벌 전부 몸통 간격 Δ≤0.50mm 로 H 를 맞혔다 = 프레임이 옳다는 검산).
 *        표본        내 재측정 더스트    위 표         모델 0.55D+5    여유
 *        뉴로티엑스   15.5              15.5  ✓일치     16.00          +0.50
 *        칼선-09     20.0              20.0  ✓일치     28.10          +8.10
 *        칼선-10     23.5              23.5  ✓일치     32.23          +8.73
 *        니치어      35.0              35.0  ✓일치     43.50          +8.50
 *        다빈기획     59.0              59.0  ✓일치     60.00          +1.00
 *      ⟹ 5/5 재현. 더스트는 이제 **실측 근거가 있는 상수**다.
 *      ⚠ 딱 한 칸이 어긋난다 — 니치어의 **두 번째** 더스트(D1 패널)를
 *        verify-imposition ORACLE 은 25.48 로 적었는데 내 계측기는 **26.73** 이다(+1.25).
 *        내 값이 더 깊으므로 잔차 게이트가 더 빡세지는 쪽이고(43.50−26.73 = +16.77,
 *        오라클 기준 +18.02), 어느 값이든 모델이 덮으므로 판정은 안 바뀐다. 그래서
 *        오라클을 조용히 고치지 않고 **불일치를 여기 적어 둔다** — 두 계측기가 같은 것을
 *        1.25mm 다르게 읽고 있다는 사실 자체가 다음 표본 때 필요한 정보다.
 *
 *    ★★ 26-08-19 — 「up 은 줄어든다」를 **주장이 아니라 측정으로** 바꿨다.
 *      이 파일은 대가를 「up 이 줄고 견적이 비싸진다」고 적어 놓고 재지는 않았다.
 *      HEAD(옛 상수) vs 현행을 실측 5벌 + 견적서 역산 2건 × 표준 판형 10종 = 70칸으로
 *      대조했다(solveImposition 직접 호출):
 *        **up 을 번 자리 0 · 줄어든 자리 12 · 불변 58.** 방향이 한쪽뿐이다 = 안전.
 *        줄어든 자리: 다빈기획 ha2 2→1 · ha4 1→0 │ 뉴로티엑스 ha2 24→18 · ha3 16→12 ·
 *          ha4 12→8 │ 칼선-09 4x62 9→8 · 4x63 6→4 │ 칼선-10 guk 9→8 │
 *          십자B 4x64 3→2 · ha2 8→6 · ha3 5→4 · ha4 4→3
 *        니치어(70×70×151.5)와 십자A(47×47×176)는 10판형 전부 **불변**이다.
 *      ⚠ 「십자 70×70×151.5 @4x64 가 1up→2up 이 되어 −46%」라는 지적은 **재현되지 않았다.**
 *        HEAD·현행 둘 다 4x64 에서 **1up**(370원 @295AB·4,000)이고, 옛 상수(LID 1.086D+10.2 ·
 *        FLOOR 0.70D · DUST 0.43D+7 · 탭 14.3)로 되돌려도 1up 이다. 그 −46% 는 CROSS_TAB
 *        봉투가 들어오기 전 작업트리 중간 상태의 값으로 보인다. 지금은 그 자리가 없다.
 *      ⚠ 이 대조를 처음 돌렸을 때 solveImposition 에 buildDieline 이 아니라 dielinePieces
 *        결과를 넘겨 **3,780칸이 전부 같은 up** 으로 나왔다. `dieline.key` 가 undefined 라
 *        캐시 키가 안 갈려서 첫 규격의 배치를 전부 돌려받은 것이다(imposition.mjs `ck`
 *        주석이 경고한 그 사고 그대로다). 이 파일을 다시 계량할 사람은 **buildDieline 을
 *        써라** — dielinePieces 는 key 를 안 만든다.
 *
 *  ★ 위 띠 **위상**도 갈린다 — 뚜껑이 idx1 인 목형과 idx3 인 목형이 섞여 있다
 *      idx1(W2): 니치어 · 칼선-09 · 칼선-10      idx3(W1): 다빈기획 · 뉴로티엑스
 *    종전 [dust, lid, dust, 0] 은 idx3 을 **0 으로 그린다.** 다빈기획에서 잔차
 *    −119.20mm(뚜껑이 통째로 빈 공간), 뉴로티엑스 −32.00mm 였다.
 *    ⟹ [dust, lid, dust, lid]. glue3 아래 띠가 「양쪽 W 다 깊게」로 간 것과 같은 이유다
 *      (거울상 목형을 한 공식으로 덮으려면 둘 다 그리는 수밖에 없다).
 *
 *  ★ 접착탭 폭이 **십자만** 산포한다 (netW 부족) — 봉투로 닫았다
 *    ─────────────────────────────────────────────────────────
 *    세로 크리스로 잰 탭 폭:  니치어 24.0 · 칼선-10 17.0 · 다빈기획 15.0 · 칼선-09 14.0 ·
 *    뉴로티엑스 12.0.  삼면접착·맞뚜껑 7벌은 **전부 정확히 15.0** 이다(geometry.GLUE_TAB
 *    주석) — 즉 산포는 십자 고유다. 그래서 공용 GLUE_TAB 을 건드리면 안 되고
 *    (건드리면 잔차 0.00 인 삼면·맞뚜껑 6벌이 통째로 깨진다) 십자 전용 상수가 맞다.
 *
 *      표본        2(W+D)    실측 netW   필요 가산분   그때의 탭폭(=가산분+0.70)
 *      니치어       280.00    303.33     23.33 ★최대   24.03
 *      칼선-10      197.00    212.00     15.00        15.70
 *      다빈기획      580.00    594.00     14.00        14.70
 *      칼선-09      168.00    181.00     13.00        13.70
 *      뉴로티엑스     200.00    211.33     11.33        12.03
 *
 *    ⟹ 5벌 안전 봉투 = **23.33** (= 니치어 실측 그 값). 잔차 0.00 / +8.33 / +9.33 /
 *      +10.33 / +12.00 — 전부 ≥ 0. 종전 14.3 은 니치어 −9.03 · 칼선-10 −0.70 **부족**이었고
 *      그 부족이 그대로 실물 겹침이 됐다 (니치어 1,269.27mm² @국2 · 칼선-10 100.17mm² @하4 —
 *      **둘 다 견적 실경로가 고르는 판형**이다. 즉 앱이 물리적으로 불가능한 판을 팔고 있었다).
 *
 *    ⚠ 왜 「봉투로 안 하고 예산으로 띄워 두는」 종전 판단을 뒤집었나
 *      종전 주석은 「netW 는 혼자 결정할 범위가 아니다」라며 verify-imposition 의
 *      KNOWN_OVERLAP/RES_BUDGET 로 넘겼다. 그런데 그 예산(겹침 1500mm² · 잔차 −9.1)은
 *      **자동선택 판형의 실물 겹침을 통과시키고 있었다.** 예산은 「원인이 특정된 미해결」을
 *      세워 두는 자리이고, 원인이 특정됐고 조치가 안전 방향이면 예산이 아니라 고칠 자리다.
 *      이 저장소의 선언된 정책 그대로다 — 「의심스러우면 크게 그려라. 작으면 겹치고(최악),
 *      크면 up 이 줄 뿐이다(안전)」. 실제로 up 은 줄고 견적은 **비싸진다**(아래 대가 참조).
 *
 *    ⚠ 대가 — 십자 **전 규격**의 netW 가 +9.03mm 다. 판걸이가 통째로 움직이고 견적이
 *      비싸지는 방향으로만 움직인다. 견적서 역산 2건(십자A 47×47×176 · 십자B 70×70×55)의
 *      up 재현이 이 대가를 계량한다 — verify-net 「견적서 판걸이 up 재현」.
 *      ⟹ 되돌리려면 탭 24.0 목형이 니치어 한 벌뿐임을 근거로 「이상치」라 부를 수 있어야
 *        하는데, 5벌 중 1벌은 20% 다. 이상치라 부를 근거가 표본에 없다. 표본을 늘려라.
 *
 *    ⚠⚠ 대가 두 번째 — **견적이 아예 안 나오는 조합이 생겼다** (26-08-19 실측)
 *      「비싸진다」로만 적어 두면 안 되는 종류의 회귀다. 점수판 밖에서 잡았다:
 *        십자 190×100×109.5 @하4(444×597) · 1,000/4,000/10,000 × 지종 2 = **6건**
 *        HEAD 는 전건 **1up** 이었다 (git archive HEAD 로 직접 실행 · 원색4·IR·단면접착
 *        기본값): AB295L 535 / 374 / 351원 · AB350 660 / 477 / 452원.
 *        지금은 netW 594.30 → **603.33** 이 597 을 6.33mm 넘겨 빈 결과가 된다.
 *      ⚠ 하필 이 규격이 실측 목형 **다빈기획**이고 그 목형의 실측 탭은 **14.00** 이다
 *        (위 표). 즉 이 목형은 실제로 하4 에 들어간다 — 못 들어가게 만든 것은 니치어
 *        24.00 이 정한 봉투 23.33 이다. 「모델이 실물보다 크다」의 대가가 여기서는
 *        up 감소가 아니라 **기능 소실**로 나타난다.
 *      판정 = **(a) 그대로 두고 화면이 이유를 말하게 한다.**
 *        · 금액 방향은 안전하다(못 부르는 것이 싸게 부르는 것보다 낫다).
 *        · 판형 「자동」이면 정상 산출된다 — 국2 3up 308원(AB295L·4,000). 소실은
 *          사용자가 하4 를 **직접 고정했을 때만** 일어난다.
 *        · (c) 「니치어만 예외로 빼고 봉투를 낮춘다」는 기각. 니치어는 **자동선택 판형**
 *          에서 1,269.27mm² 겹쳤던 그 목형이다 — 예외로 빼면 그 판이 되돌아온다.
 *        · (b) 「목형별 탭 폭을 입력으로 받는다」가 정답에 가깝지만 구조 옵션 신설이라
 *          이 지적의 범위를 넘는다. openQuestions 로 넘긴다.
 *      ⟹ 화면 문구는 quote.mjs `noFitReason` 이 소유한다(초과 mm 와 조치를 말한다).
 */

// 위 띠(뚜껑) — 5벌 안전 봉투. 적합 최적은 1.086D+10.2 였고 그때 최대 부족이 −1.04(칼선-10)라
// 상수를 +1.10 올렸다. 5벌 잔차 +2.32 / +0.70 / +0.02 / +1.91 / +0.06 — 전부 ≥ 0.
const LID = D => 1.086 * D + 11.3;
// 아래 띠 — 적합 최적은 0.70D 였고 최대 부족이 −0.60(칼선-09)라 +0.60 올렸다.
// 5벌 잔차 +0.60 / +0.60 / +0.60 / +0.00 / +0.75 — 칼선-09 는 **여유 정확히 0** 이다.
const FLOOR = D => 0.70 * D + 0.6;
// 위 띠 더스트 — 5벌 안전 봉투(위 표). 여유 최소 +0.50(뉴로티엑스 D=20).
const DUST = D => 0.55 * D + 5;
// netW 가산분 — **십자 전용**. 공용 GLUE_TAB(14.3) 이 아니다: 십자는 탭 폭이 목형마다
// 12.0~24.0 으로 흩어지고(위 「접착탭 폭이 십자만 산포한다」 표) 14.3 은 5벌 중 2벌에서
// 부족(=겹침) 방향이었다. 5벌 안전 봉투가 23.33 이다.
//   geometry.xEdges 는 탭 폭을 netW 에서 역산하므로(탭 = netW − 2W − 2D + GLUE_TAB_SHORT)
//   이 상수만 올리면 폴리곤의 탭도 24.03 으로 같이 넓어진다 — bbox 불변식은 자동으로 지켜진다.
//   검산 — 니치어 세로 크리스 실측 [0, 24.0, 94, 164, 234, 303.33] 을 이 프레임으로 되짚으면
//   마지막 D 패널이 69.33 = D − 0.67 인데 우리 모델은 D − GLUE_TAB_SHORT(0.70) 이다.
//   그 0.03 만큼 탭이 넓게(24.03) 나오고 합은 정확히 303.33 으로 맞는다 — 봉투는 이 목형 실측
//   그 자체이고 여유가 **정확히 0.00** 이다(verify-imposition 「여유 0.00」 목록에 뜬다).
const CROSS_TAB = 23.33;

// ══════════════════════════════════════════════════════════════════
//  ★ 26-08-25 — 산포를 **목형별 옵션**으로 연다 (`opt.glueTabW` = 탭 실폭 mm)
//  ────────────────────────────────────────────────────────────────
//  위 「⚠ 대가」 절이 정답이라고 적어 두고 범위 밖이라며 openQuestions 로 넘긴 그
//  (b) 「목형별 탭 폭을 입력으로 받는다」가 이것이다. 노브가 생기면 위 세 대가가
//  전부 성질이 바뀐다:
//    · 니치어형(24.03) → 그 목형에 24.03 을 주면 겹침 0.00. 봉투가 필요 없다.
//    · 다빈기획 하4 「견적 미산출」 회귀 → 실측 14.70 이면 netW 594.0 ≤ 597 로 소멸.
//    · 십자B 70×70×55 @하4 → 견적서 4up 이 재현된다(≤19.2 구간).
//
//  노브는 **탭 실폭**이다(가산분이 아니다). netW 가산분 = 탭폭 − GLUE_TAB_SHORT 로
//  geometry 의 규약(탭이 붙는 D 패널이 0.70mm 짧다)을 그대로 쓴다. geometry.xEdges 가
//  탭 폭을 netW 에서 역산하므로 여기만 바꾸면 폴리곤의 탭도 같이 움직이고 bbox
//  불변식은 자동으로 지켜진다.
//
//  ★ 기본값 = 위 CROSS_TAB(가산분 23.33 = 탭 24.03) — **현행 봉투를 유지한다.**
//    26-08-25 계량은 기본값을 15.70(4/5 실측을 덮는 다수결)으로 내리는 안을 권고했고
//    점수도 그쪽이 높다(verify-net 6→7 · scorecard2 8→10 · scorecard-unit §A 6→8).
//    그런데 그 대가가 실측돼 있다 — **탭이 실제로 24.03 인 목형(니치어형)을 입력 없이
//    기본값으로 계산하면 자동선택 판형 국전 6up 에서 실물 겹침 1,169.3mm²**
//    (탭 19.2 로 해도 673.4mm²). 즉 기본값 15.70 은 「노브가 실제로 채워질 것」에 거는
//    베팅이고, 안 채워진 순간 물리적으로 불가능한 판이 자동선택 판형에서 나온다.
//    이 저장소의 절대선은 「실측 목형은 **자기 옵션으로** 겹침 0」이고 그 위에
//    「기본값은 안전 봉투」가 온다 — 기본값을 낙관하는 것은 그 두 줄과 어긋난다.
//    ⟹ 기본은 봉투로 두고, 목형을 아는 사용자가 실폭을 넣으면 즉시 견적서 up 이 난다.
//      화면(BoxSpec)이 「목형 미지정 — 표준 가정(탭 24.03)」이라고 **말한다.**
//      기본값을 내리려면 니치어형 목형에 대한 방어를 먼저 만들어라(예: verify-imposition
//      이 목형별 옵션을 실은 뒤, 또는 탭 미지정 시 화면 경고를 차단성으로 올린 뒤).
export const CROSS_TAB_W = CROSS_TAB + GLUE_TAB_SHORT;   // 24.03 — 기본 탭 실폭

// 실측 표본은 12.03~24.03 에 있다. 그 밖의 값은 오타로 보고 기본 봉투로 접는다 —
// 조용히 clamp 하면 「넣은 값과 다른 값으로 계산됐다」가 되므로 접은 사실은 화면이 말한다.
// 하한 5 는 폴리곤 위상(가산분 > 0)의 방어선이다.
//
// ★ 26-08-25 — 이 범위를 **레지스트리에 수출한다**(TAB_MIN/TAB_MAX). 왜:
//   종전에는 접기가 여기(netSize 안)에서만 일어나고 pickDieOpt 는 빈값만 걸렀다.
//   그래서 `dieline.dieOpt.glueTabW` 에 **접히기 전 원값**(예: 999)이 남았고,
//   BoxSpec 은 그 값을 「적용된 값」이라 믿고 「✓ 목형 지정 — 접착탭 실폭 999mm」
//   이라고 진술했다. 화면이 거짓말을 하는 것 자체도 문제지만 **방향이 더 나쁘다** —
//   상한 초과(61)를 넣으면 모델 netW 가 실물보다 36.97mm **작아지고**(303.33 vs 340.30)
//   up 이 늘어 최대 −36.6% 싸게 청구된다 = 안전방향 표의 최악 칸(겹치는 판 + 싸게 부름).
//   ⟹ 접는 판정을 pickDieOpt 로 올려 dieline.dieOpt 에 **유효값만** 들어가게 했다.
//     화면은 한 줄도 안 고쳐도 접힌 건이 「⚠ 목형 미지정」으로 뜬다.
export const TAB_MIN = 5, TAB_MAX = 60;
/** 이 값이 노브로 받아들여지는가 — pickDieOpt 와 netSize 가 **같은 판정**을 쓴다. */
export const isValidTabW = v => Number.isFinite(Number(v))
                             && Number(v) >= TAB_MIN && Number(v) <= TAB_MAX;
export const crossTabW = opt => (isValidTabW(opt?.glueTabW) ? Number(opt.glueTabW) : CROSS_TAB_W);

export default {
  id: "cross", label: "십자조립 (크로스바텀)", polygon: true,

  netSize(W, D, H, opt = {}) {
    const { hangTab = 0 } = opt;
    const topLid = LID(D);       // 위 띠 — 실측 5벌 안전 봉투
    const botFloor = FLOOR(D);   // 아래 띠 — 실측 5벌 안전 봉투
    // netW 는 공용 GLUE_TAB 이 아니라 목형별 탭폭을 쓴다 — 위 「접착탭 폭이 십자만 산포한다」.
    const tabAdd = crossTabW(opt) - GLUE_TAB_SHORT;
    return { netW: 2 * (W + D) + tabAdd, netH: H + topLid + botFloor,
             topLid, botFloor, glueTab: tabAdd, hangTab };
  },

  flaps(W, D, H, opt = {}) {
    const ns = this.netSize(W, D, H, opt);
    // 더스트는 뚜껑보다 낮다 — 봉투가 뚜껑을 넘지 않게 clamp. 5벌 어디서도 안 걸린다
    // (0.55D+5 < 1.086D+11.3 이 D>0 에서 항상 참) — 방어용이다.
    const dust = Math.min(DUST(D), ns.topLid);
    // 위 띠 = [더스트, 뚜껑, 더스트, 뚜껑]. 4번째가 0 이 아닌 이유는 위 「위상」 절 참조 —
    //   뚜껑이 idx1 인 목형 3벌과 idx3 인 목형 2벌이 섞여 있어 한쪽만 그리면 반드시 진다.
    // 아래 띠 = 네 날개가 서로 물려 바닥을 짜는 십자 잠금이라 **네 장 전부 전폭**이다.
    //   실측 D패널/W패널 비: 니치어 32/49=0.65 · 다빈기획 68/70=0.97 · 뉴로티엑스 14/14=1.00 ·
    //   칼선-09 20/30=0.67 · 칼선-10 23.5/34.5=0.68. 종전 0.55 는 3건 **전부** 과소였다.
    //   비가 0.65~1.00 으로 흩어져 상수를 못 뽑으므로 1.0(= botFloor)을 쓴다 — 과대가 안전 방향이다.
    return { type: "cross", tipW: null,
      top: [dust, ns.topLid, dust, ns.topLid],
      bot: [ns.botFloor, ns.botFloor, ns.botFloor, ns.botFloor] };
  },
};
