import { GLUE_TAB, TIP_W } from "./geometry.mjs";

/**
 * 삼면접착 자동바닥(크래시록)
 *
 * 칼선 벡터 실측 3건으로 확정 (2026-08-04). 미지수 3개 · 3점 연립.
 *   웨이크버니A  W46  D46  H138  위띠 35.0  아래띠 61.0  netH 234.0
 *   웨이크버니B  W36  D36  H168  위띠 28.0  아래띠 51.0  netH 247.0
 *   소스코       W140 D43  H130  위띠 35.5  아래띠 57.9  netH 223.5
 * 소스코가 W≠D 라 처음으로 D·W 계수를 분리할 수 있었다.
 *
 *   위 띠  s = 0.67D + 0.03W + 2.8     3건 오차 0.31mm 이내
 *   아래 띠 b = D + 15                  3건 오차 0.1mm 이내
 *                                       (W 계수 −0.001 ≈ 0 — 폭은 무관)
 *   netH = H + s + b                    3건 오차 0.3mm 이내
 *
 * b = D + 15 는 물리적으로도 맞다 — 뚜껑이 깊이 D 를 덮고 15mm 혀가 더 나간다.
 * 종전 공식(0.88D+0.09W+20.6 / 0.33D+0.15W+11)은 bbox 만 보고 맞춘 회귀라
 * W 계수가 실제로는 없는데 있었고, 소스코에서 netH 를 23.4mm 크게 냈다.
 *
 * 큰 띠가 **아래**, 작은 띠가 **위**다. 종전에는 뒤바뀌어 있었고
 * 합이 비슷해 netH 로는 안 보였지만 빈 구멍 위치가 뒤집혀 맞물림이 통째로 틀렸다.
 * (몸통은 3건 모두 정확 — A 138.0 / B 168.0 / C 130.1 = H)
 *
 * ★ 26-08-16 재실측(도솔 50×50×150 이 4번째 표본으로 들어옴)에서도 위 두 식은 살아남았다.
 *   s 잔차 −0.21mm · b 잔차 +0.01mm. LSQ 재적합은 0.6866D+0.0261W+2.305 (MAE 0.049)
 *   이지만 4표본·3계수로 자유도 1 이라 채택하지 않았다. **netSize 는 손대지 않는다** —
 *   이번 변경은 날개 깊이의 **재분배**이지 netW·netH 총합 변경이 아니다.
 *
 * ★ 26-08-18 5번째 표본(칼선-14 80×45×30) — 두 식 모두 계속 산다.
 *     s 실측 34.50 vs 공식(D=44.5) 35.015 → +0.52 / b 실측 60.00 vs 59.50 → −0.50.
 *     ⚠ 그 −0.50 은 공식이 아니라 **발주 표기(D 44.5)와 목형(D 45.0)의 차이**다.
 *       D=45 로 넣으면 b 잔차 0.00 이다(60.0 = 45+15). netW 도 공식 263.3 vs 실측
 *       264.30 인데 D=45 면 264.30 으로 정확히 맞는다.
 *   ⚠ 도솔 s 잔차 −0.21mm 는 여전히 **음수 = 겹침 방향**이고, 실제로 잡힌다:
 *     격자 배치가 dy = netH 로 맞붙을 때 nest 는 clearance 를 더하지 않으므로
 *     (nest.mjs solveLayout 「겹침 0 배치도 항상 후보」) 실물이 0.2mm 파고든다 —
 *     실측 겹침 3.3~3.4mm². **이 건은 날개 프로파일이 아니라 s 공식의 문제**이고
 *     고치려면 netSize 를 건드려야 해서 이번 범위 밖이다. 아래 flaps 주석 A/B 표에서
 *     「기존」으로 분리해 계량한다.
 *
 * ══════════════════════════════════════════════════════════════════
 *  ★ 좌우 반전을 안 하는 이유 (26-08-16 판정 · geometry.PANEL_LABELS 에서 참조)
 * ══════════════════════════════════════════════════════════════════
 *  고객사 칼선 PDF 는 접착탭이 **왼쪽**이고 우리 xEdges 는 탭이 **오른쪽**이다.
 *  「실물 도면과 같은 방향으로 그리자」는 유혹이 있지만 하지 않는다.
 *
 *  ① 반전하면 패널 **타입** 순서가 현행과 정확히 일치한다:
 *       실측 반전 [D−0.7][W][D][W][15.0]  ↔  현행 xEdges [D−0.7][W][D][W][15.0]
 *     즉 현행은 이미 같은 목형을 뒤에서 본 그림이고, 좌우 반전은 인쇄판 반전과 같아
 *     **물리적으로 동일한 목형**이다. 얻는 것은 그림 방향 하나뿐이다.
 *  ② 잃는 것은 폴리곤·배치·15개 스위트·§12 도면표기(패널 인덱스로 접착면을 가른다)·
 *     verify-drag-shape 61건 픽스처 전부다.
 *  ③ 반전이 **못 고치는 것**도 확인했다 — 「탭에 붙은 W 패널」은 반전 불변이므로
 *     소스코의 깊은날개 반대편 배치는 좌우 문제가 아니다(위 flaps 주석 참조).
 *  ④ 탭 위치는 애초에 목형마다 갈린다. 왼쪽 5건(소스코·니치어·다빈기획·엔이이에이·C017)
 *     오른쪽 3건(뉴로티엑스·T002·iSHAP). 「탭이 왼쪽 6/6」은 삼면접착·맞뚜껑만 본 표본이라
 *     반전해도 「실물과 같은 그림」이 되지 않는다.
 *  ⟹ 그림 방향이 필요하면 **렌더에만** scale(-1,1) 을 걸어라 (기하·배치 0줄 수정).
 *
 *  ※ 「거울 양쪽을 다 평가해 큰 up 을 쓰자」도 계량했다 — §A 8/12 로 **당시 현행과 동점**
 *    이었다(LUXEN 만 갈리고 나머지 11건 불변). 얻는 점수가 0 인데 배치 탐색이 2배가 되고
 *    판걸이 캐시 키·손배치 프레임 예산에 거울 축이 붙는다. 채택하지 않는다.
 *    ⚠ 26-08-18 이후로는 애초에 성립하지 않는다 — 아래 띠가 양쪽 W 다 깊은 봉투라
 *      거울상이 원본과 같은 패널 배치를 갖는다(위 띠도 4패널 동일). 거울을 돌릴 이유가 없다.
 *
 * ══════════════════════════════════════════════════════════════════
 *  이번에 **반영하지 않은** 후보 (근거부족 — 지우지 마라, 다시 시도하게 된다)
 * ══════════════════════════════════════════════════════════════════
 *  · 위 띠 s = 0.67D + 3.2 (W 항을 상수로): bbox 10건 MAE 1.64 vs 현행 2.06mm 로 좋아
 *    보이지만, 그건 netH 총합 적합이다. **띠를 직접 잰 4건**에서는 현행이 MAE 0.140mm 다.
 *    bbox 회귀로 직접 실측을 덮지 마라 — 이 프로젝트가 이미 한 번 밟은 함정이다.
 *  · 위 띠 D 패널 = D/2 (26-08-16~18 사이 잠깐 채택했던 값): 웨이크A·웨이크B·도솔·소스코
 *    4표본에서 잔차 ±0.02mm(MAE 0.013) 로 **정확하다.** 그런데 5번째 표본 칼선-14 가
 *    34.5 라 D/2=22.25 로는 −12.25mm 부족하고, 부족은 겹침 방향이라 못 쓴다.
 *    ⟹ 안전 봉투 s 로 올렸다(아래 flaps ③ 참조). 비용은 측정 범위에서 0 이다.
 *    되살리려면 「칼선-14 만 s 인 조건」을 실측으로 특정해라 — H=30 의 극단 납작이
 *    유일한 후보인데 1표본이라 문턱을 세울 수 없다.
 *  · 위 띠 D 패널 = 0.63·s (비율 모델): 4건에서 MAE 0.646mm(최대 +1.23) 로 D/2 의
 *    50배다. 3표본을 「s 대비 비」로 읽으면 0.61/0.66/0.66 이 나오지만, 그 세 값은
 *    사실 전부 정확히 D/2 다 — 자유계수가 없는 쪽이 이긴다.
 *  · 아래 띠 「W≫D 면 탭에 붙은 W 가 깊다」 문턱 모델: 5/5 를 가르는 것처럼 보이지만
 *    **소스코 자신의 아래 띠가 반증한다**(W2 에도 30.0 이 있어 한쪽만 깊은 게 아니다).
 *    자세한 계량은 아래 flaps 주석 참조.
 *  · iSHAP 120×150×80 (netH 실측 324.16): 삼면접착 변종으로 풀린다. 크리스로 재면
 *    W≈150.5 · D≈120.5 (발주 표기와 W·D 가 뒤바뀌어 있다)이고 뚜껑 D+16.2 는 정상인데
 *    **자동바닥 띠만 77.7 로 짧다**(현행식 106.9). 뚜껑 안에 W/2 오시 + 원형 타공이 있어
 *    판걸이 헤더가 달린 목형이다. **표본 1건이라 공식을 세우지 않는다.**
 *    ⚠ 현행식은 netH 를 303.55 로 내 실측보다 **20.6mm 작다 = 안전 방향 위반**이고,
 *      이번 날개 재분배로는 풀리지 않는다(netH 는 s·b 가 정하고 패널 분배는 관여 안 한다).
 */
export default {
  id: "glue_3side", label: "삼면접착", polygon: true,

  netSize(W, D, H, { hangTab = 0 } = {}) {
    const topLid   = 0.67 * D + 0.03 * W + 2.8;   // 작은 띠 (위)
    const botFloor = D + 15;                      // 큰 띠 = 뚜껑 (아래)
    return { netW: 2 * (W + D) + GLUE_TAB, netH: H + topLid + botFloor,
             topLid, botFloor, glueTab: GLUE_TAB, hangTab };
  },

  // ══════════════════════════════════════════════════════════════
  //  날개 프로파일 — 26-08-18 목형 5벌 재실측으로 **안전 봉투**로 교체
  //
  //  ★ 측정 방법 (앞선 두 번의 오독이 전부 방법 문제였다 — 그래서 방법부터 적는다)
  //    ① **획(op=S)만** 쓴다. 아트워크 색면(op=f)은 도련이 붙어 칼선보다 1.7~2.8mm
  //       크게 나온다. 이걸 안 가르면 날개가 실제보다 깊게 읽힌다(아래 「④ 판정」).
  //    ② 몸통 가로 접는선을 기준선으로 잡고 패널 구간을 0.25mm 로 세로 스캔해
  //       「몸통 밖으로 나간 칼선의 최원점」을 잰다. 윤곽 추적이 아니라 점유 스캔이라
  //       둥근 모서리·열린 폴리라인에 강하다.
  //    ③ 패널마다 세로 span ≥ H 인지 완전성 게이트를 건다 — 안 걸면 결측을
  //       「날개 0」으로 조용히 오독한다(실제로 한 번 밟았다).
  //
  //  ★ 오라클 — 목형 5벌 (현행 idx 0..3 = [D2, W2, D1, W1])
  //    실측 프레임은 [탭 15][W1][D1][W2][D2−0.7] 이고 우리 프레임은 그 거울상이다.
  //    사상: 실측 [탭,W1,D1,W2,D2] ↔ 현행 [4,3,2,1,0]  (geometry.PANEL_LABELS 참조)
  //
  //      표본       W×D×H       W/D    위 띠 idx0..3          아래 띠 idx0..3
  //      웨이크A   46×46×138   1.00   23.0 35.0 23.0 35.0   23.0 61.0 23.0  0.0
  //      웨이크B   36×36×168   1.00   18.0 28.0 18.0 28.0   18.0 51.0 18.0  0.0
  //      도솔      50×50×150   1.00   25.0 38.0 25.0 38.0   24.0 65.0 24.0  0.0
  //      소스코   140×43×130   3.26   21.5 35.5 21.5 35.5   29.0 30.0 29.0 58.0 ←W1 깊음
  //      칼선-14   80×45×30    1.78   34.5 34.5 34.5 34.5   30.0  0.0 30.0 60.0 ←W1 깊음
  //    파일(영업기밀 — 저장소 밖):
  //      소스코    …/고객사_문의중/소스코/디자인/C5AF4AD1-BB73-4581-A7B3-93697D2145DC.pdf
  //                몸통 y 81.47~211.47 · 패널 x 경계 31.69/171.69/214.69/354.69/396.99
  //      웨이크    …/고객사_문의중/웨이크버니/디자인/웨이크버니삼면접착2종.pdf (p0·p1)
  //      도솔      …/도솔메디/…경옥고3입_패키지.pdf  몸통 y 45.34~195.34
  //      칼선-14   private/도면/견적용도면/24~/240717_엔이이에이/240717_엔이이에이.pdf
  //                도면이 90° 눕어 있다 — 몸통은 **pdf x** 68.13~98.13 (=H 30.00)
  //    ⚠ 소스코는 오라클 PDF 가 2개고 `삼면접착140x43x130.pdf`(선분 128개) 쪽은
  //      아래 띠 D1·W2 외곽선이 **결측**이라 W2 를 0 으로 읽게 만든다. 위 C5AF… 를 써라.
  //
  //  ★ ④ 판정 — 「소스코 D1 아래 날개가 29.02 가 아니라 31.81」 이라는 지적은 **반증됐다**
  //    두 주장을 원본에서 독립으로 다시 쟀다:
  //      · y=243.28 에 x 171.70~214.69 (폭 42.99 = D1 전폭) 수평선이 **실재한다.**
  //        그러나 그것은 콘텐트 스트림 **7번째 서브패스(#6)이고 op=f(칠하기)** 다 —
  //        닫힌 사각형 42.99×31.82 @(171.70,211.47). 같은 무리의 #5 는 45.72×31.81
  //        @(354.69,…) 로 **칼선 오른쪽 끝 396.99 를 3.42mm 넘어간다**(도련). 즉 이 7개
  //        (#0~#6)는 패널마다 깔린 아트워크 색면이고 칼선일 수 없다.
  //      · 칼선은 **#2225, op=S** 이고 D1 날개 윤곽이 (213.99,211.47)→(213.99,218.47)→
  //        (209.99,222.47)→(205.96,237.50)→…→(202.09,240.47)→(175.64,240.47)→
  //        (174.18,212.65) 다. 최원점 y=240.47 ⟹ **깊이 29.00**.
  //    ⟹ 29.02(=29.00) 가 맞다. min(W,b)/2 = min(140,58)/2 = 29.00 · 잔차 0.00.
  //       31.81 은 아트워크 도련(240.47 → 243.28, +2.81)이다.
  //    ※ 아래 띠 색면이 칼선보다 큰 것은 이 파일 전체에서 일관된다:
  //       W1 아트 271.14 vs 칼선 269.47 · W2 아트 243.31 vs 칼선 241.47.
  //
  //  ★ ② 정정 — 반례는 1건이 아니라 **2건**이다 (실측 3/5)
  //    칼선-14(240717 엔이이에이)를 이번 커밋이 cross → glue3 로 옮겼는데, 그 표본이
  //    소스코와 **같은 계열**이다(깊은 날개가 탭에 붙은 W1). 자기 커밋이 늘린 표본을
  //    근거 집계에서 빼면 안 된다. 종전 주석의 「실측 3/4 · 반례 1건」은 3/5 로 고친다.
  //
  //  ══ ★★ 26-08-19 — 「안전 봉투」라는 이름을 믿지 마라 (표현 정정) ══
  //    아래 글에서 봉투를 「최소 안전 봉투」라 부르는데, 그 말은 **여유가 있다**로
  //    읽힌다. 실제로는 점적합에 가깝다. verify-imposition 잔차 게이트가 재는 슬롯
  //    (목형 12벌 × netW·netH·날개 8칸)을 세어 보면:
  //        **여유 정확히 0.00 인 칸 31개** — 표본 하나만 더 들어와도 거기가 먼저 깨진다
  //        음수(부족) 9칸 — 전부 원인이 특정돼 예산으로 세워져 있다
  //    특히 이 파일이 정한 값들의 여유는 이렇다:
  //        아래 띠 b = D+15  : 소스코·웨이크A·웨이크B·도솔 4칸이 **0.00**
  //        botDust = min(W,b)/2 : 5표본 중 4표본이 **0.00**, 남은 하나는 −0.25(표기차)
  //        위 띠 s          : 웨이크A·웨이크B 가 **0.00**, 도솔은 −0.20(예산)
  //    ⟹ 「봉투」는 관측된 5벌을 **딱 스치는** 상한이지 여유 있는 포장이 아니다.
  //      게이트가 그 0.00 칸 목록을 매번 찍는다 — 목형이 하나 들어오면 그 줄부터 봐라.
  // ══════════════════════════════════════════════════════════════
  // ══════════════════════════════════════════════════════════════
  //  ★ 26-08-25 — 봉투를 **목형별 옵션**으로 연다 (`opt.deepFlap`)
  //  ──────────────────────────────────────────────────────────
  //  왜 지금 여는가 — 바로 아래 「⚠⚠ 가장 큰 부작용」 주석이 예고한 그 조건이
  //  충족됐다: 「맞물림으로 up 을 버는 목형이 견적서로 확보되면 그때는 봉투가 아니라
  //  **구조 옵션 입력**(목형별 깊은날개 위치)으로 풀어야 한다」. 확보된 견적서 2건 —
  //    LUXEN 130×130×55 @480×788  2up (2×1 맞물림, 발자국 91%)
  //    웨이크버니B 36×36×168 @636×469  6up (3×2 맞물림)
  //  둘 다 `deepFlap` 이 both 가 아니어야만 재현된다. 봉투로는 원리적으로 못 낸다.
  //
  //  값 (탭은 idx4 = 오른쪽 끝이므로 idx1 이 「먼 W」· idx3 이 「붙은 W」):
  //    "both" — [dust, b, dust, b]  기본값. 두 계열을 동시에 덮는 수학적 최소 상한
  //    "far"  — [dust, b, dust, dust]  깊은 날개가 탭에서 **먼** W(idx1)
  //    "near" — [dust, dust, dust, b]  깊은 날개가 탭에 **붙은** W(idx3)
  //
  //  ★ 기본값을 far 로 낙관하지 않은 근거 (26-08-25 계량, 다수결이 아슬아슬하다)
  //    실측 5벌: far 3(웨이크A·웨이크B·도솔 · 전부 W/D=1.00) / near 2(소스코 W/D=3.26 ·
  //    칼선-14 W/D=1.78). 3/5 = 60% 는 40% 가 반대편이라는 뜻이고, 기본값 far 로
  //    미상 목형이 실제 near 였을 때의 대가가 실측돼 있다 —
  //      소스코를 far 로 그리면 실물 겹침 **2,824.7mm² @하4 2up**(수동 선택 가능한 표준 판형)
  //      칼선-14 를 far 로 그리면 **397.5mm² @46**
  //    both 는 12벌 전부 겹침 0(도솔 0.47 · 칼선-14 22.23 = 기존 예산)이다.
  //    잃는 것(견적서 2건)은 목형을 아는 사용자가 far 를 켜면 즉시 회복되지만,
  //    얻는 것(물리적으로 불가능한 판)은 회복되지 않는다.
  //
  //  ★ W:D 비로 **자동 판정하는 안은 기각**했다. 5/5 실측 + LUXEN 이 W/D=1.00↔far,
  //    W/D≥1.78↔near 로 깨끗하게 갈리지만, near 를 실제로 켜면 소스코가 mm 단위에서
  //    깨진다 — 얕은 쪽 dust(29.0)가 실측 W2 30.0 에 **1.0mm 부족**해 겹침 51.98mm².
  //    (그래서 소스코·칼선-14 는 실측이 near 여도 채택은 both 다 — 아래 ⚠ 두 자리.)
  //
  //  ★ 얕은 쪽 깊이는 **노브가 아니다** — `botDust`(= min(W,b)/2) 로 못박는다.
  //    far 3벌의 얕은 쪽 실측은 전부 0.0 이므로 dust 는 이미 과대(=안전)이고,
  //    0.70D 로 올리면 LUXEN 2up 을 잃는다(계량 확인). 0 으로 내리면 소스코 오설정
  //    대가가 2,824.7 → 5,033.8mm² 로 커진다(후자는 종전 기록값).
  //
  //  ⚠ 실측이 near 인데 near 를 **못 쓰는** 자리 둘 (계량의 가장 중요한 발견)
  //    ① 칼선-14 80×44.5×30 — near 로 그리면 모델이 작아져 nest 가 @46 8×2↺ 16up
  //       맞물림을 열고 실물 겹침 397.5mm². 발주 표기 D 44.5 → 목형 실측 45.00 으로
  //       고쳐 잔차를 8칸 전부 0.00 으로 만들어도 **343.5mm² 가 남는다** — 즉 표기차가
  //       아니다. 진짜 원인은 **모델이 접착탭(idx4)에 아래 날개를 못 그린다**는 것
  //       (`flaps.top/bot` 이 4칸뿐이라 탭에 날개를 줄 통로가 없다 · 커버리지 예산
  //       733.9mm²/54.27mm). H=30 극단 납작이라 아래 띠(59.5)가 몸통보다 두 배 깊어
  //       그 결손이 판에서 실제 충돌로 나타난다. **탭 날개를 그릴 수 있기 전까지 both.**
  //    ② 소스코 140×43×130 — dust(29.0) 는 실측 W2 30.0 에 1.0mm 부족(겹침 51.98mm²),
  //       0.70D(30.1) 는 겹침 0 이지만 커버리지가 941.7 → 1,001.3 으로 예산 942.5 를
  //       넘어 ★신규 적신호(30.1 깊이 날개에 테이퍼가 걸려 가장자리 재료가 사라진다).
  //       다행히 소스코 up 은 4모드 전부 4up 이라 **near 로 얻을 것이 0** 이다.
  //  ⟹ 두 목형은 옵션 없이 기본(both)으로 두는 것이 「자기 옵션으로 겹침 0」이다.
  //
  //  ⚠ 여기에 있던 계량용 환경변수 스위치(G3MODE)를 삭제한 이유는 flaps 반환문 위
  //    주석이 소유한다 — **src/ 안에서 `process` 를 참조하지 마라.** 이 노브는 그래서
  //    환경변수가 아니라 **함수 인자**다.
  // ══════════════════════════════════════════════════════════════
  flaps(W, D, H, opt = {}) {
    const ns = this.netSize(W, D, H, opt);
    const s = ns.topLid, b = ns.botFloor;

    // ── 위 띠 (자동바닥) — 네 패널 전부 s (안전 봉투) ────────────────
    // ★ ③ 수정. 종전 [D/2, s, D/2, s] 는 D 패널을 D/2 로 그렸는데, 칼선-14 는
    //   **네 패널 전부 34.5** 다(x=132.63 에서 pdf-y 14.36~28.16 / 79.16~92.96 /
    //   116.29~137.96 / 163.66~193.66 — 네 패널 각각이 최심부에 닿는다).
    //   D/2 = 22.25 로 그리면 **−12.25mm 부족** = 겹침 방향이다.
    //   면책도 성립하지 않는다 — 종전 주석은 「D 검증범위 36~50 밖」이라 적었지만
    //   D=44.5 는 그 범위 **안**이고, 「D 패널 30.0」이라 적은 값은 사실 **아래 띠**
    //   더스트다(그건 오히려 min(W,b)/2 = 30.0 을 지지한다). 위 띠는 34.5 다.
    //
    // 그래서 D 패널도 s 로 올린다. s ≥ D/2 는 항상 참이므로(s−D/2 = 0.17D+0.03W+2.8 > 0)
    // **s 는 자유계수 0개짜리 상한**이다. 「봉투」라고 부르지만 W 패널 쪽 여유는
    // 아래 잔차에서 보듯 0.00~+0.52 뿐이다(위 「★★ 26-08-19」 참조). 5표본 잔차:
    //   W 패널 +0.00/+0.00/−0.21/+0.31/+0.52   D 패널 +12.0/+10.0/+12.8/+14.3/+0.52
    // ⚠ D/2 는 4/5 표본에서 잔차 ±0.02mm 로 **정확했다**(MAE 0.013). 그걸 버리는 게
    //   아까워 보이지만, 틀리는 방향이 겹침이라 못 쓴다. 대신 값이 싼지 확인했다 —
    //   [D/2,s,D/2,s] → [s,s,s,s] 로 바꿔도 실측 7목형×표준판형 10종 **70조합의 up 이
    //   한 칸도 안 움직인다**(합계 411 동일). scorecard2·scorecard-unit·verify-net 도 동일.
    //   즉 이 봉투의 비용은 측정 가능한 범위에서 **0** 이다.
    // ⚠ 이 절을 지키는 게이트는 **겹침 게이트가 아니라 잔차 게이트**다. D/2 로 되돌리는
    //   돌연변이를 넣어보면 실물 겹침은 66조합 전부 0 이라 겹침 게이트가 통과시킨다
    //   (그 빈 자리를 지금 격자 솔버가 마침 안 쓴다). 잡는 것은
    //   verify-imposition 「잔차 게이트 — 모델 날개 깊이 ≥ 실측」이고 −12.25mm 로 죽는다.
    //   두 게이트가 보는 것이 다르다는 뜻이다 — 하나만 믿지 마라.
    // ⚠ 부작용은 안다 — 위 띠가 통짜 사각형이 되어 그쪽 맞물림 공간이 사라진다.
    //   단, **위 띠가 맞물림을 죽인 원인은 아니다**: [D/2,s,D/2,s] 를 그대로 두고 아래 띠만
    //   봉투로 올려도 15케이스 배치가 이미 전부 「칼선공유」다(측정 확인). 아래 참조.
    //   D/2 로 되돌리고 싶으면 「칼선-14 를 덮는 문턱」을 먼저 실측으로 찾아라.

    // ── 아래 띠 (뚜껑) — 양쪽 W 패널 다 깊게 (안전 봉투) ──────────────
    // ★ ① 수정. 종전은 깊은 날개를 idx 1(탭에서 먼 W)에 **고정**했다. 그러면
    //   소스코형 목형에서 idx 3(W1)을 0 으로 그리고, 격자 솔버가 그 빈자리를 물고
    //   들어간다 — 소스코 140×43×130 @ 하4(444×597) 에서
    //     dy 223.81→181.00 (세로 물림 42.81) + sx 190.15 반피치 엇갈 = 2up
    //   을 고르는데, 실물 W1 은 폭 140 전체에 58mm 날개가 있으므로 이 판은
    //   **물리적으로 불가능하다** (독립 격자 샘플링 0.3mm · NFP 미사용: 5,033.8mm²).
    //   ⚠ up 이 2 로 같아서 **금액이 안 갈린다** — 어떤 금액 스코어카드도 이걸 못 잡았고
    //     bbox 도 같아서 verify-dieline 불변식도 못 잡았다. 그래서 게이트를 새로 만들었다:
    //     verify-imposition 「실측 프로파일 겹침 게이트」(목형 7벌 × 표준판형 10종).
    //     이 줄을 [.., b, .., 0] 으로 되돌리면 거기서 exit 1 로 죽는다(확인함).
    //
    // 왜 「어느 쪽인지 고르기」로는 못 푸는가 — 실측이 그걸 허용하지 않는다:
    //   idx1(W2) 실측 최대 = 도솔 65.0 = b  ⟹ 모델 idx1 ≥ b
    //   idx3(W1) 실측 최대 = 칼선-14 60.0 = b · 소스코 58.0 ≈ b  ⟹ 모델 idx3 ≥ b
    //   두 부등식이 동시에 성립해야 하므로 **[.., b, .., b] 가 최소 상한**이다.
    //   이건 선택이 아니라 측정 결과다. 다만 그 상한의 여유는 **정확히 0** 이다 —
    //   두 부등식이 등호로 성립한다(도솔 65.0 = b · 칼선-14 60.0 = b). 위 「★★」 참조.
    //
    // ⚠ 「W≫D 면 붙은 쪽, W≈D 면 먼 쪽」 가설(먼쪽 3건 전부 W=D · 붙은쪽 2건 W/D 3.26·1.78)
    //   은 **소스코 자신의 실측이 반증한다.** 소스코는 「한쪽만 깊다」가 아니다 —
    //   아래 띠가 [D2 29.0, W2 30.0, D1 29.0, W1 58.0] 로 **W2 에도 30.0 이 있다.**
    //   가설대로 붙은 쪽으로 넘기면 W2 를 0 으로 그려 −30.0mm 부족해지고, 같은 하4
    //   배치에서 겹침이 56.2mm² 로 남는다(0 이 아니다). 자유 문턱을 하나 도입하는
    //   5표본 적합인데다 **적합 대상 표본조차 못 덮는다** — 채택하지 않는다.
    //
    // D 패널 더스트 = min(W, b)/2 — 5표본 잔차 0.00/0.00/+1.00/0.00/−0.25 (MAE 0.25).
    //   물리 해석: D 패널 날개는 접히면 W 방향으로 들어가므로 서로 안 겹치려면 ≤ W/2
    //   (웨이크A 23.0=W/2 · 웨이크B 18.0=W/2 가 정확히 맞는다).
    //   ⚠ 유일한 음수 −0.25 는 칼선-14 다. 그런데 그 목형의 **실측 D 는 44.5 가 아니라
    //     45.00** 이다(패널 폭 93.66→138.66 = 45.00 · 마지막 패널 44.30 = D−0.7).
    //     D=45 로 넣으면 b=60.0 · min(80,60)/2 = 30.0 으로 잔차 0.00 이다. 즉 이 −0.25 는
    //     공식이 아니라 **발주 표기(44.5)와 목형(45.0)의 차이**다. netW 도 같은 이유로
    //     공식 263.3 vs 실측 264.30 (+1.00) 이다.
    //   ⚠ 상한이 b/2 라는 **형태는 소스코 1표본**만 결정한다(W=140 ≫ b=58 인 유일한
    //     표본). min(W/2, 0.675D) 도 사실상 같은 MAE 로 구분 불가다. 표본이 늘면 재판정.
    const botDust = Math.min(W, b) / 2;

    // ★ 이 봉투가 실제로 무엇을 잃는지 (숨기지 않는다 — 26-08-18 실측 A/B)
    //   잃는 것 2건, 둘 다 **앱 실경로(solveImposition)에서도** 잃는다:
    //     · LUXEN 130×130×55 @480×788   2up → 1up   (견적서 2up 재현 상실)
    //     · 웨이크버니B 36×36×168 @636×469  6up → 4up (견적서 6up 재현 상실)
    //   ⚠ 「원시 네스터에서만 지고 앱은 MAX_FOOT_PCT 가 흡수한다」는 완화 논리는
    //     **웨이크버니B 에서 성립하지 않는다.** 현행(먼쪽)은 원시 8up → 앱 6up 로
    //     상한이 흡수하지만, 이 봉투는 원시가 이미 4up 이라 흡수할 것이 없다.
    //     비용은 진짜다. 그래도 채택하는 이유는 대안이 「싸고 물리적으로 불가능한 판」이라서다.
    //   점수 A/B (현행 → 이 봉투):
    //     실측 7목형 × 표준판형 10종, 실물 프로파일 격자 샘플링 겹침
    //         신규 겹침 1건 5,033.8mm² → **0건 0.0mm²**
    //         최대 음수 잔차 −60.00mm → **−0.50mm** (그 −0.50 도 위 D=44.5/45.0 표기차)
    //     scorecard-unit §A  8/12 → 7/12   (움직인 것 = LUXEN)
    //     scorecard2(원시)  10/15 → 9/15   (움직인 것 = LUXEN · 웨이크버니B)
    //     verify-net 판걸이  7/10 → 7/10   (불변)
    //     ★ 절대선 소스코 204원/개 — 불변 (4×62 4up 유지)
    //   up 총합(70조합) 421 → 411. 줄어드는 방향이므로 견적은 **비싸지는** 쪽이다.
    //
    // ⚠⚠ 가장 큰 부작용을 따로 적는다 — **삼면접착에서 맞물림이 사실상 사라진다.**
    //   scorecard-unit §A(맞물림 허용) 12건이 전부 「칼선공유」로 바뀌어 §B(맞물림 금지)와
    //   같은 값이 됐다. 아래 띠 양쪽 W 가 다 깊으면 반전 이웃의 깊은 날개와 항상 충돌해서
    //   반피치 엇갈로도 피할 자리가 없기 때문이다(종전 [.., b, .., 0] 은 그 빈 자리를
    //   맞물림 공간으로 썼다 — 그게 소스코형에서 실물 겹침이 된 바로 그 자리다).
    //   ⚠ 맞물림 자체는 **실재한다**: 실측 대지 2건에서 삼면E 20.8mm · 칼선-05 43.9mm
    //     (verify-imposition 「대지 2건 — 맞물림 실측」). 즉 이 봉투는 그 실물을
    //     재현하지 못한다 = 보수적으로 틀린다. 두 대지 모두 up 은 2 로 같으므로 지금은
    //     금액에 안 나타나지만, 「맞물림으로 up 을 버는 목형」이 견적서로 확보되면
    //     그때는 봉투가 아니라 **구조 옵션 입력**(목형별 깊은날개 위치)으로 풀어야 한다.
    //     실측이 두 계열로 갈리는 것이 확정 사실이므로 한 공식으로는 원리적으로 못 덮는다.
    // ⚠ 여기에 있던 계량용 분기 스위치(G3MODE 환경변수 · A/B/C/목형표)는 삭제했다.
    //   왜: src/ 는 브라우저에서 그대로 실행된다. 브라우저에는 `process` 가 없어서
    //   `process.env.G3MODE` 한 줄이 삼면접착 전개도를 그릴 때마다 ReferenceError 를 내고,
    //   App.jsx 의 try/catch 가 그걸 삼켜 견적서·쇼룸이 **에러 표시 없이 빈 화면**이 됐다.
    //   Node(테스트)와 프로덕션 빌드(롤업이 process.env 를 {} 로 접음)에서는 멀쩡해서
    //   16개 스위트가 전부 초록인 채 dev 실행만 조용히 죽는 형태였다.
    //   ★ 규약: src/ 안에서는 `process` 를 참조하지 마라. 계량 분기가 다시 필요하면
    //     환경변수가 아니라 **함수 인자**(목형별 구조 옵션)로 받아라.
    // ── 목형별 노브 ①  깊은 아래날개 위치 ───────────────────────────
    // 모르는 값은 조용히 both 로 접는다 — 링크·해시로 낡은 문자열이 들어와도
    // 「안전 봉투」로 떨어지는 것이 맞다(작은 도형 = 겹치는 판 = 최악).
    const deep = opt.deepFlap === "far" || opt.deepFlap === "near" ? opt.deepFlap : "both";
    // ⚠ 세 줄을 **표로** 적는다(삼항 사슬이 아니라). verify-imposition 돌연변이표가
    //   `both:` 줄을 문자열로 패치해 「봉투 되돌림(W1 b → 0)」을 감시하는데, 정렬 공백이
    //   섞인 삼항 사슬은 서식만 손대도 패치 지점이 사라진다(실제로 한 번 사라졌다).
    const BOT = {
      far:  [botDust, b, botDust, botDust],
      near: [botDust, botDust, botDust, b],
      both: [botDust, b, botDust, b],
    };
    const bot = BOT[deep];
    return { type: "glue3", tipW: TIP_W,
      top: [s, s, s, s],                       // 위 띠: 네 패널 전부 s (③)
      bot };                                   // 아래 띠: 노브 ① (기본 = 양쪽 b = 봉투)
  },
};
