// ══════════════════════════════════════════════════════════════════
// [v8] 코팅·접착·톰슨·후가공 단가 DB  —  docs/ 견적서 78건 실측
// ══════════════════════════════════════════════════════════════════

// ── 코팅 (원/R) ───────────────────────────────────────────────────
// min = 소량(공정R 1식) 금액. 실측: 무광 4×64 55,000 / 하4 60,000 / 하3 70,000
//       4×62 62,400~65,000 / 4×6전지 112,000 / 46전지 양면 126,000
//       IR 4×62·하4 30,000 / 4×64 40,000~48,000 / 국2(ir수성) 50,000 / 전지 60,000
// ── ★ labelJa — 일본 전시회 부스 화면(쇼룸)이 읽는다 (26-08-26) ────────
//  왜 데이터에 붙나: 화면에서 문자열을 다시 지으면 견적 앱과 쇼룸이 갈린다.
//  ⚠ **비어 있어도 된다.** 없으면 쇼룸이 자동으로 한국어 label 로 떨어진다 —
//    「전부 아니면 전무」가 아니다. 근거 없는 제품명을 지어내는 것이 더 나쁘다.
//  · matte/gloss 를 マットPP/グロスPP 로 적은 근거는 아래 26-08-06 주석이다
//    (「무광라미 → 무광코팅과 같은 것이다」 = 라미네이트 = 일본 실무의 PP 貼り).
//  · hg「글로스코팅」은 **비운다.** 유광코팅과 무엇이 다른지 이 파일에 근거가 없고
//    (단가만 다르다: 4절 65,000 vs 55,000), 高光沢/UV 중 무엇인지 지어낼 수 없다.
export const COAT_OPTS = [
  { id:"none",   label:"없음",         labelJa:"無し",       "4절":0,      "2절":0,      "전지":0,      min:0      },
  { id:"matte",  label:"무광코팅",     labelJa:"マットPP",   "4절":55000,  "2절":65000,  "전지":112000, min:55000  },
  { id:"gloss",  label:"유광코팅",     labelJa:"グロスPP",   "4절":55000,  "2절":65000,  "전지":112000, min:55000  },
  { id:"hg",     label:"글로스코팅",                         "4절":65000,  "2절":65000,  "전지":140000, min:65000  },
  { id:"ir",     label:"IR코팅",       labelJa:"IRコート",   "4절":48000,  "2절":40000,  "전지":60000,  min:40000  },  // 2절: 30,000 → 40,000 (26-08-04 소스코)
  { id:"velvet", label:"벨벳코팅",     labelJa:"ベルベット", "4절":241722, "2절":241722, "전지":241722, min:241722 },
  // ── hidden — 드롭다운에 안 띄우지만 단가 데이터는 살려 둔다 ──────────
  // 부분코팅은 코팅 드롭다운이 아니라 **별도 토글**로 고른다(process/partial-uv.mjs).
  // 그런데 그 공정이 여기서 coatById("part") 로 단가를 읽어간다 —
  // 이 줄을 지우면 coatById 가 COAT_OPTS[0](없음, 0원) 으로 폴백해
  // 부분코팅비가 **조용히 0원**이 된다. 그래서 지우지 않고 숨긴다.
  { id:"part",   label:"부분코팅",     "4절":95000,  "2절":95000,  "전지":95000,  min:95000, hidden:true },
];

// ── 26-08-06 실무 확인으로 드롭다운에서 제거한 항목 ────────────────────
//  · 무광라미  → 무광코팅과 같은 것이다. matte 를 쓴다.
//      ⚠ 단가가 달랐다: 라미 85,000/85,000/115,000 vs 무광 55,000/65,000/112,000.
//        무광라미로 뽑은 과거 견적이 있으면 지금은 무광코팅 단가로 계산된다.
//        (2절 기준 85,000 → 65,000, 20,000원 낮아짐)
//  · 에폭시    → 부분코팅과 같은 것이다. 단가도 95,000 으로 동일했다(교차 확인).
//                부분코팅 토글로 고른다.
//  · 부분코팅  → 코팅이 아니다. 별도 토글 소관. 위 hidden 으로만 남긴다.
//  · 양면코팅(전지) → 후면코팅을 함께 고르면 되는 것이라 별도 항목이 불필요하다.

// ── 접착 (원/EA) ──────────────────────────────────────────────────
// 실측: 단면 15→20 / 삼면 25~30 / 손잡이형 30 / 슬리브 80 / PP 70
//       소량 1식 45,000~50,000
// ★ 26-09-11 — 최소 1식 50,000 → **45,000**. 근거: 26-09-09 샤카에프앤비 더치커피 견적서.
//   같은 견적서 안에 수량 3단이 있어 **최소값과 EA 단가의 경계가 직접 보인다**:
//     2,000ea → 1식 **45,000**  /  3,000ea → 3,000 × 20 = 60,000  /  4,000ea → 80,000
//   즉 max(45,000, 수량 × 20) 이고 경계는 2,250ea 다(종전 50,000 이면 2,500ea).
//   주석이 이미 「45,000~50,000」 범위를 알고 있었고 50,000 을 「최근」으로 골랐던 것인데,
//   이 견적서가 그보다 최신이다. ⚠ 방향이 **싸지는 쪽**이라 더 최신 견적서가 오면 다시 봐라.
// 단면 20 근거: 26-08-04 소스코 140×43×130 4,000ea 견적서 (4,000 × 20 = 80,000)
//   종전 15 는 그 이전 견적서 기준. 접착·코팅이 함께 올랐다(IR 30,000→40,000).
export const GLUE_MIN_LOT = 45000;
//  labelJa — 「풀발이」는 **비운다.** 우리 현장 용어이고 대응하는 일본 실무어를
//  확인하지 못했다(のり貼り 로 적으면 단면·삼면과 구분이 사라진다).
export const GLUE_OPTS = [
  { id:"none",   label:"없음",         labelJa:"無し",       ea:0  },
  { id:"dan",    label:"단면",         labelJa:"片面貼り",   ea:20 },
  { id:"sam",    label:"삼면",         labelJa:"三面貼り",   ea:30 },
  { id:"pull",   label:"풀발이",                             ea:22 },
  { id:"handle", label:"손잡이형",     labelJa:"手提げ型",   ea:30 },
  { id:"sleeve", label:"슬리브",       labelJa:"スリーブ",   ea:80 },
  { id:"pp",     label:"PP접착",       labelJa:"PP貼り",     ea:70 },
];

// ── 톰슨 (원/R) ───────────────────────────────────────────────────
// 실측: 4×64 45,000~50,000 / 하4 45,000~50,000 / 하3·4×63 45,000
//       국2 50,000~55,000 / 4×62 40,000~60,000 / 46전지 80,000 / 4×6전지 70,000~75,000
//  labelJa — 「일반형」은 이 목록의 **기본 티어**라 標準型 으로 적는다(一般型 도 뜻은
//  같으나 일본 실무는 標準 을 쓴다). 「측면 풀발이 12단」은 위 GLUE_OPTS 의 풀발이와
//  같은 이유로 **비운다** — 한 용어를 한 곳에서만 번역하면 화면이 스스로와 어긋난다.
export const THOMSON_OPTS = [
  { id:"s",     label:"단순형",           labelJa:"単純型",   "4절":45000, "2절":50000, "전지":70000, min:45000 },
  { id:"n",     label:"일반형",           labelJa:"標準型",   "4절":50000, "2절":55000, "전지":75000, min:50000 },
  { id:"c",     label:"복잡형",           labelJa:"複雑型",   "4절":50000, "2절":60000, "전지":80000, min:50000 },
  { id:"sp",    label:"측면 풀발이 12단",                     "4절":80000, "2절":80000, "전지":80000, min:80000 },
  { id:"g_std", label:"G형 표준",         labelJa:"G型 標準", "4절":45000, "2절":55000, "전지":70000, min:45000 },
];

// ── 후가공 (원/R) ─────────────────────────────────────────────────
// 실측: 금박 140,000 (조립형) / 100,000 (트레이B, 양면 2회)
//       먹박 120,000 / 형압(디보싱) 100,000 / 형압 80,000 / 재단 30,000
export const FOIL_RPR_DEFAULT = 140000;
export const EMB_RPR_DEFAULT  = 100000;

/** 코팅·톰슨 R단가를 판형 티어로 선택 */
export const rprFor = (opt, tier) => opt ? (opt[tier] ?? opt["2절"] ?? 0) : 0;

export const coatById    = id => COAT_OPTS.find(o => o.id === id)    || COAT_OPTS[0];     // none
export const glueById    = id => GLUE_OPTS.find(o => o.id === id)    || GLUE_OPTS[0];     // none
export const thomsonById = id => THOMSON_OPTS.find(o => o.id === id) || THOMSON_OPTS[0];  // s 단순형

/** ⚠ 판형 랭킹 전용 공정비 추정. COAT_OPTS·THOMSON_OPTS 와 값이 다르다
 *  (matte 2절 65,000 vs 랭킹 62,000). **통합하지 마라** — 통합하면 판형 선택이 움직인다.
 *  의도인지 확인되기 전에는 두 벌을 유지한다. */
export const RANK_COAT_EST = { "4절": 55000, "2절": 62000, "전지": 112000 };
export const RANK_THOM_EST = { "4절": 50000, "2절": 55000, "전지":  75000 };
