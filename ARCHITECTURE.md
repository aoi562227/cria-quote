# ARCHITECTURE — cria-quote

단상자(종이박스) 견적 계산 React 앱. 이 문서는 **구조 안내서**다.
왜 이 상수가 이 값인지(실측 근거)는 `readme.md` 와 각 파일 주석이 소유한다.

- 규모(26-08-26 실측): `src/` **60파일**(jsx 19 · mjs 41) 12,528줄 · `test/` **17파일**(= 스위트 **16** + `die-profiles.mjs` 목형표 픽스처) 7,061줄 · 합 **19,589줄**
  큰 파일 셋 — `showroom/ShowroomPage.jsx` 1,555 · `domain/pdf-dieline.mjs` 1,330 · `ui/viz/nest-drag.mjs` 984
- 빌드: `npx vite build` → **350.76 kB** (gzip **126.89 kB**) · 88 모듈 · 693ms
- 검증: `npm run verify` (**16개** 스위트 전부 실코드 import) — 26-08-26 재실행 **EXIT=0**
  ⚠ `verify-pdf` 는 **오라클을 못 읽으면 exit 1** 이다 — §9 「SKIP 은 실패다」 참조

### ★★ 안전 방향 규약 — 기하를 만지기 전에 이것부터 읽어라

| 모델 도형이 | 결과 | 등급 |
|---|---|---|
| 실물보다 **작다** | 판에서 **겹친다** = **물리적으로 불가능한 판**을 앱이 만든다 | **최악** |
| 실물보다 **크다** | up 이 줄 뿐이다 | **안전** |
| up 이 **늘어난다** | 앱이 **싸게 부른다** | **영업 위험** |

그래서 이 저장소는 애매하면 **크게 그린다**(= 안전 봉투). 그 대가로 up 재현 점수가
내려가는 일이 실제로 있었고, 그건 실패가 아니다 — §7 「내려간 숫자를 어떻게 읽어야 하는가」.
같은 이유로 게이트는 **음수 잔차(부족)만** 실패로 센다.

---

## 1. 레이어

```
data ──► domain ──► ui
 (표)     (계산)     (표시)
```

의존은 **왼쪽으로만** 흐른다. 순환 0.
`domain` 은 React 를 모르고, `ui` 는 단가·공식을 모른다.
`data/` 5파일은 **import 가 0줄**이다 — 순수 상수·테이블 + 그 근거 주석뿐이다.

```
src/
├─ main.jsx                     ReactDOM 마운트 (3줄)
├─ App.jsx                      상태 한 벌 + 2단 레이아웃 + 해시 라우터. buildQuote 를 딱 1회 호출 (275줄)
├─ nest.mjs                     ★ NFP(No-Fit Polygon) 무겹침 **격자** 배치 엔진 (506줄)
│                                 순수 기하. 판형·단가·구조를 모른다.
├─ nest-free.mjs                ★ **자유배치** 네스터 (SVGnest 방식 재구현 — 코드 복사 없음)
│                                 nest 의 NFP·구간스윕을 재사용한다. 격자가 아니므로
│                                 자리마다 자세가 다를 수 있고 틈에 한 장만 더 넣는다.
│                                 ⚠ 기본 엔진이 아니다 — §11 참조
│
├─ domain/                      ── 계산. React 를 import 하지 않는다 ──
│  ├─ quote.mjs                 견적 1건 조립. 도메인의 단일 진입점
│  │                              normalizeQuoteInput → buildContext → collectLines → summarize
│  ├─ imposition.mjs            판걸이 어댑터. nest.mjs·nest-free.mjs 를 부르는 **유일한** 지점
│  │                              LAYOUT_ENGINE = grid(기본) | free — §11
│  ├─ sheet-select.mjs          판형(원지) 자동선택 — findBestSheet / pickFrom / chooseHadrong
│  ├─ reams.mjs                 지대R · 여분(손지) · 공정R
│  ├─ paper-repo.mjs            지종×판형 → 지대 단가 (룩업 우선, 없으면 면적환산 추정)
│  ├─ units.mjs                 올림 헬퍼 ceil1 / ceil3
│  ├─ pdf-dieline.mjs           ★ 협력사 칼선 PDF → 실측 전개도 (1,330줄, 의존 = sheets.mjs 뿐)
│  │                              컨테이너 파싱 + 콘텐트 스트림 + 성분 분해 + 후보 순위가
│  │                              **전부 이 한 파일**에 있다. 쪼개지 마라 — §9 참조
│  │
│  ├─ data/                     ── 실측 테이블. 근거 주석이 값 옆에 붙어 있다 ──
│  │  ├─ sheets.mjs             원지·절수·인쇄기 상한·물림·발자국 상한·판형 우선순위
│  │  ├─ papers.mjs             지종 DB
│  │  ├─ paper-prices.mjs       [지종][판형] → 원/R
│  │  ├─ print-prices.mjs       인쇄·소부·별색 단가, 판형 티어별 도당단가
│  │  └─ process-prices.mjs     코팅·접착·톰슨·후가공 단가 + 랭킹용 추정치
│  │
│  ├─ dieline/                  ── 박스 구조 레지스트리 (전개도 치수 + 폴리곤) ──
│  │  ├─ index.mjs              REGISTRY 배열 + dielinePieces / calcNetSize / structures
│  │  ├─ geometry.mjs           ★ 치수와 그림의 단일 출처. piecesFromFlaps / xEdges / GLUE_TAB
│  │  ├─ tuck-both.mjs          맞뚜껑 (상하 텍)      polygon
│  │  ├─ cross.mjs              십자조립 (크로스바텀)  polygon
│  │  ├─ glue3.mjs              삼면접착 (자동바닥)    polygon
│  │  ├─ gtype.mjs              G형 (톰슨조립)        폴리곤 미확정 → 직사각
│  │  ├─ gtype-tray.mjs         G형 트레이 (뚜껑일체)  폴리곤 미확정 → 직사각
│  │  └─ direct.mjs             전개도 전체크기 직접입력 (hidden)
│  │
│  └─ process/                  ── 견적서 한 줄 = 파일 한 개 ──
│     ├─ index.mjs              PROCESSES 배열(= 견적서 표기 순서) + collectLines/DevLines/Flags
│     ├─ paper soboo print coating foil emboss partial-uv thomson glue admin
│     ├─ dev-costs.mjs          개발비(목형·필름)
│     └─ overrides.mjs          rprOf / lotOf — 단가 직접입력 통로
│
├─ showroom/                    ── 전시회 부스에서 **고객에게** 보여주는 한 화면 ──
│  │                              실무앱과 목적이 다르다: 판걸이·개당단가·**도면 표기**만.
│  │                              ★ 사양은 실무앱에서 **URL 해시로 실려 온다** — §13
│  ├─ ShowroomPage.jsx          그리기 + 포인터만. 기하 0줄 (1,555줄)
│  ├─ showroom-core.mjs         「무엇을 그릴지」 판단 — partOf / auditItems(겹침 게이트) /
│  │                              autoPlace / capToQuoteUp / precisionKeyOf / 한·일 사전
│  │                              + ★ **SHOWROOM_SPEC_KEYS**(고객과 같이 넣는 14칸) ·
│  │                              qtyRowsOf(수량별 개당) · upForPrice / resetKindOf (868줄) — §12 · §17
│  └─ price-formula.mjs         「**얼마로 보여줄지**」 — 관리자 수식 파서(eval 없음) ·
│                                 0 차단 · 통화·반올림 · 저장 · 가림 비트 · shownRowsOf (618줄) — §16
│                                 ★ 표시 레이어다. 도메인 import **0줄** — 원가는 읽기만 한다
│
└─ ui/                          ── 표시. 값을 다시 계산하지 않는다 ──
   ├─ state.mjs                 UI 상태 ↔ QuoteInput 변환 (여기 한 곳에서만)
   │                            + **사양 링크 코덱**(SPEC_KEYS / encodeSpec / decodeSpec) — §13
   ├─ box-types.mjs             구조 레지스트리 → 드롭다운 (검증 ✓ 표기를 여기서 붙임)
   ├─ format.mjs                fmtR / fmtMM / today
   ├─ primitives.jsx            Input / Select / Toggle / Section / Field / Row2 / Row3
   ├─ QuoteSheet.jsx  QuoteRow.jsx        우패널 견적서
   ├─ panels/                   좌패널 7섹션
   │  BasicInfo BoxSpec PaperPanel PrintPanel CoatingPanel ProcessPanel DevCostPanel
   └─ viz/                      DielineShape LayoutViz NetDiagram SheetCompare QtyCompareTable
      SheetCanvas.jsx           판 + 물림 + 자(ruler)만. 얹는 도형은 DielineShape·PDF 폴리곤 (548줄)
                                ★ 3단계 이음새 3개(placement / data-cell / 좌표변환) — §10
      nest-drag.mjs             ★ 손배치 기하 (984줄) — 스냅 · 겹침(`overlapPairs`) ·
                                폴리라인 → 볼록 조각 분해(`makeDragPart` mode =
                                exact/stitched/raster/hull/bbox) · 직렬화.
                                `showroom-core` · `ShowroomPage` · `BoxSpec` · `SheetCanvas`
                                **넷이 같은 이 파일**을 쓴다 = 두 화면의 겹침 판정이 안 갈린다.
                                검증 `verify-drag-shape` 61/61 (§10·§12)
```

---

## 2. 데이터 흐름

```
UI 상태 s (`INITIAL_STATE` **71칸** · 이 중 **59칸**이 단가를 움직여 `SPEC_KEYS` 로 실린다 — §13)
   │  ui/state.mjs  toQuoteInput(s)
   ▼
QuoteInput { qty, box, paper, print, finish, overrides, dev }
   │  domain/quote.mjs  buildQuote(input)
   │
   ├─ normalizeQuoteInput ─── buildDieline ── dieline/index.mjs dielinePieces
   │    │                                       └─ geometry.piecesFromFlaps → pieces/cuts/folds/panels
   │    └─ lossOptionsOf (reams.mjs)
   │
   ├─ decideSheet ──┬─ overrides.up 있음   → 판형 고정, 배치 우회
   │                ├─ overrides.reams 있음 → 판형 고정, up 만 자동
   │                └─ 그 밖               → sheet-select.findBestSheet
   │                                          └─ `BASE_SHEETS` 11개마다 solveImposition
   │
   ├─ solveImposition ── printableArea(판형 ∩ 인쇄기 990×720)
   │                     └─ nest.solveLayout(pieces, printW, printH)
   │                        → up / cols / rows / cells / bbox / interlocked
   │                     └─ 발자국 92% 상한 → 초과하면 maxUp 낮춰 재시도
   │
   ├─ reams: 정미 = ceil(qty/up) → 여분 → 지대R · 공정R
   │
   ├─ collectLines(ctx)  PROCESSES 10개가 각자 lines(ctx) 를 낸다
   ├─ collectFlags(ctx)  공정이 경고 플래그를 낸다 (coating → irWarning)
   └─ summarize          공정합계 → 개발비 → 개당단가 = round(공정합계/수량) → 부가세
   ▼
QuoteResult { netSize, dieline, sheet, layout, reams, paperPrice, print, lines, devLines, totals, flags }
   │
   ▼  App.jsx 가 좌패널 7섹션 + 우패널 견적서에 나눠 준다
```

**호출 규칙 3개**

1. `nest.solveLayout` 과 `nest-free.solveFree` 는 `imposition.mjs` 만 부른다. 다른 곳에서
   직접 부르면 인쇄기 클램프·발자국 상한·행거탭 정책이 빠진 up 이 견적에 들어간다.
   엔진 선택은 `solveImposition({…, engine})` 한 인자이고 **기본은 격자**다 (§11).
2. `buildQuote` 는 App 에서 **1회만** 부른다. `useMemo` 로 감싼 그 한 번이 전부다.
3. `ui` 는 도메인이 준 값을 **다시 계산하지 않는다.** 수율(`layout.utilPct`)처럼
   분모가 갈릴 수 있는 값은 정본이 하나뿐이다.

---

## 3. 이런 걸 바꾸려면 어디를 보라

| 바꾸고 싶은 것 | 파일 (순서대로) | 검증 |
|---|---|---|
| **새 박스 구조 추가** | ① `src/domain/dieline/<구조>.mjs` 새로 만들고 `netSize()`·`flaps()` 작성 → ② `dieline/index.mjs` REGISTRY 배열에 1줄 | `verify-dieline` (불변식·계약) |
| 새 구조가 슬리브 위상이 아님 (트레이 등) | 위 + 그 파일에 `pieces(W,D,H,{net,flaps,opt})` 훅 선언. `geometry.mjs` 는 건드리지 않는다 | `verify-dieline` bbox 불변식 |
| **목형별 구조 옵션** (같은 W·D·H 인데 목형이 달라 전개도가 다르다) | ① 구조 파일 `flaps(W,D,H,opt)`·`netSize(W,D,H,opt)` 가 자기 노브를 읽는다 → ② `dieline/index.mjs` `DIE_OPTS` 에 위젯 1줄 (**number 면 `min`/`max`/`valid` 를 같이** — 허용범위의 정본은 그 노브를 읽는 **구조 파일**이 소유하고 여기서 import 한다) → ③ `ui/state.mjs` INITIAL_STATE·`toQuoteInput`(`box.dieOpt`)·**`SPEC_KEYS` 를 같이** → 화면·쇼룸은 자동. ⚠ §15 를 먼저 읽어라 — **범위 밖 값을 접는 곳은 `pickDieOpt` 한 곳**이고, 그래야 화면이 「넣은 값」이 아니라 「쓴 값」을 말한다 | `verify-speclink` §L · `scorecard2` 「목형별 노브 배선」 · `verify-imposition` 「추정 되먹임 금지」 |
| 구조의 톰슨 기본값 | 구조 파일에 `thomsonDefault: "g_std"` — `state.nextThomId` 가 레지스트리를 읽는다 | `verify-dieline` 계약 |
| 구조의 입력범위 경고 | 구조의 `netSize()` 가 `warning` 문자열 반환. UI 수정 불필요 | 화면 확인 |
| 구조 이름/검증 표기 | 구조 파일의 `label`·`tag`·`verified`. ✓ 표기는 `ui/box-types.mjs` 가 붙인다 | — |
| **새 공정 추가** | ① `src/domain/process/<공정>.mjs` (`applies`/`lines`, 필요시 `flags`) → ② `process/index.mjs` PROCESSES 배열에 1줄 | `verify-total` |
| 공정이 새 입력을 읽어야 함 | `state.mjs` 에 UI 키 추가 → `toQuoteInput` 의 `finish` 에 넣으면 `ctx.finish` 로 그대로 흐른다 (화이트리스트 없음) | `verify-total` |
| **지대 단가** | `domain/data/paper-prices.mjs` — 값 뒤 주석에 근거 견적서 날짜 | `verify-total` |
| 코팅·톰슨·접착·후가공 단가 | `domain/data/process-prices.mjs` (컬럼 = `"4절"`/`"2절"`/`"전지"`/`min`) | `verify-total` |
| 인쇄·소부·별색 단가 | `domain/data/print-prices.mjs` | `verify-print` · `verify-total` |
| 지종 목록 | `domain/data/papers.mjs` | — |
| **고객에게 보여줄 금액**(마진·환율·통화·반올림) | `showroom/price-formula.mjs` **한 곳** — 문법·거부·0 차단·밴드·반올림·저장·「가림 비트」. ⚠ 도메인·`buildQuote` 는 **건드리지 않는다**(표시 레이어다). 통화를 늘리면 `BAND`·`ROUND_NOTE`·`CUR_CHOICES` 에 **같이** 한 줄씩 | `verify-speclink` §M (§16) |
| **원지 규격·절수** | `domain/data/sheets.mjs` BASE_SHEETS | `verify-r` (1R 장수) |
| 판형 실무 우선순위 | `sheets.mjs` SHEET_PRIORITY | `verify-print` §B |
| 하드롱 채택 문턱 | `sheet-select.mjs` HADRONG_EDGE | `verify-print` §C |
| 동점 판정 폭 | `sheet-select.mjs` TIE_PCT | `verify-print` §B |
| 추정단가 페널티 | `sheet-select.mjs` EST_PRICE_PENALTY | `verify-print` §D |
| **물림(gripper) 방침** | `imposition.mjs` `printableArea` ⚠ 아래 §5 를 먼저 읽어라 | `scorecard-unit` §A/§C · `verify-net` |
| 발자국 상한 | `sheets.mjs` MAX_FOOT_PCT + `imposition.mjs` `footOf` 분모 | `verify-net` 발자국% 열 |
| 인쇄기 최대 판 | `sheets.mjs` PRESS_MAX_LONG/SHORT | `verify-layout` §E |
| 배치 알고리즘 (격자) | `src/nest.mjs` ⚠ 순수 기하. 정책을 넣지 마라 | `verify-nest` 44케이스 |
| 배치 알고리즘 (자유) | `src/nest-free.mjs` ⚠ 같은 규칙. 정책은 `imposition` §11 | `verify-nest`(공유 기하) · §11 표 |
| **엔진 선택 · 자유배치 UI** | `imposition.mjs` `LAYOUT_ENGINE` → `ui/panels/BoxSpec.jsx` 「배치 엔진」 | §11 |
| **여분(손지) 규칙** | `domain/reams.mjs` LOSS_* + `estimateLoss` | `verify-r` 여분 7케이스 |
| 지대R·공정R 올림 규칙 | `domain/reams.mjs` `calcR`/`calcProcessR` + `units.mjs` | `verify-r` |
| 개당단가 반올림 | `quote.mjs` `summarize` (`round`, floor 아님) | `verify-total` |
| 견적서 줄 순서 | `process/index.mjs` PROCESSES 배열 순서 — **계약이다** | `verify-total` |
| 견적서 화면 | `ui/QuoteSheet.jsx` · `ui/QuoteRow.jsx` | 화면 확인 |
| 배치 그림 | `ui/viz/LayoutViz.jsx` (좌표는 `layout.boxes` 만 씀) | 화면 확인 |
| 전개도 그림 | `ui/viz/DielineShape.jsx` + `NetDiagram.jsx` (패널은 `dieline.panels`) | 화면 확인 |
| **도면 표기(칼선·오시선·접착면)** | `showroom/showroom-core.mjs` `partOf` → 선 3종을 가른다. 색·굵기·dash 는 `ShowroomPage.jsx` 상수. ⚠ §12 를 먼저 읽어라 | 화면 확인 (§12 자기검증) |
| 쇼룸 문구·번역(한/일) | `showroom/showroom-core.mjs` `KO`/`JA` — JSX 에 문자열을 흩뿌리지 마라 | 화면 확인 |
| **두 화면 사양 연동** | `ui/state.mjs` 「사양 링크」 절 (`SPEC_KEYS`·`encodeSpec`·`decodeSpec`·`saveNames`/`loadNames`) + `App.jsx` 라우터. ⚠ §13 을 먼저 읽어라 | `verify-speclink` **348/348** |
| **쇼룸에서 고객과 같이 넣는 사양 칸** (지종·도수·코팅·후가공·접착·톰슨) | `showroom/showroom-core.mjs` `SHOWROOM_SPEC_KEYS` **한 곳** — 계산(`quoteInputOf`)과 링크(`linkStateOf`)가 같은 목록을 읽는다. 칸을 늘리기 전에 그 주석의 「뺀 것과 근거」를 읽어라 (단가 직접입력·개발비는 **원가 구조 그 자체**다) | `verify-speclink` §N (§17) |
| **수량별 개당단가(부스 대화의 본체)** | `showroom-core.qtyRowsOf` — 계산은 `buildQuoteRange`(견적 앱 비교표와 **같은 함수** · 복제 0줄). 표시는 `price-formula.shownRowsOf` ⚠ **수식 미설정이면 표를 안 그린다**(원가 노출면 유지 — §17) | `verify-speclink` §N ⑥⑦⑪ · §M ⑯ (c-2) |
| **쇼룸 드롭다운의 일본어** | 도메인 항목에 `labelJa` 한 칸 (`domain/data/papers.mjs` · `process-prices.mjs`) → `showroom-core.pairs(list, lang)` 가 고른다. **없으면 한국어로 떨어진다** — 근거 없는 제품명을 지어내지 마라(§17) | `verify-speclink` §N ①-2 |
| **판형이 바뀌었을 때 배치를 어떻게 하나** | `showroom-core.resetKindOf`(도형이 바뀐 것 vs 판만 바뀐 것) + `ShowroomPage` 의 `reseat` 효과. ⚠ 「자동 재계산 금지」의 **셋째 예외**다 — 늘리기 전에 그 주석을 읽어라 | `verify-speclink` §N ⑨ |
| **과도기 프레임에 옛 배치의 up 이 새는 것** | `showroom-core.upForPrice` — 배치와 「그 배치가 놓인 판」이 짝이 아니면 up 0 | `verify-speclink` §N ⑩ |
| 단가를 움직이는 키가 하나 늘었다 | `ui/state.mjs` `toQuoteInput` **과 `SPEC_KEYS` 를 같이** 고쳐라 — 하나만 고치면 두 화면 금액이 갈린다 | `verify-speclink` §A (Proxy 실측) |
| **칼선 PDF 추출** | `domain/pdf-dieline.mjs` — 후보 점수·성분 병합 규칙까지 한 파일. §9 를 먼저 읽어라 | `verify-pdf` 72/74 (§E 회귀 게이트 8/8 · §F /ObjStm 5/5) |
| PDF 드롭 UI·후보 드롭다운 | `ui/panels/BoxSpec.jsx` (드롭 영역 → `sizeMode="net"` + `nW`/`nH`) | 화면 확인 |
| 「고른 후보 → 그 polygons」 규칙 | `ui/state.mjs` `pdfPickOf(s)` — **여기 한 곳**. BoxSpec 과 3단계가 같이 부른다 | §10 계약표 |
| 판형 캔버스(판·물림·자) | `ui/viz/SheetCanvas.jsx` ⚠ 배치를 다시 풀지 마라 — `placement` 없으면 `layout.boxes` 만 읽는다 | 화면 확인 |
| **드래그 배치(3단계) 붙이기** | §10 「붙이는 순서」 6단계 — 도메인 수정 0줄이 목표다 | 화면 확인 |
| 입력 위젯 스타일 | `ui/primitives.jsx` | — |

---

## 4. 실측 근거는 어디 있나

이 프로젝트의 **모든 상수에는 실측 근거가 있다.** 근거를 잃으면 후임자가 임의로 바꾼다.
근거는 값 바로 위 주석에 있다 — 아래는 그 색인이다.

### 전개도 (칼선 벡터 실측)

| 상수·공식 | 값 | 근거 위치 |
|---|---|---|
| 접착날개 `GLUE_TAB` | 14.3 | `dieline/geometry.mjs` — netW 가산분. **탭 폭이 아니다**: 실제 탭은 15.0 이고 14.3 = 15.0 − 탭이 붙는 D 패널의 부족분 0.7 |
| 십자 전용 접착탭 `CROSS_TAB` (= **기본값**. 목형별 노브 `opt.glueTabW` 로 덮는다 — §15) | **23.33** (탭 실폭 24.03) | `dieline/cross.mjs:185` — 십자만 탭이 산포한다(실측 24/17/15/14/12). 공용 14.3 으로 되돌리면 니치어 netW 가 **−9.03mm** 부족 → 자동선택 판형에서 실물 겹침 1,269mm². 대가는 십자B 4→3up (§7·§14) — ★ 26-08-25 그 대가 중 **하나만** 목형별 노브로 갚아졌다: 다빈기획은 **실측** 14.70 이라 하4 netW 594.0 ≤ 597 로 「견적 미산출」 회귀가 소멸한다. 십자B 4up 은 **철회했다** — 그 값(15.70)이 추정이었고, 같은 W·D 의 최근접 **실측**(니치어 70×70)이 24.03 을 가리킨다(§15) |
| 접착탭 부족분 `GLUE_TAB_SHORT` | 0.7 | `geometry.mjs` — 세로 크리스 직접 실측 3건(웨이크A·웨이크B·도솔) 6/6 경계 잔차 0.00mm |
| 패널 x 경계 `xEdges` | `[0, D−0.7, D−0.7+W, 2D−0.7+W, 2D−0.7+2W, netW]` = 폭 `[D−0.7, W, D, W, 15.0]` | `geometry.mjs` · 검산 `test/verify-net.mjs` 「패널 x 경계」 4건. ⚠ 종전 `[D,W,D,W,14.3]` 은 내부 접는선 4개가 전부 +0.70mm 어긋나 있었고, 유일한 픽스처(바이오머 D=15=탭폭)가 두 프레임을 **구분하지 못해** 「오차 0.0mm」로 보였다 |
| 날개 끝단폭 `TIP_W` | 10.8 | `geometry.mjs` — 웨이크버니 y레벨 절단 실측, A(46mm)·B(36mm) 패널 모두 10.8 → **상수** |
| 날개 인셋 `TAPER_MAX` | 10.2 | `geometry.mjs` — 깊은 날개 끝 폭 A 46.0→25.6 / B 36.0→15.6, 한쪽 인셋 환산 둘 다 10.2 |
| 맞뚜껑 `netH` | **`H + 2(D+18.0)`** | `dieline/tuck-both.mjs:146` — 혀는 상수가 아니다: 목형 5벌 산포 11.0/15.0/16.5/16.5/17.0/18.0. **상한 18.0 을 채택**한 이유는 음수(=부족=겹침) 잔차가 0 이 되기 때문이다. 종전 16.5 는 더파이러츠에서 실물 겹침 510mm² 를 만들고 있었다. 대가는 바이오머 netH +6mm 과대(=안전측) |
| 맞뚜껑 날개 배치 | `top = bot = [dust, 뚜껑, dust, 0]` (**스트레이트텍**) | `dieline/tuck-both.mjs` — 실측 2/2 잔차 −0.01mm(바이오머·더파이러츠). 종전 역텍 `bot=[dust,0,dust,뚜껑]` 은 더파이러츠에서 잔차 **−86.01mm** |
| 십자 `netH` | **`H + (1.086D+11.3) + (0.70D+0.6)`** | `dieline/cross.mjs:170,173` — 위/아래 분리 실측 5벌의 **안전 봉투**(회귀선이 아니라 상한). 종전 `+10.2`/`+0`(회귀선)은 다빈기획 −0.40 · 칼선-09 −0.60 으로 **음수 잔차**를 남겼고 돌연변이표 2행이 그 되돌림을 상시 감시한다. ⚠ 그 전 `2(7D/8+5.5)` 의 **반반 분할은 실측이 아니라 가정**이었다(5/5 비대칭, 위가 아래의 1.55~2.28배) |
| 십자 아래날개 깊이 | `[b, b, b, b]` | `dieline/cross.mjs` — [추정]. 실측 D/W 비 0.65 / 0.97 / 1.00, 종전 0.55 는 3건 전부 과소(=도형이 작다=up 과대) |
| 삼면접착 위 띠 `s` | `0.67D + 0.03W + 2.8` | `dieline/glue3.mjs` · 도출 `test/verify-imposition.mjs` 「소스코 …」 절. 4번째 표본(도솔 50×50×150)에서도 잔차 −0.21mm 로 생존 — MAE 0.140mm |
| 삼면접착 아래 띠 `b` | `D + 15` | 같은 절 — 4건 잔차 ±0.02mm. `W` 계수 −0.001 ≈ 0 |
| 삼면 **위**날개 깊이 | **`[s, s, s, s]`** ← 안전 봉투 | `dieline/glue3.mjs:282` — 이 자리는 두 번 뒤집혔다. ① `[s,s,s,s]`(근거 없음) → ② `[D/2,s,D/2,s]`(D 패널 자유계수 0 · 4건 MAE 0.013mm) → ③ **다시 `[s,s,s,s]`**. ③ 의 이유는 ②가 칼선-14 의 D 패널에서 **−12.25mm 부족**(=겹침 방향)을 냈기 때문이다. 「MAE 가 작다」와 「음수 잔차가 없다」는 다른 기준이고, 이 저장소는 **후자**를 택한다. 돌연변이표 `삼면 위 띠 [s,s,s,s] → [D/2,s,D/2,s]` 행이 되돌림을 감시한다 |
| 삼면 **아래**날개 깊이 (= **기본값**. 목형별 노브 `opt.deepFlap` far/near 로 덮는다 — §15) | **`[min(W,b)/2, b, min(W,b)/2, b]`** ← 안전 봉투(양쪽 W 다 깊게) | `dieline/glue3.mjs:250,283` — ★ **깊은 날개 위치는 실측이 두 계열로 갈린다. 근거는 3/5 다** — 상세·폐기안·대가는 §14. 두 부등식(`idx1 ≥ b` 도솔 65.0 · `idx3 ≥ b` 칼선-14 60.0)이 **동시에** 성립해야 하므로 `[.., b, .., b]` 가 수학적 **최소** 봉투다. 대가 = 삼면접착에서 **맞물림 사실상 상실** · LUXEN 2→1up · 웨이크버니B 6→4up. ★ 26-08-25 2·3차 — LUXEN 을 회복시키던 `deepFlap` 값은 **추정**이었고 철회했다(§15 「미상 기본값과 그 위험」). **웨이크버니B 는 다르다** — 그 `far` 는 **실측**이고 지금도 실려 있으며, 앱 실경로에서 기본 4up → 목형 지정 **6up = 견적서 재현**이다(3차 실측 · `verify-net` 「표 밖 실측」). 어느 채점표에도 그 건이 없어서 **점수에는 안 잡힌다.** 「실측 far 3벌은 up 을 안 움직인다」고 적었던 종전 문장은 **거짓**이다 — 웨이크A·도솔은 안 움직이고 웨이크B 는 움직인다. 얕은 쪽 깊이는 노브가 아니라 `min(W,b)/2` 로 못박았다 — 0 도 0.70D 도 안 된다. ⚠ 그 전 `[0.10b,0.40b,b,0.40b]`(깊은 날개를 D 패널에) 는 4/4 표본 전부와 어긋났다(잔차 −30.6~−39.0mm) |
| 프레임 좌우 반전 | **하지 않는다** | `dieline/glue3.mjs` 「좌우 반전을 안 하는 이유」 — 반전하면 패널 타입 순서가 현행과 정확히 일치(=같은 목형의 이면 뷰). 거울 양쪽을 다 평가해 max 를 취해도 8/12 로 **현행과 동일**하다(측정 확인) |
| G형 트레이 | `W+4H+44.5` / `2D+3H+19.5` | `dieline/gtype-tray.mjs` — **bbox 4건 회귀(폴리곤 좌표는 소실)**. 원본 PDF 4건이 이 PC 에 없어 재검증 불가 |
| G형 D/H 분기 | 0.8 | `dieline/gtype.mjs:1-17` — 실측 6건 |
| 왜 치수와 그림이 한 파일인가 | — | `geometry.mjs:5-32` — 종전 두 회귀식이 갈려 폴리곤이 25~30% 작았고, 그래서 십자B 가 4up 대신 6up 이 나왔다 |

### 판형·배치 (견적서 78건 역산)

| 상수 | 값 | 근거 위치 |
|---|---|---|
| 원지 3계열·절수 | 788×1091 / 636×939 / 889×1194 | `data/sheets.mjs:1-31` |
| 인쇄기 최대 판 | 990 × 720 | `sheets.mjs:47-56` — 견적서 99장 「단위」 전건이 이 안에 들어감 |
| 물림 `BITE_SHORT/LONG` | 30 / 20 (**적용 안 함**) | `sheets.mjs:58-70` — 코리팩 산출식 엑셀 B14 vs B15. 적용하지 않는 이유는 §5 |
| 배치 허용오차 `FIT_TOL` | 0.005 | `sheets.mjs:73-77` — 삼면E 실측 265.0 vs 공식 268.0 |
| 발자국 상한 `MAX_FOOT_PCT` | 92 | `sheets.mjs:79-88` — 분모는 **판형 면적**이어야 한다 |
| 판형 우선순위 | 4×64→국2→4×62→… | `sheets.mjs:33-45` — 실무 확인 |
| 하드롱 문턱 `HADRONG_EDGE` | 0.06 | `sheet-select.mjs:13-26` — 조립형 324×428 실측. 배수 페널티로 하면 어긋나는 이유까지 적혀 있다 |
| 동점 폭 `TIE_PCT` | 0.015 | `sheet-select.mjs:28-43` — 삼면 50×40×81 에서 도수만 바꿔 판형이 뒤집힌 실측 |
| 추정단가 페널티 | 0.03 | `sheet-select.mjs:45-52` |
| 맞물림 상수 감산이 왜 틀렸나 | — | `test/verify-nest.mjs` §5 — IL_W=10 이 반전 이웃과 464mm² 겹침(독립 샘플링). 실제 허용 최대 행겹침 26.0mm vs 종전 145.0mm |

### 금액 (견적서 78건 역산)

| 규칙 | 값 | 근거 위치 |
|---|---|---|
| 지대R | `(정미+여분)/(500×절수)`, 전지급 0.1 올림 / 2절이하 3자리 올림 | `domain/reams.mjs:1-39` |
| 여분 기본 | 300장 (무인쇄 200) | `reams.mjs:14-32` — 견적서 99건 중 정미 6,000장 이하 전건이 정확히 300 |
| 여분 **수동 0 = 자동**(300장) | `manual > 0` | `reams.mjs` — ⚠ **알려진 동작이고 일부러 그대로 뒀다.** 0 을 「여분 0장」으로 받으면 방향이 **싸지는 쪽**으로 바뀐다(인쇄 여분 0 은 실무에서 성립하지 않는다). 1 은 먹힌다. 입력칸 note(`ui/panels/PaperPanel.jsx`)가 화면에서 그 사실을 말한다 |
| 여분 가산 | 양면 +100 / 베다 +100 / 형압 +50 / **박 0** | `reams.mjs:45-50` — 십자B 한 장 안에서 직접 대조. 박 0 은 조립형 금박 2건 여분 300 |
| 공정R | `ceil(정미/1000×10)/10`, 정미<1000 → 1식 | `reams.mjs:34-38` |
| 별색 가중치 `SPOT_WEIGHT` | 3 | `data/print-prices.mjs:21-33` — 삼면E 별2 84,000 vs 원4 56,000 대조 견적서 |
| 단가 인상 이력 | 소부 10,000→11,000→12,000 등 | `print-prices.mjs:1-12` |
| 접착 단면 / IR 2절 | 20원/EA / 40,000 | `data/process-prices.mjs:22-31` · `:14` — 26-08-04 소스코 견적서 |
| 접착 최소 1식 | 50,000 | `process-prices.mjs:27` |
| 랭킹용 추정치를 왜 안 합치나 | — | `process-prices.mjs:62-64` — 합치면 판형 선택이 움직인다 |
| 개당단가 = `round` | — | `domain/quote.mjs:1-12` — 맞뚜껑A 30,000ea 77.84→78 (floor면 77 ✗) |
| 전체 금액 재현 | 24/26 | `test/verify-total.mjs` — 나머지 2건은 견적서 자체 손수정(파일 안에 `knownDiff` 로 명시) |

### 폐기된 가설 (지우지 마라 — "왜 이 공식이 아닌가" 를 잃는다)

- `test/verify-net.mjs:24-28` `OLD` — 폐기된 netH 회귀식 3종. 원본이 코드에 없다.
- `test/verify-print.mjs` §B 주석 — 우선순위를 총비용에 곱했던 종전 규칙이 왜 틀렸나.
- `test/verify-imposition.mjs` 「날개 테이퍼 — 해결됨」 절 — 전폭 사각형 모델의 실패 이력.
- `test/verify-interlock.mjs` §A~§C — 코리팩 산출식 엑셀 원문. 앱 로직의 미러가 아니라 엑셀 전사다(§A 가 엑셀 자체 예시 8/8 로 검산).

---

## 5. ⚠ 물림(gripper) — 되돌리기 전에 읽어라

`imposition.printableArea` 는 판형에서 물림을 **빼지 않는다.**

물림 자체는 실재한다(코리팩 엑셀 「종이 규격」 vs 「제작 규격」 = 짧은변 −30 / 긴변 −20,
근거는 `sheets.mjs:58-70` 에 보존). 그런데 **그 차감은 이미 판형 숫자 안에 있다.**
`BASE_SHEETS` 의 절지 규격과 견적서 「단위」열은 원지가 아니라 인쇄기에 걸리는 재단
크기이고, 견적서의 up 은 그 크기 위에서 실제로 나온 값이다. 또 빼면 이중 차감이다.

반증 불가능한 사례 — 삼면E 90×70×130 을 545×394(4×64)에 2up:
회전 2열이 545 중 536mm 를 쓴다. 남는 여백이 전체 9mm 인데, 긴변에서 20mm 를 더 빼면
그 대지가 물리적으로 성립하지 않는다. 그런데 견적서에 실존한다.

| 측정 | 물림 0 (현행) | 물림 20/30 |
|---|---|---|
| `scorecard-unit` (판 = 견적서 「단위」) | **6/12** ‡ | **1/12** ‡ |
| `verify-net` (앱 실경로) | **6/10** ‡ | 1/10 † |
| `scorecard-nest` 지대금액 평균절대오차 | **17.1%** ‡ | 48.9% † |

‡ = 26-08-25 재실행값. † = 물림을 실제로 켜야 나오는 값이라 **이번 패스에서 재실행하지
않았다** — 26-08-07 측정값 그대로다. 현행 열이 7→6 · 14.4→17.1% 로 내려간 것은 물림과
무관하고 원인은 안전 봉투·`CROSS_TAB` 이다(§7·§14). **A/B 결론은 그대로다** — 격차가
6 vs 1 로 여전히 크고, `scorecard-unit` §A(6/12) 와 §C(1/12) 는 이번 실행에서 직접 확인했다.

되돌리려면 `BASE_SHEETS` 를 원지 규격으로 재정의하고 견적서 78건을 다시 역산하는 것이
먼저다. `scorecard-unit.mjs` 가 §A(물림 0) vs §C(물림 적용)를 계속 계량하며,
§C 가 §A 를 앞지르면 그 파일이 exit 1 로 실패한다.

### 2026-08-07 재조사 — 「표준=원지 / 사용자입력=재단」 분기는 반증됐다

물림을 판형 출처에 따라 갈라 보려는 시도가 한 번 더 있었다. 규칙은
「표준 판형(`BASE_SHEETS`)은 원지니까 차감 / 사용자가 직접 준 크기(주문생산 `custom`,
PDF 로 읽은 판)는 이미 재단됐으니 미차감」. **구현해서 6개 스위트를 돌렸고 폐기했다.**

결정적 근거는 숫자의 동일성이다 — 견적서 「단위」 12건을 `BASE_SHEETS` 와 대조하면
**8건이 완전 동일**하다(국2 636×469 · 4×62 788×545 · 하4 444×597 · 4×64 394×545).
「단위」가 재단 크기라는 것은 확정 사실이므로 그와 같은 숫자인 절지 규격도 재단 크기다.
표준이라는 이유로 차감하면 정의상 이중 차감이고, 실제로 **깨진 4건이 전부 그 8건에
속했다**(맞뚜껑A 국2 6→4 · 삼면A 하4 2→1 · 삼면C 4×64 4→2 · 십자B 하4 4→3).
나머지 4건(760×480 · 980×720 · 480×788)은 전지·2절급 주문재단이라 표준 규격이 아니다.

⚠ **이 표는 26-08-07 당시의 측정값이다. 그대로 둔다** — 세 열의 **차이**가 논거이고,
그 차이를 만든 실험은 이 패스에서 재실행하지 않았다. 첫 열의 절대값은 그 뒤 안전 봉투·
`CROSS_TAB` 로 내려갔다(현행: `verify-net` 6/10 · `scorecard-nest` 6/10 · 17.1% ·
`scorecard2` 8/15 · `scorecard-unit` 6/12). **결론은 안 바뀐다.**

| 측정 (26-08-07 당시) | 미차감(유지) | 표준 전부 차감 | 전지급(`cut=1`)만 차감 |
|---|---|---|---|
| `verify-net` 판걸이 up | **7/10** | 3/10 · exit 1 | 7/10 |
| `verify-net` 비폴리곤 구조 up | **6/6** | 5/6 | 6/6 |
| `scorecard-nest` up | **7/10** | 3/10 | 7/10 |
| `scorecard-nest` 지대금액 평균절대오차 | **14.4%** | 36.3% | 14.4% |
| `scorecard2` · `scorecard-unit` | 10/15 · 7/12 | 변화 없음 | 변화 없음 |
| `verify-total` · `verify-r` | 24/26 · 37/39 | 변화 없음 | 변화 없음 |

- 「표준 전부 차감」열은 물림에 **가장 유리한** 구현이다(`printW/printH` 만 줄이고 발자국
  분모는 보존). 분모까지 줄이면 위 첫 표의 1/10 · 48.9% 가 된다 — 둘 다 실행 확인했다.
- 「전지급만 차감」은 **어느 점수도 움직이지 않는다.** 바뀌는 것은 삼면B 46전지의 배치
  모양뿐이다(칼선공유 → 맞물림 ↕18mm, up 은 2 그대로). 득도 실도 없는 분기는 넣지 않았다.
- ⚠ `verify-total`·`verify-r` 이 불변인 것은 안전 신호가 **아니다.** 그 두 스위트는
  `overrides.up` 으로 판걸이를 고정하므로 물림 회귀를 원리적으로 못 잡는다
  (`verify-total.mjs:7` 분업 주석). 물림 방침의 감시자는 `verify-net`·`scorecard-nest` 다.

부산물로 **판걸이 캐시 키에 `custom` 을 넣었다** (`imposition.mjs` `ck`). 현행 방침에서는
표준/주문생산의 배치가 같아 무해하지만, `custom` 으로 갈리는 정책이 하나라도 들어오는
순간 같은 `w×h` 를 직접 입력한 주문생산이 표준 판형의 배치를 캐시에서 돌려받는다 —
화면에 아무 표시 없이 up 만 틀리는 사고다. 지우지 마라.

---

## 6. 불변식과 그걸 지키는 테스트

| 불변식 | 테스트 |
|---|---|
| 폴리곤 bbox === `netW × netH` (오차 0) | `verify-dieline` 불변식 360/360 · `scorecard2` |
| `max(top) === topLid` · `max(bot) === botFloor` | `verify-dieline` |
| 배치된 조각이 **서로 겹치지 않음** (NFP 완전검증 + 독립 샘플링) | `verify-nest` 44/44 |
| 행거탭이 열 피치를 늘리지 않음 (0/15/20mm 전 구간 같은 up) | `verify-net` 「행거탭」 |
| 회전금지 구조가 실제로 회전하지 않음 | `verify-net` 「회전금지 실효」 |
| 1R 장수 = 500 × 절수 | `verify-r` |
| 판형 선택 정책 3개(우선순위·하드롱·추정단가)를 뒤집으면 실패 | `verify-print` §B·§C·§D — 돌연변이 3종 전부 검출 확인 |
| 물림 방침이 뒤집히면 실패 | `scorecard-unit` §A(물림0) vs §C(물림적용) — §C 가 §A 를 앞지르면 exit 1 |
| 견적서 전체 금액 원 단위 일치 | `verify-total` 24/26 |
| **모델 폴리곤 ≥ 실물 칼선** (겹침·잔차·커버리지 3종) | `verify-imposition` — 진실은 **원본 PDF**다. §14 「게이트의 진실」 |
| **목형 옵션이 실린 도형도 실물을 덮는다** (세 게이트 **두 열**: 목형 지정/기본값) | `verify-imposition` — 26-08-25 신설. 「목형 지정」열이 절대선(실측 목형은 자기 옵션으로 겹침 0), 「기본값」열은 목형을 **모르는** 사용자가 받는 도형 |
| **상수를 작게 만들면 빨간불이 켜진다** (16축) | `verify-imposition` 돌연변이 16/16 — 그중 2축은 **노브의 기본값**을 겨눈다 |
| **두 화면(견적서·쇼룸)이 같은 개당단가를 말한다** | `verify-speclink` 348/348 — 대표 게이트 = **204원** + **공정합계 814,969원**(반올림이 가리는 −970원 구간) |
| **고객 화면에 0 원/0 엔이 뜨지 않는다** | `verify-speclink` §M ⑫ — 0 부류 **42건 × 통화 2벌**. 게이트는 **반올림 뒤** 한 곳(`!(n > 0)`) — 0 · IEEE **-0** · NaN 을 한 자리에서 막는다. 정확값으로 재면 `*0.001` 과 「원가 1원의 `*0.4`」를 놓친다 (§16) |
| **고객 화면·DOM·링크 어디에도 원가가 없다**(수식이 켜졌을 때) | `verify-speclink` §M ④⑪⑯ — 모델 · 소스 · ★ **렌더 결과**(`react-dom/server`) 3층. ⑯ 은 실증된 2줄 주입에서 **빨개지는 것까지** 매번 확인한다. 링크는 사양을 싣되 「가림 비트」로 접는다 (§16) |
| **부스에서 친 문자열이 코드가 되지 않는다** | `verify-speclink` §M ⑧ — `eval`·`Function`·`.constructor`·동적 `import` 라는 **이름**이 `src/` 에 0건. ⚠ 조립한 이름은 못 잡는다(그 한계도 단언으로 적혀 있다) |
| **왕복 2·3회차에도 판형·up·개당단가가 안 움직인다** | `verify-speclink` §K — 판형 11개 전부 |
| **목형별 노브가 값마다 폴리곤을 실제로 바꾼다** (죽은 손잡이 금지) | `scorecard2` 「목형별 노브 배선」 13/13 — ⚠ **면적이 아니라 폴리곤 서명**으로 본다(far/near 는 거울상이라 면적이 같다) |
| **목형 옵션이 두 화면에 같이 흘러가고 왕복에서 산다** | `verify-speclink` §L — 미지정이 조용히 켜지지도 않는다 |
| **점수를 올린 노브가 견적서 역산이 아니다** | `verify-imposition` 「추정 되먹임 금지」 — 점수에 실린 추정 노브 **0건**. 추정을 넣으면 그 값을 같은 구조의 실측 12벌에 먹여 겹침 0 을 증명해야 한다 (§15) |
| **범위 밖 노브 값이 화면 문구에 그대로 안 나온다** | `verify-speclink` §L — `pickDieOpt` 가 유효값만 `dieline.dieOpt` 에 넣는다. 종전에는 999 를 넣으면 화면이 「✓ 목형 지정 — 접착탭 실폭 999mm」라고 진술했고, 상한 초과(61)는 모델을 실물보다 **작게** 만들어 최대 −36.6% 싸게 청구했다 |
| **음수 수량으로 견적이 나오지 않는다** | `domain/quote.normQty` — qty=−100 이 4up · 개당 **0원**을 내던 구멍. 0/NaN 은 접히는데 음수만 빠져나갔다 |

**테스트는 전부 실코드를 import 한다** (**16/16 파일** — 26-08-25 확인, 파일당 `from "../src…"` 1~7건).
자체 계산 함수가 남아 있는 곳은 앱 로직의 미러가 아니라 (a) 엑셀 원문 전사
(`verify-interlock` §A~§C), (b) 후보값 스캔 도구(`verify-layout` `upOn`),
(c) 폐기된 구현의 문서형 재현(`verify-net` `OLD`)이고 각 파일이 그렇게 명시한다.

---

## 7. 점수판 (2026-08-26 재실행 — `npm run verify` **16개 스위트 · EXIT=0**)

아래는 전부 **그날 실제로 돌린 출력**에서 옮긴 값이다. 돌리지 않은 줄은 여기 없다.
스위트 순서는 `package.json` 의 `verify` 체인 순서와 같다.

| 스위트 | 점수 | 무엇을 재나 / 내려갔다면 왜 |
|---|---|---|
| `verify-dieline` | 불변식 360/360 · 계약 36/36 | 전개도 치수 ↔ 폴리곤 정합 |
| `verify-nest` | 44 통과 / 0 실패 | 배치 기하 — 무겹침·NFP 성질 |
| `verify-r` | 지대R 37/39 · 공정R 39/39 · 여분 7/7 | R수·여분 |
| `verify-print` | 인쇄비 19/20 · 원색+별색 병행 7/7 · 하드롱 3/3 · 추정단가 2/2 · 베다 6/6 | 인쇄비 + 판형 선택 정책 |
| `verify-total` | 24 / 26 | 견적서 전체 금액 (나머지 2건은 견적서 자체 손수정 — `knownDiff`) |
| `verify-net` | ★ **판걸이 up 6/10**(목형 미지정) · ★ **목형 지정 6/10** · ★ **실측 전용 5/8** (미실측 2건 제외 — 그중 1건 불일치) · 비폴리곤 6/6 · 패널 x 경계 4/4 · 행거탭 불변 ✓ · ★ **표 밖 실측 3건**(점수 아님) | ★ **앱 실경로 판걸이 대표 점수.** 7→6 의 원인은 **`CROSS_TAB` 14.3→23.33 한 건**(십자B 4→3up)이다. 되돌리면 7/10 로 오르지만 니치어가 자동선택 판형에서 **실물 겹침 1,269mm²** 로 되살아난다. ★ 26-08-25 **2차** — 목형 지정 열이 7→**6** 으로 내려갔다. 그 7 을 만들던 두 행(십자A·십자B)이 **추정** 노브였고, 그 추정 규칙을 실측 목형에 되먹이면 니치어에서 1,178.96mm² 가 자동선택 판형에 뜬다 → **추정 3행을 전부 철회**했다(§15). **대표 숫자는 「실측 전용」 줄이다** — 원본 도면이 없는 건은 분모에서 빼고 몇 건인지 같이 찍는다. ★ 26-08-25 **3차** — 점수는 안 움직였고 **「표 밖 실측」 절을 붙였다**(점수 아님 · 3건): 이 표에 없는 웨이크버니A·B·탈취제A 를 두 열로 매번 다시 재서, 주석에만 있던 주장이 아직 사실인지 보여준다. 거기서 **「실측 far 3벌은 up 을 안 움직인다」가 거짓**인 것이 드러났다 — 웨이크버니B 는 기본 4up → 목형 지정 **6up = 견적서 재현**이다(§14·§15) |
| `verify-imposition` | ★ **실측 대조 39/55** (+ **알려진 공식 오차 16건**) · 진실 적재 12/12벌 · 판형 **193조합 신규 겹침 0** · 자동선택 판형에서 겹치는 목형·모드 **1건**(칼선-14 22.23mm² — §15) · **돌연변이 16/16** · ★ **추정 되먹임 금지 0건** · ★ **표기차 검산 2/2** | ★ 진실 = **원본 칼선 PDF 폴리라인**. 게이트 3종 + 계측기 자기검사 3종 (§14) + 세 게이트 **두 열**(목형 지정/기본값). ★ 26-08-25 **2·3차** — 분자 34→37→**39** 는 전부 절을 **새로 넣은 것**이다(점수가 좋아진 게 아니다 · 분모도 같이 늘었다): 「추정 되먹임 금지」(픽스처의 추정 규칙을 실측 12벌에 먹여 겹침 0 을 요구) · 「표기차 검산」(칼선-14 를 목형 실측 D=45.00 으로 넣으면 겹침 0.00 · 잔차 0.00 인지 매번 실측) · **「철회된 후보 규칙」 상설 시연 2건**(철회한 두 추정 규칙이 **아직도 RED 인지**를 매번 다시 잰다 — RED 가 사라지면 그건 채택 신호가 아니라 **조사 신호**다). 예산은 **16건 그대로**. 「실측 자세」 절의 max 두 개에 **계측 잡음 폭**을 명시적으로 얹었다(소스코 52→60 · 칼선-14 398→410 — 독립 2D 격자가 41.64 / 401.32 를 읽는다) |
| `verify-interlock` | 엑셀 8/8 · 물림 5/6 · up **6/10** | 코리팩 엑셀 모델 대조. ⚠ 이 문서가 오래 **7/10** 이라 적고 있었다 — 26-08-25 실행값은 **6/10** 이고, 내려간 원인은 `verify-net` 과 같은 십자B 한 건이다 |
| `verify-layout` | 원지 5/11 → 인쇄기제약 3/11 | ⚠ **후보값 스캔 도구 — 앱 점수가 아니다** (스위트 자신이 출력에 그렇게 적는다) |
| `verify-pdf` | **72 / 74** (§B 합성 7 · §C 실패경로 3 · §D guessSheet 8 · §E 회귀 8 · §F /ObjStm 5 · FAIL 0) | 칼선 PDF → bbox·polygons 계약. ⚠ 오라클 SKIP 이면 exit 1 — §9 「SKIP 은 실패다」 |
| `verify-autonest` | 7케이스 × 7seed **겹침 0** (최대 0.000mm²) | 자유배치 결정성 + 독립 측정 겹침 (§11) |
| `verify-drag-shape` | **61 / 61** | 손배치 충돌 도형 — 실측 칼선 → mode 고정 · 오목 커버리지 0mm² · 물 샘 (§10·§12) |
| `verify-speclink` | ★ **348 / 348** (26-08-26 2차) | ★ 두 화면 사양 연동 + ★ **§M 쇼룸 고객 표시가**(§16) — 파서 정상 24/거부 25 · **0 부류 42건 × 통화 2벌** · 통화별 상식 밴드 · 전각 IME · **가림 비트 `nc=1`** · **`react-dom/server` 렌더 게이트**(모델이 아니라 화면 문자열을 잰다 — 이빨: 실증된 2줄 주입에서 빨개진다) · eval·Function **이름** grep(별칭 4건 이빨 + 한계 1건 명시) — §A SPEC_KEYS 빠짐없음(Proxy **59키** · 기본 1회 50 / 합집합 59 를 스위트가 찍는다) · §B **204원 게이트** + ★ **공정합계 814,969원 게이트**(반올림이 가려 주는 −970원 구간) · §B-2/3 도수·지종·판형·override 27건 · §C 사양 줄 진위 · §D 해시 왕복 · §E 단독 진입 223원 · §F~§I 왕복 고리 · §J 거래처·품명 · §K **왕복 2·3회차 판형 11개 전부** · ★ §L **목형별 구조 옵션 왕복 불변식** — 26-08-25 2차에 단언을 **방향에서 관통으로** 약화하고(「far 가 up 을 늘린다」는 미검증 가정을 계약에서 뺐다) **범위 밖 값이 화면 문구에 안 나오는지**를 새로 잰다(§13·§15) · ★ §N **고객과 같이 넣는 사양 14칸 + 수량별 개당단가**(§17) — 13칸 전부 이빨 · 왕복 3회차 무손실 · 표에도 원가 0 · ★ 26-08-26 **2차에 33건 늘었다**(315 → 348): §N ①-2 일본어 선택지 · ⑥ **전 칸 관측**(첫 줄만 재던 부등호를 내리고 105칸 스윕으로 「예외 8칸 · 최악 −3.7%」를 예산으로 잡았다) · ⑦ **cost 모드에서 표 0칸** · ⑨ 판형 뒤집힘 13칸 · ⑩ `upForPrice` · ⑪ 스윕 · ⑫ 「인쇄 없음」 |
| `scorecard2` | **8 / 15** (테이퍼 없음 8/15 · 테이퍼 적용 8/15) · ★ **목형 지정 8/15** · ★ **실측 전용 7/12** · 테이퍼 노브 배선 15/15 · ★ **목형별 노브 배선 13/13** | ⚠⚠ **원시 네스터 점수다. 앱 점수가 아니다** — 아래 「경고 ②」. 26-08-25 2차에 목형 지정 열이 10→**8** 로 내려갔다. 오른 2점(LUXEN·십자B)이 **둘 다 추정**이었고 철회했다(§15). 실측 노브(웨이크A·B far)는 폴리곤은 갈리지만 up 은 안 움직인다 |
| `scorecard-nest` | **6 / 10** (목형 미지정) · ★ **목형 지정 6/10** · ★ **실측 전용 5/8** · 지대금액 평균절대오차 **17.1%** (목형 지정 시에도 **17.1%**, 최대 −60%) | 같은 케이스의 **금액** 영향. 7→6 · 14.4%→17.1% 의 원인은 `verify-net` 과 같은 십자B 한 건. 26-08-25 2차 — 목형 지정 열의 7 과 14.4% 는 **추정 십자B 한 행**이 만든 값이었고 철회했다 |
| `scorecard-unit` | §A 물림0·맞물림허용 **6/12** · ★ **§A-die 목형 지정 6/12**(실측 전용 **5/9**) · §B 맞물림금지 **6/12** · §C 물림적용 **1/12** | 물림 방침 A/B 회귀 게이트. **§A > §C 가 유지되어야 게이트가 산다**(6 vs 1 — 유지). ★ 26-08-25 **2차 정정** — 1차에 「§A-die 8 > §B 6 = 목형을 알려주면 맞물림이 돌아온다」고 적었는데 **그 8 은 추정 3행(LUXEN·십자A·십자B)이 만든 값**이었다. 철회 후 §A-die 는 §B 와 같은 **6** 이다 = **실측만으로는 삼면접착 맞물림이 돌아오지 않는다.** 원 관측(§14 「맞물림 사실상 상실」)이 여전히 사실이다 |

### ★ 내려간 숫자를 어떻게 읽어야 하는가

내려간 항목은 넷이고 원인은 **셋뿐**이다. 셋 다 도형을 **크게** 만들거나, **크게 만든 것을 되돌리지 않는** 변경이다.

| 원인 | 무엇을 했나 | 내려간 것 | 대신 없앤 것 |
|---|---|---|---|
| ⓐ **안전 봉투** (`glue3` 아래 띠 양쪽 W 다 깊게) | 실측이 두 계열로 갈려 한 공식으로 못 덮는다 → 큰 쪽 봉투 | `scorecard-unit` §A 7→6 · `scorecard2` 10→8 · LUXEN 2→1up · 웨이크버니B 6→4up | 소스코형 **5,033mm² 실물 겹침** |
| ⓑ **`CROSS_TAB` 14.3 → 23.33** | 십자만 접착탭이 산포(24/17/15/14/12) | `verify-net` 7→6 · `scorecard-nest` 7→6 · `verify-interlock` 7→6 · 십자B 4→3up | 니치어 **1,269mm²** · 칼선-10 **100mm²** (둘 다 **자동선택 판형**) |
| ⓒ **추정 3행 철회** (26-08-25 2차 · §15) | 목형표의 추정값(LUXEN `far` · 십자A·십자B `15.70`)이 점수를 올리던 유일한 것이었고, 그 추정 **규칙**을 실측 목형에 되먹이면 실물이 겹친다 | `verify-net` 목형 7→6 · `scorecard-nest` 목형 7→6 · `scorecard2` 목형 10→8 · `scorecard-unit` §A-die 8→6 · `scorecard-nest` 목형 MAE 14.4%→17.1% | 니치어 **1,178.96mm²** · 소스코 **2,817.88mm²** · 칼선-14 **401.32mm²** (전부 그 추정 규칙을 실측에 먹였을 때) — 그리고 **점수의 순환**(견적서로 값을 정하고 견적서를 맞추는 것) |

⚠ ⓒ 는 앞의 둘과 성격이 다르다. ⓐⓑ 은 **모델을 고친** 것이고 ⓒ 는 **점수의 정의를 고친** 것이다.
「목형을 알려주면 몇 점인가」의 답이 7 이 아니라 6 이었다 — 7 은 견적서 역산으로 만든 값이었다.
그래서 이제 점수판은 **「실측 전용 점수」**를 대표 숫자로 찍는다(§15).

**「점수가 내려갔다」가 아니라 「틀린 판을 안 만들게 됐다」로 읽어라.**
근거는 안전 방향 규약이다 — 도형이 실물보다 **작으면 판에서 겹친다**(= 물리적으로 불가능한
판 = 최악). **크면 up 이 줄 뿐이다**(= 안전). 반대로 up 이 늘면 앱이 **싸게 부른다** = 영업 위험.
종전 7/10 중 일부는 실물보다 작은 도형으로 얻은 숫자였고 그중 일부가 겹치는 판이었다.

**그리고 「올라간 숫자」도 같은 엄격함으로 읽어라.** 목형별 노브(§15)가 점수를 올린다면
그 올린 값이 **실측인지 추정인지** 먼저 봐라. 26-08-25 1차에 오른 점수는 **전부** 추정이었고,
그 추정 규칙을 실측 목형에 되먹이면 자동선택 판형에서 실물이 겹쳤다. 「점수가 올랐다」는
그 자체로는 증거가 아니다 — 무엇으로 올렸는지가 증거다.

### 경고 ① — 판걸이 대표값은 `verify-net` 하나다

`scorecard2` 를 요약에 썼다가 앱 점수 붕괴를 **4주간** 가린 이력이 있다.

### ★ 경고 ② — `scorecard2` 는 **원시 네스터**를 채점한다 (앱 실경로가 아니다)

| | `scorecard2` | 앱 실경로 |
|---|---|---|
| 부르는 것 | `nest.solveLayout` **직접** | `imposition.solveImposition` |
| `printableArea`(인쇄기 990×720 클램프) | **건너뛴다** | 적용 |
| 발자국 상한 `MAX_FOOT_PCT = 92` 재시도 루프 | **건너뛴다** | 적용 |
| 판 | 견적서 「단위」 문자열 | `BASE_SHEETS` 자동선택 |

그래서 두 숫자가 **갈릴 수 있고, 실제로 갈린다.** `MAX_FOOT_PCT` 가 원시 up 을 깎아
앱 점수만 살려 두는 구간이 있다 — 폐기한 「먼 쪽 고정」 안이 정확히 그랬다
(원시 8up → 앱 6up, 흡수됨). **그러나 흡수를 일반 법칙으로 쓰지 마라**: 채택한 봉투안은
원시 4up → 앱 4up 으로 **흡수할 것이 없었다**(§14). 즉 `scorecard2` 하락이 금액에
안 나타난다는 보장은 **없다**. 이걸 모르면 다음 사람이 같은 오판을 한다.

**갱신 규칙**

1. 판걸이 대표값은 `verify-net` 「견적서 판걸이 up 재현」이다 (경고 ①·②).
2. 점수가 떨어지면 **떨어진 대로 적는다.** 숫자를 맞추려고 테스트를 고치지 않는다.
   그리고 **왜 떨어졌는지를 같은 줄에** 적는다 — 숫자만 바꾸면 다음 사람이 되돌린다.
3. 실측과 어긋나는 케이스는 지우지 말고 이유를 붙여 남긴다
   (`knownDiff` / 「목형 설계 요인」처럼).
4. 상수를 바꿀 때는 근거 주석을 같이 갱신한다. 근거 없는 상수는 다음 사람이 지운다.
5. **돌리지 않은 것을 통과라고 적지 마라.** 이 표의 모든 칸은 실행 출력에서 옮긴 것이다.
6. **점수를 올린 값이 실측인지 추정인지 같은 줄에 적어라.** 추정으로 올린 점수는 순환이다
   (견적서로 값을 정하고 견적서를 맞춘다). 대표 숫자는 **실측 전용 점수**다 — 원본 도면이
   없어 채점할 수 없는 건은 분모에서 빼되 **몇 건인지·그중 몇 건이 어긋나는지 항상 같이 찍는다**.
   감시자는 `verify-imposition` 「추정 되먹임 금지」 절이다(§15).

---

## 8. 도메인 용어 사전

### 규격·전개도

| 한글 (견적서·실무) | 코드 이름 | 뜻 |
|---|---|---|
| 가로 / 깊이 / 높이 | `W` / `D` / `H` | 완성 박스 3치수(mm) |
| 전개도 · 총규격 | `netSize`, `dieline.net` | 펼친 종이 한 장의 크기 |
| 전개도 가로 / 세로 | `netW` / `netH` | 같은 것의 두 축 |
| 칼선 | `cuts` (폴리라인) | 자르는 선 |
| 접는선 | `folds` | 누름선 |
| 뚜껑 / 바닥 | `topLid` / `botFloor` | 몸통 위·아래로 나가는 최대 깊이 |
| 날개 | `flaps.top[]` / `flaps.bot[]` | 패널별 날개 깊이 배열 |
| 텍 혀 | (tongue) | 상자 아가리로 들어가는 좁은 끝 |
| 더스트 | (dust flap) | 뚜껑 양옆 작은 날개 |
| 접착날개 · 접착탭 | `GLUE_TAB` = 14.3 | 이어붙이는 여유 폭 |
| 행거탭 · 유로홀 | `hangTab` | 걸이봉용 상단 돌출 |
| 조각 | `pieces` | 배치 입력용 **볼록** 다각형 목록 |
| 패널 | `panels[]` `{i,x0,x1,label}` | 측면1·전면·측면2·후면·접착 |

### 판형·배치

| 한글 | 코드 이름 | 뜻 |
|---|---|---|
| 원지 · 판형 | `sheet`, `BASE_SHEETS` | 종이 규격 |
| 절수 | `sheet.cut` | 전지 1장에서 나오는 장수 (1=전지, 2=2절 …) |
| 전지 / 2절 / 3절 / 4절 | `cut: 1 / 2 / 3 / 4` | |
| 사륙(4×6) / 국전 / 하드롱 | `family: "사륙"/"국전"/"하드롱"` | 원지 3계열 |
| 판형 티어 | `tier` = `"4절"` / `"2절"` / `"전지"` | 단가 구간. `"2절"` 은 2절·3절을 함께 덮는다 |
| 단위 | (견적서 열) | 인쇄기에 걸리는 **재단 크기**. 원지 규격이 아니다 — §5 |
| 대지 | (실측 자료) | 실제로 앉힌 인쇄판 한 장 |
| 판걸이 | `up`, `layout` | 판 한 장에 박스 몇 개 |
| 물림 | `BITE_SHORT` / `BITE_LONG` | 그리퍼가 잡는 띠. **판걸이에는 적용하지 않는다** — §5 |
| 맞물림 · 인터로킹 | `interlocked`, `overlapX/Y` | 반전 이웃과 날개를 겹쳐 끼움 |
| 칼선 공유 | `dx === netW` | 경계 칼선을 맞대기만 함 (겹침 0) |
| 엇갈림 | `sx` | 행마다 옆으로 미는 양 |
| 수율 | `layout.utilPct` | `up × netW × netH ÷ 판형 면적` |
| 발자국 | `layout.footPct` | `배치 외곽 bbox ÷ 판형 면적` — 상한 판정은 이쪽 |
| 무겹침 다각형 | NFP (No-Fit Polygon) | `nest.mjs` 의 배치 원리 |

### 금액

| 한글 | 코드 이름 | 뜻 |
|---|---|---|
| 정미 | `reams.net` | 필요한 종이 장수 = `ceil(수량/up)` |
| 여분 · 손지 | `reams.loss` | 작업 손실 여유 장수 |
| 지대 | `paperPrice` | 종이값 |
| 지대R · 연 | `reams.R` | 종이 수량 단위. `1R = 500 × 절수` 장 |
| 공정R | `reams.processR` | 공정 수량 단위. 절수 무관 `1,000장 = 1R` |
| 1식 | `reams.isLot` | 정미 1,000장 미만 → 최소 금액 청구 |
| 소부 | `process/soboo.mjs` | 인쇄판 제작 (도수당) |
| 도수 | `sideColors`, `fColors/bColors` | 인쇄 색 수 |
| 원색 4도 | `side.color` | CMYK |
| 별색 | `side.spot` | 지정색. 1도 = 인쇄 3회 (`SPOT_WEIGHT`) |
| 먹 | `side.black` | 검정 1도 |
| 베다 | `print.beda` | 바탕 전면 인쇄 → 별색 R단가 상향 |
| 코팅 | `opts.coatFront/Back` | 무광·유광·IR·라미 등 |
| 톰슨 · 도무송 | `opts.thomson` | 목형으로 찍어내는 공정 |
| 박 | `finish.foil` | 금박·은박·먹박 |
| 형압 · 디보싱 | `finish.emb` | 눌러 요철 |
| 부분코팅 | `finish.puv` | 부분 UV |
| 목형 | `dev.newDie` | 칼 틀 (개발비) |
| 공정합계 / 개당단가 | `totals.process` / `totals.perEA` | `perEA = round(공정합계/수량)` |
| 주문생산 | `sheet.custom` | 코리팩에 크기·절수를 직접 지정해 재단 |
| 추정 단가 | `priceEstimated` | 룩업에 없어 면적환산한 지대 단가 (견적서에 ⚠추정) |

---

## 9. ⚠ 칼선 PDF (`domain/pdf-dieline.mjs`) — 되돌리기 전에 읽어라

W·D·H 회귀식은 **변종을 못 잡는다.** iSHAP 120×150×80 은 netW 는 맞는데(557.1 =
2(120+150)+14.3, Δ1.4mm) netH 실측 324.2 가 어느 구조 공식으로도 안 나온다
(삼면접착 302.7~351.9). 그래서 협력사 PDF 를 직접 읽는다 — 추측이 사라진다.

### 흐름 — 도메인은 PDF 를 모른다

```
BoxSpec 드롭 → readDielineFile(File)
   → { pageSize, bbox, polygons, candidates[], source, unit:"mm", warnings[], pageCount, sheetId }
   → sizeMode="net" + nW/nH ← bbox          ← 기존 dieline/direct.mjs 경로를 그대로 쓴다
   → s.pdfDl 에 **통째로** 보관 (polygons 포함)  ← 3단계(드래그 배치)의 입력
```

새 도메인 경로를 만들지 않은 것이 핵심이다. `toQuoteInput` 은 `pdfDl` 을 읽지 않는다 —
치수는 `nW`/`nH` 로만 흐르고, `pdfDl` 은 화면·3단계용 원본이다.

### 되돌리면 무너지는 설계 판단 4개

| 판단 | 왜 | 뒤집으면 |
|---|---|---|
| 성분 병합은 「접는선을 공유하고 **바깥으로만** 나가는」 것만 | 실측 4건 경계오차 전부 0.00mm | 종전 y여유 60mm 규칙 → 바이오머 netH 210.0 → **267.79** (배치 이미지를 삼킴) |
| x 방향(좌/우) 병합 안 함 | 2up 대지를 읽는 게 목적 | iSHAP 무제-3 에서 옆 판 17.44mm 조각을 끌어와 557.1 → **574.54** |
| `fit = min(면적,페이지)/max(면적,페이지)` (1.0 에서 봉우리) | 「가장 가까운」이지 「가장 큰」이 아니다 | 아트보드 밖 스크래치 도형이 1순위가 된다 |
| `nodes >= 8` 필터 | 대지·도련 사각형(정점 4개)을 걸러내는 **핵심** | 실측 8건 전부 페이지를 꽉 채운 사각형이 어떤 점수로도 1순위 |

두 겹 윤곽은 사방 인셋 편차 ≤2.5mm & 최대 ≤12mm 일 때만 인정하고 안쪽을 고른다
(iSHAP 사방 5.91mm → 안쪽 채택 + warning).

### ★ 노드 병합 — 격자 반올림만으로는 **원리적으로** 부족하다 (26-08-26)

사용자 신고: 아르토 클립클로우(13948) **한 장짜리** 도면을 넣었더니 「도형 후보」에 조각
다섯이 뜨고 고를 수 있는 것도 전부 조각이었다 —
185.0×184.0 / 86.5×356.0 / 65.2×166.1 / 137.3×34.0 ×2.

**원인.** `buildComponents` 는 좌표를 0.01mm 격자(`GRID = 100`)로 **반올림**해 노드를 만든다.
이 도면의 이음새는 **0.040mm** 다 — 격자 4칸 차이라 영원히 다른 노드다.
**격자를 키우는 것은 해법이 아니다.** 칸 경계를 사이에 둔 두 점은 칸이 아무리 커져도 같은
칸에 못 들어가고(경계에 걸릴 확률만 줄 뿐이다), 그러면서 진짜로 떨어져 있는 점은 붙는다.
양쪽으로 손해다 — 반올림이 아니라 **병합**이 필요하다.

**고친 방식** (`pdf-dieline.mjs` §7 ② · `JOIN_TOL = 0.275`). 자물쇠가 셋이고, 셋 다 필요하다:

| 자물쇠 | 무엇을 막나 | 빼면 (전부 실측) |
|---|---|---|
| **열린** 폴리라인의 **끝점**끼리만 | 아트워크가 칼선 위를 지나가며 닿는 것 | 더파이러츠 아트워크 띠(518.29×23.0)를 삼켜 몸통 530.3 → **533.3** (견적 netW 가 3mm 부푼다) |
| 거리 ≤ `JOIN_TOL` | 구조 간극·니크를 잇는 것 | 아래 값 표 |
| **완전연결** — 군의 **지름** ≤ `JOIN_TOL` | 사슬 병합. union-find 는 추이적이라 0.27+0.27+… 이 이어지면 실효 반경이 **무제한**이 된다 | 지름 **1.0689mm** 짜리 군이 한 점이 됐다(소스코 C5AF4AD1) — 끊어야 할 니크 0.89 보다 크다. 웨이크A 0.7159 · 웨이크B 0.7139 · 도솔 0.7533 도 구조 치수 0.700 을 넘었다 |

⚠ **「열린」이 왜 붙었나** — 닫힌 서브패스(`h`·`re`)는 시작점을 끝에 다시 push 하므로
`ids[0] === ids[last]` 다. 닫힌 도형에 「끊긴 끝」은 없고, 그 시작 정점은 **어디서부터
그렸느냐가 정한 임의의 점**이다(첫 자물쇠가 배제하려던 「경로 중간 정점」과 성질이 같다).
종전 코드는 그것을 끝점으로 등록했고, 그래서 첫 자물쇠가 사실상 **안 걸려 있었다** —
실측 병합쌍 중 닫힌 시작점이 낀 비율: 웨이크A 850/850 · 웨이크B 1075/1075 ·
바이오머 17/17 · 도솔 27/27 · 소스코C5AF 302/343. 실제로 작동한 방어는 허용오차 하나뿐이었다.

**값의 근거 — 잇는 것 0.040 / 끊는 것 니크 0.890.** 16개 파일 전수 실측(26-08-26):

| | 거리(mm) | 무엇 |
|---|---|---|
| 이어야 함 | 0.0100 ~ **0.2746** | **실제로 이어지는 거리 전수.** 최소는 소스코C5AF·니치어·아르토, 최대는 칼선-14 모서리 필렛 T 접합. **이 버그의 이음새 0.0400 이 여기 있다** |
| 안정대 | **0.25 ~ 0.30** | 이 구간에서 16개 파일 결과가 **완전히 같다.** `JOIN_TOL = 0.275` 는 그 한가운데다 |
| 첫 변화 | 0.3000 | 소스코 삼면접착 — 여기부터 채택 도형 선분 128 → 130 |
| 끊어야 함 | 0.700 | 텍 혀가 몸통 접는선에서 물러난 폭(= 구조 치수 D−0.7). 0.7 로 올리면 16개 중 13개가 이 거리를 잇는다 |
| 끊어야 함 | **0.890** | **니크** — 목형이 오시선을 끊어 놓는 폭(소스코 가로 오시선 3/3 · §4) |

**과병합하면 무엇이 깨지나 — 그리고 왜 오라클이 그걸 못 잡나.**
니크를 이으면 오시선이 칼선 성분에 흡수되어 도형이 뭉개진다. 그런데 오시선도 텍 혀 이음도
칼선 윤곽 **안쪽**에 있어서 **bbox 가 안 변한다.** 실측: `JOIN_TOL` 을 0.95 로 올리면
소스코 삼면접착의 최대 군 지름이 **0.8900(= 니크)** 이 되는데 bbox 는 380.3×223.5 그대로고
**오라클 9건이 전부 초록이다.** 바뀌는 것은 채택 도형의 **선분 수**(128 → 170)와
`polygons` — 3단계 드래그 배치가 쓰는 충돌 도형이다.

> ⟹ **이 값을 「오라클이 안 깨지니까」로 올리지 마라. 여기서 오라클은 눈이 없다.**
> 올릴 근거는 구조 치수(0.700)·니크(0.890) **아래**라는 것 하나뿐이다.

그 자리를 대신 지키는 것이 합성 게이트 **`verify-pdf` §E10** 이다 — 끝점 0.25+0.25 사슬을
일부러 만들어, 지름 상한이 없으면 bbox 가 50 → **65mm** 로 부푸는 것을 **눈에 보이게** 했다.
(상한을 지우고 돌려 실제로 65×30 이 나오는 것을 확인하고 넣었다.)
반대 방향은 **§E9** 가 지킨다 — 0.04mm 이음새를 못 이으면 25×30 + 25.04×30 두 조각이 된다.

⚠ `JOIN_TOL` 이 격자(0.01mm)의 배수가 **아닌 것은 일부러다.** 좌표가 0.01 격자에 얹혀 있어
두 점 사이 d² 는 언제나 `1e-4 × 정수`인데 T² = `1e-4 × 756.25` 라 **정수가 될 수 없다** —
어떤 쌍도 문턱에 정확히 얹히지 못한다. 종전 0.28(= 0.01×28)에서는 d² = T² 인 쌍이 실제로
나왔고 부동소수 잔차로 갈렸다: iSHAP 무제-3 의 dx=0·dy=0.280 짜리 **6쌍이 3쌍은 병합
3쌍은 미병합**이었다(같은 도면의 기하학적으로 똑같은 간극이다). 값을 옮길 일이 있으면
**0.005 의 홀수배**로 유지해라 — 0.265 · 0.275 · 0.285 …

### ⚠ 이 도구가 원리적으로 못 하는 것

**칼선이 아닌 PDF 를 넣어도 에러가 아니라 그럴듯한 숫자가 나온다** (iSHAP 견적서 →
170.3×255.65, 표 괘선이다). 그래서 UI 는 `candidates` 드롭다운과 `warnings` 를 항상 띄우고
사용자 확인을 받는다 — **값을 조용히 견적에 넣지 마라.**

### `/ObjStm`(압축 객체 스트림) — 지원한다 (26-08-07)

칼선 PDF 8건은 전부 구식 xref table 이지만, Acrobat 을 거쳐 오는 파일은 사실상 전건이
/ObjStm 이다 — 고객사 폴더 실측 PDF 485건 중 **351건**(견적서·납품서·발주서·사업자등록증).
그 351건에 `readDieline` 을 태우면 **332건 성공 / 19건 실패**(전부 스캔 이미지·본문 텍스트라
「벡터 도형이 없다」·「칼선으로 볼 만한 도형이 없다」 = 정상), 합계 1.0초 · 최대 32ms.
⚠ 「성공」은 칼선을 맞혔다는 뜻이 **아니다** — 견적서는 표 괘선을 170.3×254.6 으로 돌려준다.
위 「원리적으로 못 하는 것」 그대로이고, 그래서 UI 가 후보·경고를 띄운다.

되돌리면 무너지는 판단 3개 (전부 `verify-pdf` §F 가 게이트한다):

| 판단 | 왜 | 뒤집으면 |
|---|---|---|
| 컨테이너 해제는 **미리**(eager), 조회는 동기 | `inflate` 는 `DecompressionStream` 이라 async 인데 `Doc.get`/`resolve` 는 파일 전역에서 동기다 | `get` 안에서 풀 수 없다 — 구조가 async 로 번진다 |
| 컨테이너 해제 실패는 **던지지 않는다.** 그 객체를 실제로 꺼낼 때 실패한다 | 하이브리드 파일은 메타데이터만 압축하고 페이지·칼선은 평문으로 둔다 | 참조도 안 되는 압축객체 하나 때문에 읽히던 파일을 못 읽는다 (F2) |
| 전수 조사 루프는 `probeDict`(던지지 않음)로 돈다 | /Catalog·/Page 를 찾는 복구 경로 3곳이 **모든** 객체 번호를 훑는다 | 남의 깨진 객체 하나가 조사를 중단시켜, 카탈로그가 바로 옆에 있어도 못 찾는다 (F5) |

⚠ **평문(type 1) 이 압축본(type 2) 을 덮으면 안 된다.** 최신 xref 가 「이 번호의 정본은
압축본」이라 했는데 옛 xref 의 평문 오프셋을 등록하면 `Doc.get` 이 평문을 먼저 보므로
**구판이 조용히 이긴다.** Acrobat 증분 저장이 정확히 그 모양이다 — 합성 대조에서 새 페이지
80×30 대신 구 페이지 50×30 이 경고 0개로 나왔다(F3). `readXrefTable`·`readXrefStream`
양쪽이 `!objStm.has(n)` 를 같이 본다.

xref 체인이 통째로 깨진 /ObjStm 파일은 `discoverObjStms` 가 본문에서 컨테이너를 직접 찾아
푼다 — `_scanAll` 은 평문 `"N G obj"` 만 훑어 압축객체가 아예 안 보이므로, 이게 없으면
멀쩡한 파일이 「PDF 가 아니거나 손상됐다」로 끝난다(F4).

지원 후에도 못 꺼내는 경우(컨테이너 해제 실패·암호화)는 **여전히 명확한 에러**다.
조용히 다른 값으로 때우지 않는다 — `verify-pdf` §C 가 그 자리를 지킨다.

### 브라우저 확인 방법

`test-pdf/` 에 칼선 PDF 를 두고 `npm run dev` → `/test-pdf/<파일>.pdf` 로 읽힌다.
⚠ **`public/` 에 두지 마라** — `vite build` 가 `dist/` 로 복사하고 `npm run deploy` 가
그 `dist` 를 **공개** GitHub Pages 로 올린다(고객사 도면 유출).

가드는 사람이 아니라 **기계가** 갖고 있다: `package.json` 의 `guard:public` 이
`predeploy` 에 걸려 있어 `public/` 에서 PDF 를 찾으면 배포가 exit 1 로 멈춘다
(하위 폴더까지 재귀 검사). `.gitignore` 의 `public/*.pdf` 는 **커밋만** 막고 배포는
못 막으며, 오히려 `git status` 에서도 안 보이게 만든다 — 그래서 가드가 따로 있다.
CI 경로는 git 체크아웃이므로 무시된 파일이 도달하지 못한다. 남은 유출 경로는
**로컬 수동 배포** 하나이고 그것을 `predeploy` 가 닫는다.

### SKIP 은 실패다 — `verify-pdf` 를 초록으로 착각하지 마라

§A 실측 오라클 8건은 **저장소 밖** 고객사 폴더에 있다(영업기밀이라 git 에 못 넣는다).
종전 집계는 SKIP 을 분모에서 빼고 `FAIL` 만 exit 1 로 올려서, 다른 PC·CI·클린 클론에서는
합성 18건만 돌면서 「18/18」 · EXIT=0 이 떴다 — **실측 대조 0건인데 만점으로 보였다.**

| 상황 | 결과 |
|---|---|
| 오라클 8건 다 읽힘 (개발 PC) | **`72/74`** · EXIT=0 ← **이것만 점수다** (26-08-25 실행값) |
| 오라클 없음, 면제 안 함 | EXIT=**1** + 「오라클 N건을 읽지 못했다」 |
| 오라클 없음, `PDF_ALLOW_SKIP=1` 또는 `--allow-skip` | EXIT=0 + 「실측 대조 미실행 — **점수 아님**」 |

오라클 위치는 `PDF_ORACLE_DIR` 로 갈아끼운다(기본값은 이 PC 경로). CI 두 워크플로는
`PDF_ALLOW_SKIP: "1"` 를 스텝 env 에 명시했다 — 면제를 **환경이 스스로 밝히게** 한 것이고,
면제해도 점수 문자열이 「점수 아님」으로 바뀌므로 초록으로 위장하지 않는다.

---

## 10. 3단계(마우스 드래그 배치)를 붙이는 자리와 순서

1·2단계(실측 추출 + 판형 캔버스)는 3단계를 **염두에 두고** 만들었다. 이 절은 그 이음새를
못 박아 둔다 — 계약을 문서에 박아두지 않으면 다음 사람이 추출기를 다시 만든다.

### 추출 결과 계약 (`readDieline` / `readDielineFile`)

```js
{
  pageSize: { w, h },            // mm. /Rotate 적용 후 = 화면에 보이는 방향
  bbox:     { w, h },            // 채택한 칼선 1개의 크기 (mm)
  polygons: [ [[x,y],…], … ],    // ★ 3단계의 입력. 지금도 반드시 채워진다
  candidates: [ {                // 점수순. 사용자가 드롭다운에서 바꾼다
    bbox:{w,h,x0,y0}, points, area, segs, parts,
    fitPct, offPct, score, chosen, polygons,   // ← 후보마다 polygons 를 따로 담는다
  }, … ],
  source, unit:"mm", warnings:[], pageCount, sheetId,
}
```

| 필드 | 3단계가 알아야 할 것 |
|---|---|
| `polygons` | **폴리라인 묶음이다.** 닫힌 윤곽도, `nest.mjs` 가 요구하는 볼록 조각도 아니다. 2점짜리 선분이 다수 섞인다(실측: 웨이크버니 `2/3/2/2/2/2/2/2` · 소스코 `2/2/6/17/2/2/2/9`). **그리기**에는 그대로 쓴다. `nest` 에 넘기려면 세그먼트 스티칭 + 볼록 분해가 필요하다 — 근거는 `pdf-dieline.mjs` `polysOf()` 주석 |
| 좌표계 | **페이지 절대좌표**(mm, y 위로). bbox 원점 기준이 **아니다** (소스코 `polygons[0] = [[396.99,211.47],[355.39,211.47]]`, 페이지 420×297). `SheetCanvas` 는 그래서 `translate(-x0, y0+h) scale(1,-1)` 하나를 씌운다 |
| 합집합 bbox | `bbox` 와 정확히 일치한다. `verify-pdf` 가 Δ0.02mm 로 게이트한다 |
| **어느 polygons 를 읽나** | `s.pdfDl.polygons`(추출기 1순위)가 아니라 **`pdfPickOf(s).polygons`**(사용자가 고른 후보). 두 벌이 있고 정답은 후자다 — iSHAP 무제-3 은 후보가 4개(2up 대지의 좌/우 × 도련/칼선)라 실제로 갈린다. 규칙은 `ui/state.mjs` `pdfPickOf` **한 곳**이 소유한다 |

### 캔버스 이음새 (`ui/viz/SheetCanvas.jsx`)

| 이음새 | 지금 상태 | 3단계가 할 일 |
|---|---|---|
| ① 좌표 변환 | `px(mm)` `py(mm)` `sc(mm)` · `boxTf(box)`. 역변환 = `(v - RULER) / scale` | 그대로 쓴다 |
| ② 그릴 배치 | `props.placement` — 주면 그걸 그리고, 없으면 `layout.boxes` | 손배치 배열을 넘긴다. 되돌림은 `placement={null}` 하나 |
| ③ 히트영역 | `<g data-cell={i}>` 안 `rect` 가 `fill="transparent"` | 그 `<g>` 에 핸들러를 단다 |
| ④ 계산 되먹임 | `overrides.up` (`state` 의 `mUp`/`mUpV` → `quote.mjs` 분기 ①) | 손배치 up 을 견적에 넣으려면 이 통로를 쓴다. **도메인은 손대지 않는다** |

⚠ ③ 의 `fill` 을 `"none"` 으로 되돌리지 마라. `"none"` 은 히트테스트 **대상이 아니고**
`"transparent"` 는 대상이다 — 단어 하나 차이로 기능이 죽는다. 실측: `"none"` 일 때
칸 0 내부 81표본 중 `g[data-cell]` 에 잡힌 점이 **4개**뿐이었고(그 4개도 `rect` 가 아니라
칸번호 `text`) 나머지는 판 배경 `rect` 로 빠졌다. `"transparent"` 로 바꾼 뒤 **81/81**.

### 붙이는 순서 (이 순서를 지키면 도메인·추출기를 건드릴 일이 없다)

1. **상태 한 칸** — `state.mjs` 에 `placement: null` 추가. `toQuoteInput` 에는 **넣지 않는다**
   (치수는 `nW`/`nH` 로만 흐른다는 §9 규칙 유지).
2. **초기값** — `pdfPickOf(s).bbox` 와 `layout.boxes` 로 씨앗을 만든다.
   `layout.up === 0` 이면 캔버스가 이미 원점에 1개를 판 밖으로 걸쳐 그려 두므로
   (`data-ghost="1"`, 히트영역 있음) 그것이 출발점이다.
3. **드래그** — `<g data-cell>` 에 `onPointerDown/Move/Up`. mm 변환은 위 ①.
   격자 스냅·판 경계 클램프는 이 단계에서 UI 가 판단한다.
4. **그리기** — `<SheetCanvas placement={s.placement} …/>` 한 줄. 캔버스는 이미 준비됐다.
5. **겹침 검사** — `nest.mjs` 의 폴리곤 겹침 판정을 재사용하려면 여기서
   **스티칭 + 볼록 분해**가 필요하다(위 표 `polygons` 행). 그 전까지는 발자국 사각형
   겹침만으로도 실용적이다.
6. **견적 반영** — `u("mUp", true); u("mUpV", String(placement.length))`.
   `quote.mjs` 는 이미 `overrides.up` 을 받는다. **도메인 수정 0줄.**

되돌리기: 5·6 을 빼고 `placement={null}` 로 두면 1·2단계 상태로 정확히 돌아간다.

---

## 11. 배치 엔진 — 규칙격자(기본) vs 자유배치

`solveImposition({ dieline, sheet, hangTab, engine })` 의 `engine` 하나로 갈린다.
**기본은 `LAYOUT_ENGINE.GRID` 이고, 견적 경로는 이 기본만 쓴다.**

| | 규칙격자 `grid` | 자유배치 `free` |
|---|---|---|
| 엔진 | `nest.solveLayout` | `nest-free.solveFree` |
| 배치를 정하는 것 | (dx, dy, sx, 반전규칙) **5개** | 한 장씩 앉힌 좌표 **전부** |
| 자리마다 다른 자세 | 못 한다 | 한다 |
| 틈에 **한 장만** 더 | 못 한다(열·행 단위) | 한다 |
| 목형·톰슨 | 그대로 선다 | **사람이 확인해야 한다** |
| 견적 반영 | 자동 | 손배치로 확정해야(`mUp`/`mUpV`) |

### 왜 기본을 바꾸지 않았나

재현율 기준선이 전부 격자 위에서 교정됐고(견적서 78건 역산 · `verify-net` · `scorecard-nest`),
자유배치는 **최적해를 보장하지 않는다**. 그래서 `solveImposition` 은 free 를 부를 때도
격자를 같이 풀고 **큰 쪽을 쓴다**(동점이면 격자 — 같은 up 이면 규칙격자가 유리하다).
반환값의 `layout.free.up` 은 **max 를 씌우지 않은 원본**이다 — 이걸 max 로 덮으면
「자유 < 격자」(= 엔진 결함)를 아무도 못 본다.

### ★ 실측 비교표 (2026-08-17 · 견적서 케이스 전건 실행값)

`grid` = 현행 엔진 · `free` = 자유배치 원본 · `견적서` = 실측 up.

⚠ **26-08-17 값이다. 「격자 일치」 열은 그 뒤 날개 재보정으로 내려갔다**
(현행: 앱 실경로 **6/10** · `scorecard-unit` **6/12** — §7). 이 표에서 읽어야 하는 것은 절대값이
아니라 **「자유<격자」 열이 전부 0」** 이라는 사실이고, 그건 엔진 성질이라 바뀌지 않았다.
숫자를 갱신하려면 두 엔진을 다시 전건 스윕해야 한다 — 이번 패스는 안 돌렸다.

| 케이스군 (26-08-17) | 건수 | 격자 일치 | max(격자,자유) 일치 | 자유<격자 |
|---|---|---|---|---|
| `verify-net`·`scorecard-nest` (BASE_SHEETS, 앱 실경로) | 10 | **7** | **8** | 0 |
| `scorecard-unit` (견적서 「단위」를 판으로) | 12 | **7** | **8** | 0 |
| `verify-net` 비폴리곤 6건 (직사각 경로) | 6 | 6 | 6 | 0 |
| 행거탭 0/15/20mm (맞뚜껑A 국2) | 3 | 3 | 3 | 0 |
| **합계** | **31** | **23** | **25** | **0** |

바뀐 것은 **삼면B 210×90×180 한 건**이고, 그 한 건이 두 케이스군에 다 들어 있다:

```
삼면B 210×90×180 · 46전지(990×720 클램프) · 전개도 614×354
  격자 2up (1×2, 발자국 61%)  →  자유 3up (발자국 87%, 전략 lb·자세R/Rf, 128ms)
  견적서 3up ✓                     ← verify-net 「남은 불일치 3건」 중 하나가 이것이다
```

`verify-net` 주석이 이 건을 「46전지를 990×720 으로 클램프한 뒤 3열이 안 들어간다」고
적어 뒀는데, **3열이 아니라 자세를 섞으면 들어간다**는 것이 자유배치의 답이다.
⚠ 그렇다고 `verify-net` 점수가 6/10 → 7/10 이 되지는 **않는다.** 그 스위트는 기본 엔진
(격자)을 채점하고, 자유배치 결과는 사람이 손배치로 확정해야 견적에 들어가기 때문이다.
점수를 올리고 싶으면 기본 엔진을 바꿔야 하는데 그건 위 「왜 기본을 바꾸지 않았나」다.

### 자유 < 격자 — 넓은 스윕에서 나온 2건과 그 원인

견적서 케이스 31건에는 0건이지만, 구조 3 × 치수 14 × 판형 6 = **252건 스윕**에서 2건 나왔다.
겹침·판밖 위반은 **0건**, 겹쳐서 버린 해도 **0개**, 평균 161ms · 최대 995ms.

| 케이스 | 격자 | 자유 | 원인 | 근거 |
|---|---|---|---|---|
| 삼면 30×30×60 @46전지 | 35 | 33 | **탐색 예산 소진**. 알고리즘 결함이 아니다 | `ops` 2,027,237 / cap 2,000,000 → 9개 전략 중 **1개**만 돌고 끊겼다. 예산 ×4 → **38up**(격자보다 많다) · ×20 → **41up** |
| 맞뚜껑 30×30×60 @하4 | 15 | 13 | **드롭&슬라이드가 못 찾는 자리.** `nest-free.mjs` 「한계」의 "두 이웃에 동시에 닿아야만 가능한 자리" 그대로다 | 예산 미소진(445,403 / 2,000,000)이고 9개 전략 전수 실행. 격자는 `5×3↺⇅` = **엇갈림(sx) + 반전 맞물림**으로 이겼다 |

둘 다 `imposition` 이 큰 쪽(격자)을 쓰므로 **사용자에게는 손해가 없다.**
예산은 `nest-free.DEFAULT_OPS` 다 — 올리면 첫 줄이 회복되지만 버튼이 1~4초가 된다.
측정만 남기고 기본값은 그대로 뒀다(근거 없이 상수를 움직이지 않는다는 §4 규칙).

### 정책 3개 (되돌리기 전에 읽어라)

| 판단 | 왜 | 뒤집으면 |
|---|---|---|
| **엔진을 캐시 키에 넣는다** (`ck` 첫 조각) | 같은 규격·판이라도 두 엔진의 답이 다르다 | 자유배치를 한 번 돌린 뒤 **견적 경로(격자)** 가 자유배치 결과를 캐시에서 돌려받는다. 화면 표시 없이 up 만 틀린다 — `custom` 키와 정확히 같은 사고다 |
| **발자국 상한을 자유배치에 적용하지 않고 알린다** (`free.footOver`) | 격자의 상한 루프는 「maxUp 한 단계 ↓ = 열·행 하나 ↓ = 외곽이 실제로 작아진다」는 격자 성질에 기댄다. 자유배치는 한 장을 빼도 bbox 가 그대로일 수 있어 그 루프가 성립하지 않는다 | 루프가 up 만 깎고 발자국은 안 줄어 자유배치가 무의미해지거나, 조용히 상한 초과 배치를 낸다 |
| **행거탭은 양축에서 뺀다** | 격자는 배치 전체가 `rotated` 한 값이라 탭 축을 고를 수 있다(`solveNest` 가 축별로 나눠 부른다). 자유배치는 자리마다 자세가 달라 축이 하나로 안 정해진다 | 탭 자리를 침범한 배치가 나온다. ※ 대신 HT>0 에서 자유가 격자보다 작을 수 있는데, 큰 쪽을 쓰므로 무해하다(위 표 행거탭 3/3) |

### UI (`ui/panels/BoxSpec.jsx`)

- 「배치 엔진」 Select → 자유배치는 **버튼으로만** 돈다. 자동 재계산 금지 —
  1회가 60~1,000ms 라 입력마다 돌리면 W 칸에 「140」을 치는 동안 세 번 돌아 앱이 멎는다.
- 엔진·실행결과는 `s` 가 아니라 **BoxSpec 로컬 state** 다. `s` 가 바뀌면 `toQuoteInput` 이
  새 객체를 만들어 `buildQuote` 가 통째로 다시 돈다 — 도메인이 읽지 않는 값이라 넣을 이유가 없다.
- 「손배치로 이어받기」 → `s.placement` + `mUp`/`mUpV`. **`quote.mjs` 수정 0줄** (§10-6 과 같은 통로).
- 겹침 경고는 **up 이 나가는 자리**에 붙는다 — 손배치 up 줄과 같은 함수(`overlapPairs`)를 쓴다.
  ⚠ `overlapPairs` 는 겹친 칸의 **인덱스 Set** 이다(쌍 배열이 아니다). `.size` 로 읽어라.
- 규격·후보·판·행거탭이 바뀌면 앞 결과에 「낡았다」를 붙이고 이어받기를 잠근다.
  **지우지는 않는다** — 조용히 지우면 「눌렀는데 사라졌다」로 읽힌다.
- PDF 칼선을 얹은 상태에서는 도메인 전개도가 `direct`(= bbox 직사각형)라 자유배치가
  격자와 같은 답만 낸다. 그래서 `makeDragPart` 가 분해한 **실제 칼선 조각**으로 푼다
  (손배치와 같은 도형이어야 그림·스냅·겹침이 갈리지 않는다). 그 사실을 화면에 적는다.

---

## 12. 도면 표기 — 칼선 · 오시선 · 접착면

고객이 도면에서 읽는 것은 세 가지다: **어디가 잘리고, 어디가 접히고, 어디에 풀이
발리는지.** 구분이 없으면 전개도는 도면이 아니라 선 뭉치로 보인다.
표기를 정하는 곳은 `showroom/showroom-core.mjs` `partOf`(무엇이 어느 종류인가)와
`showroom/ShowroomPage.jsx` 상수(그 종류를 어떻게 그리는가) **두 군데뿐**이다.
실무앱 쪽 렌더(`ui/viz/DielineShape.jsx`)와 섞지 마라 — 목적이 다르다.

### 선 3종과 그리는 규칙

| 종류 | 코드 | 그림 | 실무 용어 | 일본 부스 표기 |
|---|---|---|---|---|
| 칼선 | `part.cuts` | **실선** 0.9px (선택 1.15 · 미리보기 1) | 칼선 · 잘리는 선 | カットライン |
| 오시선 | `part.folds` | **파선** 0.7px `dash "2.5 2"` (미리보기 `"3 2.5"`) | 오시 · 압선 · 접히는 선 | 折り罫（罫線） |
| 접착면 | `part.glue` | **평톤** 같은 잉크 `fillOpacity 0.20`, 테두리 없음 | 접착탭 · 풀이 발리는 면 | のりしろ |

**색은 한 색(`INK #1b2129` = K100 먹)뿐이다. 형태로만 가른다.**

### 왜 이 표기인가 — 원본에는 선 구분이 **없다**

칼선 PDF 실측 6건(고객사 5곳 · 구조 2종)을 콘텐트 스트림에서 셌다:

| 연산자 | 횟수 | 뜻 |
|---|---|---|
| `d` (dash) | **0회** | 원본에 파선이 없다 |
| `w` (선굵기) | 파일당 1회 | 굵기 분기가 없다 |
| `K` (색) | 1회 | 색 분기가 없다 |
| `S` (stroke) | 파일당 1~2회 | 칼선·오시선이 **한 그래픽 상태의 서브패스**로 한 번에 그려진다 |
| 해칭·Pattern | 0건 | 접착면 표기가 없다 |
| 「접착」「오시」 글자 | 0건 | 글자 표기도 없다 |

즉 **「실선/파선」도 「진함/옅음」도 원본에는 없고 우리가 얹는 관용이다.** 그래서
화면이 그 사실을 글로 적는다(아래 「출처 3갈래」). 적지 않으면 고객이 우리 관용을
실측으로 읽는다.

⚠ **PDF 경로에서 「내부 직선이면 오시선」으로 유추하지 마라.** iSHAP 2건은 stroke 가
1개뿐이고 채택 칼선이 아트워크 색면의 윤곽이다 — 유추하면 **조용히 틀린 도면**이 된다.
그래서 PDF 경로는 `folds = []` · `glue = []` 이고 전부 칼선으로 그린다.
**부스 시연 순서가 여기서 갈린다:** 표기(칼선·오시선·접착면)는 **치수 입력** 탭으로
보여주고, 실측 정확도는 그다음에 PDF 로 보여준다. PDF 를 먼저 얹으면 고객이 읽을
구분이 화면에서 사라진다. 화면도 그 한 줄을 적는다(「선 구분을 보시려면 치수 입력 탭」).

### 대비 — 되돌리기 전에 읽어라

| 대상 | 값 | 대비 | 판정 |
|---|---|---|---|
| 칼선 `#1b2129` on 흰 판 | — | **16.20:1** | 기준 |
| 오시선 (종전 `#d5dae1`) | — | 1.41:1 | **폐기.** 「선이 있다」는 사실조차 전달하지 못한다 — 부스 조명·빔프로젝터에서 통째로 소실된다 |
| 오시선 (현행 = 칼선과 같은 잉크) | — | 16.20:1 | 위계를 주고 싶으면 `#5b6472`(5.98:1)까지다 |
| 접착면 톤 (종전 0.10) | — | 1.24:1 (desktop) / 1.27:1 (tablet) | **폐기.** WCAG 1.4.11(의미 있는 그래픽 3:1)을 크게 밑돈다 |
| 접착면 톤 (현행 0.20) | 산술 1.50:1 | **1.52:1** (desktop 1.26 px/mm) · **1.52:1** (tablet 0.45 px/mm) | 채택. **두 배율이 같다** — 톤은 알파라 배율을 안 탄다(해칭을 버린 이유) |
| 범례 뜻풀이·출처·정밀도 (종전 `#aab2bd`) | 10.5~11.5px | 1.86:1 | **폐기.** DOM 에 있는 정직성 문구가 읽히지 않으면 없는 것과 같다 |
| 같은 줄 (현행 `#6a7280`) | 10.5~11.5px | **4.20:1** | 채택 (색 추가 0) |

- 접착면 톤이 3:1 을 못 넘는 것은 **원리적**이다. 3:1 이면 `#a0a4a9` 급이 되어 그 위의
  칼선·오시선을 삼킨다 = 정보를 잃는다. 대신 접착면의 **경계**는 칼선(16.20:1)과
  오시선이 이미 긋고 있고 톤이 하는 일은 「이 닫힌 면이 그 면이다」 하나다.
- 범례 글자 4.20:1 은 WCAG 1.4.3 의 4.5:1 에 아직 못 미친다. `#5b6472`(5.21:1)면 넘지만
  그건 색을 하나 늘리는 것이고, 이 화면의 「알록달록 금지」와 맞바꿀 값이 0.3 포인트뿐이다.
- 회색 두 단의 용도가 갈려 있다: `sub #6a7280` = **읽어야 하는 글**,
  `faint #aab2bd` = **값에 붙은 이름표**(옆의 숫자가 뜻을 나른다)와 장식. 섞지 마라.

⚠ **접착면에 실선 테두리를 두르지 마라.** 접착탭의 한 변은 **오시선**이다. 실선을 깔면
파선의 빈칸이 잉크로 채워져 「접히는 선」이 「잘리는 선」으로 보인다 — 표기가 거짓말을 한다.

### dash 값의 단위는 화면 px 다

폴리라인이 `vectorEffect="non-scaling-stroke"` 라 굵기와 dash 가 **둘 다 뷰박스 배율을
타지 않는다.** 그래서 판을 작게 그려도 파선이 파선으로 보인다.
브라우저 실측: 같은 선을 ×0.44 / ×1 / ×4 로 래스터해 센 결과 세 배율 전부 **대시 3px ·
주기 4.44px** 로 동일했고, 대조군(vectorEffect 없음)은 2.53 / 4.44 / 16.67px 로 배율을
그대로 탔다. 값은 가시성 산술로 정했다(실측값이 아니다): 가장 짧은 오시선이 접착탭
경계(맞뚜껑 D=15 → 15mm)이고 화면 배율이 0.41~1.62 px/mm 라 화면 길이가 6~24px 다.
종전 `"4 3"`(주기 7px)은 대시를 1~2개만 남겨 실선과 구별이 안 됐고 `"2.5 2"`(주기 4.5px)
면 최소 1.4주기가 남는다.

### ★ 접착면 표기는 세 자리가 **완전히 같아야 한다**

미리보기(사이드바) · 판 · 범례 견본. 한 자리만 다른 무늬면 고객은 범례를 읽고 판에서
그 무늬를 찾다가 못 찾는다 — 적대검증에서 major 로 잡힌 상태가 정확히 그것이었다
(범례 45° 해칭 vs 판 평톤, 둘 사이 거리 0px · 해칭이 실제로 있는 미리보기와는 424px).

**그래서 해칭(`<pattern>`)을 버렸다.** `pattern` 은 `non-scaling-stroke` 같은 장치가
없어 간격이 mm(사용자 단위)로 굳는데, 판의 배율은 컨테이너에 따라 **0.45~1.62 px/mm**
로 3.6배 움직인다(desktop 0.94~1.26 · tablet 0.45 · 좁은 판 1.62 실측). 어느 mm 값을
골라도 한쪽 배율에서 1px 미만으로 뭉개지고, 폭 14.3mm 탭에서는 그 덩어리가 곧 톤이다.
해칭이 살아 있던 곳이 높이 112px **고정**인 미리보기 하나뿐이었던 이유가 이것이다.
되돌리려면 판의 실제 CTM 을 측정해 셀을 px 로 환산해야 한다(ResizeObserver + state) —
표기 하나에 렌더 루프를 늘릴 값이 없다.
범례 견본만 CSS 라 `fillOpacity` 를 못 쓴다 → `GLUE_FILL_CSS = rgba(27,33,41,0.20)`
(27,33,41 = `#1b2129`). **두 값이 갈리는 순간 위 major 가 되살아난다.**

### 접착면 위치를 어떻게 판정하나

`partOf` 는 **도메인이 이미 붙여 준 표시**를 읽는다 — 새 기하 0줄:

```js
const gi = (dl.meta || []).findIndex(m => m.part === "tab");   // 접착탭 조각
glue: gi >= 0 ? [dl.pieces[gi]] : []
```

- ⚠ **`cuts` 에서 집으면 안 된다.** `meta` 는 `pieces` 와 인덱스가 정렬이지만 `cuts` 는
  아니다 — `geometry.addFlap` 이 조각 2개에 칼선 1개를 밀고, 날개 없는 경계·좌측 최외곽
  칼선을 따로 민다.
- 폴리곤 미확정 구조(G형·G형트레이·direct)는 `meta` 가 `[{panel:0, part:"body"}]` 뿐이라
  `glue` 가 자동으로 빈다(§1 `!st.polygon` 분기). 화면 코드가 구조 id 목록을 들 필요가 없다.
- 실측이 이 자리를 확정했다(칼선 6건 6/6 오차 0.00): 패널 폭이 좌→우
  `[접착탭 15.00][W][D][W][D−0.70]` 이고 합이 정확히 `2(W+D)+14.30` 이다. 즉
  `geometry.GLUE_TAB = 14.3` 은 「탭 폭」이 아니라 **탭 15.0 − 마지막 패널의 0.7 부족분**
  이다(`netW` 공식은 결과적으로 정확하므로 상수는 그대로 맞다).
  ★ **그래서 화면에 「접착탭 14.3mm」라고 숫자를 쓰지 않는다** — 실물 탭은 15.0mm 이고,
  고객에게 어긋난 숫자를 말하게 된다. 범례는 이름만 쓴다.

### 좌우 뒤집힌 도면 — 우리 프레임은 실측의 거울상이다

우리 `xEdges` 는 `[D, W, D, W, 탭]` 로 탭이 **오른쪽**이고, 실측 칼선은
`[탭, W, D, W, D−0.7]` 로 **왼쪽**이다. 좌우를 뒤집으면 일치한다.

**뒤집지 않는다.** 폴리곤·배치·검증 전부가 이 프레임 위에 서 있고 bbox·up·금액에는
영향이 0 이다(거울상은 면적·외곽이 같다). 영향은 표기 딱 하나다 — 「접착탭이 붙는
**상대면**」이 우리 프레임에서는 **왼쪽 끝 패널0** 이다. 그런데 아래 이유로 어차피
그리지 않으므로 실효 영향이 없다. 뒤집고 싶어지면 먼저 `verify-dieline` 불변식
360/360 과 `verify-net` 판걸이 **6/10**(26-08-25 현행)을 재현할 계획을 세워라.
(180° 반전 배치의 거울은 이것과 무관하다 — `it.flipped` 는 로컬 중심 회전이고
`SheetCanvas.boxTf` 와 **같은 식**이라 두 화면의 그림이 갈리지 않는다.)

### 일부러 그리지 않는 것 3개 — 각각 조건이 있다

| 안 그리는 것 | 왜 | 붙일 수 있는 조건 |
|---|---|---|
| 접착탭이 붙는 **상대면** | 실제로 풀이 닿는 것은 그 면의 **뒷면**이고 이 화면은 인쇄면(앞면)이다. 앞면에 해칭을 얹으면 **거짓 표기** | 앞/뒷면을 나눠 그리는 화면이 생기면 |
| **아래 날개 접착**(자동바닥) | 삼면접착이 측면1+바닥2 로 세 면이 붙는 것은 맞지만 **어느** 날개가 붙는지를 `flaps.bot` 이 말할 수 없다. 26-08-19 이후 아래 띠는 `[min(W,b)/2, b, min(W,b)/2, b]` = **양쪽 W 를 다 깊게** 그리는 안전 봉투이고, 실물은 목형마다 **한쪽만** 깊다(§14 두 계열). 즉 봉투는 「깊은 쪽이 어디냐」를 **일부러 답하지 않는 도형**이다. 지금 그리면 **틀린 확언** | 목형별 「깊은 날개 위치」가 **구조 옵션 입력**으로 들어온 뒤 (§14) |
| 도면 안 **「접착」 글자** (SVG `<text>` = **0개**) | 읽히지 않는다. 탭(14.3mm)의 화면 폭이 미리보기 5~6.4px · 판 7~23px 이고, 탭 안에 세운 글자는 높이가 탭 폭을 넘을 수 없다. 판 쪽은 읽힐 크기지만 탭이 up 개(4~6) 뜨고 180° 반전 칸에서는 거꾸로 뒤집힌다 — 「칸 위에 배지를 붙이지 않는다」와 정면 충돌 | 판 뷰의 **선택된 칸 하나**에만 붙이는 것은 가능하다 |

같은 판단의 선례: 니크 0.89mm · 뚜껑 오시선 0.5mm 오프셋도 쇼룸 배율에서 1px 미만이라
그리지 않기로 했다. **읽히지 않는 표기는 정보가 아니라 잡티다.**

### 오시선을 몸통 구간으로 자른다 (`bodyFolds`) — 도메인 수정 0줄

실측(칼선 6건 6/6): 세로 오시선의 y 범위는 **몸통 높이 그대로**다. 몸통 =
`[net.topLid, net.netH − net.botFloor]` 이고 실행값으로 그 폭이 정확히 `H` 다
(삼면 140×43×130 → 130.00 · 삼면 50×50×150 → 150.00 · 맞뚜껑 150×15×150 → 150.00 ·
십자 70×70×55 → 55.00). 날개 쪽은 실물에서 니크(0.89mm)로 끊긴 **별개 세그먼트**다.

우리 `folds` 는 「양쪽 날개가 겹치는 깊이까지」 이어져 띠를 관통한다. 자르지 않으면:

1. **칼선 위에 파선을 겹쳐 그린다** — 그 구간이 날개의 옆면 칼선과 좌표가 같다
   (`geometry.addFlap` 이 `[x0,base]→[x0,yF]` 를 `cuts` 에 민다). 「잘리는 선」을
   「접히는 선」이라고 두 번 말하는 셈이다. 실측: 몸통밖 159.63mm 중 127.35mm.
2. **종이 밖에 선이 뜬다** — 날개가 테이퍼로 좁아진 구간에는 도형이 없다.
   실측 32.25mm(삼면 140×43×130) · 34.05mm(50×50×150).

세로 오시선은 언제나 몸통을 관통하므로 잘려서 **사라지는 선은 없다**(가로 오시선은 y 가
이미 경계값이라 무변화). 그래도 길이 0 은 그리지 않는다.

### ★ 자기검증 — 범례는 화면에 **실제로 있는 것만** 말한다

`Legend` 는 `part.folds`·`part.glue` 가 비면 그 줄을 **아예 내지 않는다.** 그래서
「범례에는 있는데 도면에는 없다」가 **원리적으로 불가능**하다. 실측 확인:

| 구조 | 범례 | 출처 줄 | 정밀도 줄 |
|---|---|---|---|
| 맞뚜껑 · 십자 · 삼면접착 | 칼선 + 오시선 + 접착면 | `srcBox` | (없음) |
| G형 · G형트레이 (`!st.polygon`) | **칼선 1줄** | `srcOutline` | (없음) |
| 칼선 PDF | **칼선 1줄** | `srcPdf` | `pStitched`/`pRaster`/`pHull`/`pBbox` |

**출처 3갈래 · 조건은 `part.folds.length`.**

```jsx
note={srcMode === "pdf" ? t.srcPdf : (part.folds?.length ? t.srcBox : t.srcOutline)}
```

- `srcBox` 는 「날개 모양은 표준형 근사입니다」라고 말한다. G형·G형트레이에는 **틀린
  말**이다 — 근사된 날개가 아니라 날개·오시선·접착면이 **아예 없다**(전개도가 빈 직사각
  1조각이다). 그래서 `srcOutline`(「바깥 윤곽만 표시합니다」)로 갈랐다.
  ⚠ `gtype_tray` 는 드롭다운 라벨이 「✓칼선 4건」이라 고객이 실측 검증된 도면으로 읽는데
  화면에는 빈 사각형이 뜬다. **크기는 실제로 검증됐고 모양이 미확정**이라는 것이 사실이고,
  `srcOutline` 이 그 둘을 나눠 말한다.
- 조건이 `folds.length` 인 이유: 폴리곤이 확정된 구조는 `folds` 가 원리적으로 0 이 될 수
  없고, `folds === 0` 이 `dieline/index.mjs` `!st.polygon` 분기의 **관측 가능한 흔적**이다.
  화면 코드가 구조 id 목록을 들지 않아도 갈릴 수 있다(들면 구조를 추가할 때 같이 썩는다).

### 정밀도 줄은 **PDF 경로에서만** 나온다

`showroom-core.precisionKeyOf` 가 `"exact"`(= 구조 폴리곤 = 치수 입력 경로)에 **`null`**
을 돌려준다. 이 함수가 재는 것은 「PDF 폴리라인을 볼록 조각으로 분해할 때 근사가
끼었는가」= **배치 판정 도형**의 정밀도이고 **그림의 정밀도가 아니다.**

치수 경로에서 「칼선 폴리곤 그대로 (근사 없음)」을 내면 같은 화면 173px 위의 출처 줄
「날개 모양은 표준형 근사입니다」와 정면 충돌한다. 두 줄이 같은 크기·같은 대비로 마주
서면 **더 확언조인 쪽**이 이기고, 고객은 우리 근사를 실측으로 읽는다 — 적대검증 major
였다. G형은 더 나쁘다: 도형이 사각형이므로 mode 는 여전히 `"exact"` 이고,
**「근사 없음」이 붙은 빈 네모**가 뜬다.
치수 경로의 정직성은 출처 줄이 소유한다. **두 곳에서 말하지 않는다.**

### 이 화면이 지키는 규칙 (깨면 존재 이유가 사라진다)

1. 색은 잉크 한 색(`INK`) + 강조 한 색(`acc`). 칸마다 다른 색을 칠하지 않는다
   (`LayoutViz` 반면교사). 채우기는 접착면 하나뿐이고 그것도 같은 잉크의 투명도다.
2. SVG `<text>` = **0개**. 칸 위에 배지·번호를 붙이지 않는다. 선택은 테두리 하나로
   말하고, 표기의 **이름은 판 밖 HTML 범례**가 갖는다.
3. 겹침은 **절대 그리지 않는다.** 그릴 배치는 전부 `auditItems` 를 통과해야 한다
   (`commit` 게이트 + 그리는 자리의 `drawable` 게이트 = 두 겹). 드래그 미리보기도
   성립하지 않는 자리는 **마지막으로 성립한 자리**에 머문다 — 한 프레임도 보여주지 않는다.
4. 기하는 이 화면에 한 줄도 없다. 스냅·겹침 `ui/viz/nest-drag.mjs`, 자동배치
   `nest-free.mjs`, 판 `imposition.printableArea`, 단가 `domain/quote.buildQuote`.
   **도메인 수정 0줄**이고 up 은 기존 `overrides.up` 통로로만 흐른다(§10-6 과 같은 길).

### 알려진 미결 — 뷰포트 메타

`index.html` 에 `<meta name="viewport">` 가 **없다.** 실제 태블릿·폰에서는 레이아웃
뷰포트가 981px 로 잡혀 375px 화면에 0.38× 로 축소된다 → 범례 11.5px 가 **4.4 device px**,
접착탭이 4.0 device px. 부스 기기가 태블릿·폰이면 위 표기가 **원리적으로 안 읽힌다**.
(768px **뷰포트 자체는 괜찮다** — 래스터 실측: 접착탭 7×59px · 톤 대비 1.52:1 ·
파선의 가장 진한 픽셀 11.55:1(잉크가 실제로 찍힌다) · 범례가 2줄로 접히고 4up 유지.)

한 줄만 넣으면 안 되는 이유: 375px 에서 **292px 고정 사이드바 + 판이 한 줄에 안 들어간다.**
좌패널 접힘이 같이 필요하다. **부스 기기를 정한 뒤에 판단하는 게 맞다** — 정하지 않고
메타만 넣으면 축소 대신 가로 스크롤이 생겨 다른 방향으로 못 쓰는 화면이 된다.

★ 26-08-18 결정: **부스 기기 = 노트북.** 뷰포트 메타는 이번에도 넣지 않았고, 확인은
1280×800 · 1920×1080 두 크기에서 했다(둘 다 가로 스크롤 0 · 판 1524×745px @1920).
태블릿·폰 이야기는 기기가 바뀔 때 다시 꺼내라.

---

## 13. 사양 링크 — 두 화면이 **같은 값을 말하게** 하는 통로 (26-08-18 · 26-08-19 보강)

### 무엇이 고장나 있었나

`App.jsx` 는 「두 화면은 상태를 공유하지 않는다」고 못 박고 있었고, 쇼룸은 자기
**표준사양 한 벌**(`showroom-core.STD`)로 계산했다. 그래서 같은 박스인데

> 삼면접착 140×43×130 · 295AB · 4×62 · 4,000 · 원색4 · IR · 단면접착
> → **견적서 204원 · 쇼룸 223원**

이 나왔다. 갈린 줄은 지대 **한 줄**이고 원인은 지종이 안 넘어간 것뿐이었다
(413,969 vs 492,196). 15개 스위트는 전부 초록이었다 — 어느 스위트도 「두 화면이
같은 입력을 보는가」를 재지 않았기 때문이다. 그래서 `verify-speclink` 를 만들었다.

또 견적서 화면에는 **쇼룸으로 가는 길이 아예 없었다**(주소를 직접 쳐야 했다).

### 방식 — URL 해시. 왜 그것인가

| 후보 | 새로고침 | 링크·북마크 | 이번 범위 |
|---|---|---|---|
| App 으로 상태 끌어올리기 | ✗ 날아간다 | ✗ | ✗ QuoteApp 상태 **71칸**을 App 이 들어야 한다 |
| sessionStorage | ✓ | ✗ **링크가 안 된다** | ✓ |
| **URL 해시** ← 채택 | ✓ | ✓ | ✓ 버튼 1개 + 코덱 |

부스에서 「그 사양 그대로 다시」를 주소 하나로 넘길 수 있고, 브라우저 뒤로가기가
그대로 「돌아가기」가 된다(hashchange 라우터가 이미 있었다). 주소창에 값이 보이므로
무엇이 넘어갔는지 눈으로 확인된다:

```
#/showroom?q=v~1;boxType~glue_3side;bW~140;bD~43;bH~130;qty~4000;paperId~AB295L;sheetId~4x62;fpColor~1;fpSp~0;fpBk~0
```

형식은 `키~값;키~값` 이고 **초기값과 다른 키만** 싣는다(위 예가 11쌍 · 실측 213자).
`v~1` 은 형식 버전이자 **「사양이 실렸다」 표시**다 — 이게 없으면 초기값 그대로인
사양을 넘겼을 때 페이로드가 비어 받는 쪽이 「단독 진입」으로 읽고 표준사양으로
갈아탄다(= 금액이 갈리는 길). 지우지 마라.

### 코덱이 `ui/state.mjs` 안에 있는 이유

싣는 목록(`SPEC_KEYS` 59개)이 곧 **`toQuoteInput` 이 읽는 키**다. 다른 파일에 두면
단가를 움직이는 키가 하나 늘 때 변환기만 고쳐지고 목록은 조용히 낡는다 — 그 순간
증상이 바로 위의 204 vs 223 이다. 변환기 바로 아래 두면 같이 보인다.

빠짐없음은 눈으로 세지 않는다. `verify-speclink` §A 가 **Proxy 로 실제 읽기를 잡아**
대조한다. ⚠ **합집합으로 재야 한다** — 9개(`hangV`·`mRV`·`mPriceV`·`admin`·`embRpr`·
`foilType`·`foilS`·`foilRpr`·`puvS`)는 짝 불리언이 참일 때만 읽혀서, 기본 상태 한 번만
재면 48개만 잡히고 나머지가 빠져 있어도 초록이 된다(= 죽은 픽스처).

### 일부러 안 싣는 것 — 거래처·품명 (그리고 그 대가를 어떻게 갚았나)

**URL 에 싣지 않는다.** 단가에 안 들어가고(§A 가 그 사실을 못 박는다), 이 주소는 부스에서
**고객 앞 화면의 주소창**에 뜨며 링크로 나가기도 한다. 거기 다른 고객사 이름이 실려
있으면 안 된다. 방침을 바꾸려면 `SPEC_KEYS` 에 두 줄을 더하면 된다 — 트레이드오프를 알고 더해라.

★ **그런데 「안 싣는다」만으로는 부족하다는 것을 26-08-26 에 실측으로 배웠다.** 해시는
사양 전부를 싣고, 사양이 있으면 **받는 브라우저가 같은 계산을 다시 한다.** 쇼룸 표시가에서
정확히 그 일이 났다 — 마진 수식은 안 실었는데 링크를 받은 고객 화면이 원가를 다시
계산해서 그렸다. 고친 방식(「가렸다」는 비트 `nc=1` 을 형제 파라미터로 싣는다)과
그 한계는 **§16** 에 있다. 다음에 「이건 안 실으면 안전하다」를 판단할 때 그 절을 먼저 읽어라.

⚠ **대가를 안 갚은 시기가 있었다.** 왕복하면 두 칸이 조용히 초기값으로 돌아갔는데,
초기값 「코리팩」·「십자B 패키지」는 **빈칸이 아니라 그럴듯한 다른 고객사 이름**이다.
그대로 출력하면 **남의 고객사 이름이 박힌 견적서**가 나간다(적대검증 minor ⑥).
방향이 「조용히 틀린 출력」이라 금액 버그와 같은 등급으로 다룬다.

**26-08-19 조치 — 그 두 칸만 `sessionStorage` 가 나른다** (`state.mjs` `saveNames`/`loadNames`,
키 `cria-quote.names.v1`). 위에서 **사양** 전체를 sessionStorage 로 나르는 안을 버린 이유는
「링크가 되지 않는다」였는데, 이 두 칸에는 그것이 **정확히 원하는 성질**이다:

| 성질 | 이 두 칸에는 |
|---|---|
| 링크·주소창에 안 실린다 | ✓ 유출 방침이 그대로 지켜진다 |
| 같은 탭에서는 산다 | ✓ 왕복·새로고침에서 이름이 안 사라진다 |
| 탭이 닫히면 사라진다 | ✓ 다음 사람이 남의 거래처명을 물려받지 않는다 |

복원에 실패하면(다른 탭에서 링크를 열었다 · 사생활 보호 모드) **비운다.**
「코리팩」으로 접으면 안 된다 — 그게 이 고장의 본체다. 비었다는 사실은 `App.jsx` 의
링크 안내 줄이 말한다. `loadNames()` 가 없을 때 돌려주는 것은 초기값이 아니라 **`null`**
(=「모른다」)이고, 그 구분이 이 조치의 전부다. 게이트는 `verify-speclink` §J 4건.

### 쇼룸이 실려 온 사양을 쓰는 법

`baseOf(carried)` 가 계산의 바탕을 정한다: **실려 왔으면 그것, 없으면 `STD`.**
그 위에 쇼룸이 **화면에서 실제로 만질 수 있는 것만** 덮어쓴다 — 구조·치수·판형·수량·
판걸이. 도수·지종·코팅·후가공·톰슨·접착·개발비는 안 덮어쓰므로 base 값이 그대로 산다.
「없으면 STD」가 **단독 진입(`#showroom`)이 종전 그대로 도는 이유**다(223원 · 4up).

사양 줄(화면 왼쪽 아래)도 두 갈래다. 실려 왔으면 `specSummaryOf` 가 **실제 견적
라인의 `spec` 문자열**을 이어서 적는다 — 문구를 새로 짓지 않으므로 견적서와 같은 말이
나오고, 공정이 늘어도 고칠 데가 없다. ⚠ 도메인 라벨이 한국어 한 벌뿐이라 **일본어
화면에서도 그 줄만 한국어**다. 대안은 실려 온 사양을 두고 「標準仕様・AB 350g」라는
**틀린 말**을 계속 적는 것이고, 이 화면의 규칙에서 그게 더 나쁘다.

### 되돌리기 전에 읽어라 — 실측으로 밟은 함정 2개

**① `key={payload}` 는 두 화면 **다** 걸어야 한다.**
`App` 이 화면 종류를 안 바꾸고 사양만 바꾸면(`#showroom?q=…` → `#showroom`,
주소창 직접 편집, 뒤로가기) 컴포넌트가 그대로 남아 `useState` 초기화 함수가 다시
돌지 않는다. 실측: 사양을 실어 들어간 쇼룸에서 해시만 `#showroom` 으로 바꾸니
**입력칸은 실려 온 값(140×43×130 · 4×62) 그대로인데 단가만 표준사양 223원**으로
갈렸다 — 화면과 주소가 서로 다른 사양을 말하는 상태다.

**② 왕복 고리가 안 닫히면 「앱이 싸게 부른다」로 끝난다.**
복귀 링크는 판걸이(up)를 **격자해와 다를 때만** 직접입력으로 싣는다(같으면 앱이
스스로 같은 값을 내고, 켜두면 뒤에 치수를 바꿨을 때 낡은 up 이 남는다). 그런데 그
상태로 **다시** 쇼룸에 들어가면 쇼룸이 스스로 풀어 개수가 되돌아간다:
쇼룸 3up 247원 → 복귀 견적서 3up 247원 → 다시 쇼룸 **4up 204원**.
`showroom-core.capToQuoteUp` 이 그 자리를 막는다(자동해를 고정값까지만 남긴다).
⚠ 고정값이 **실제로 들어가는 수보다 많으면 자르지 않는다** — 없는 자리를 그릴 수는
없고, 겹침을 그리지 않는다는 §12-3 계약이 우선이다.
⚠ 사람이 직접 한 행동(+한 장 · 빈 곳 채우기 · 삭제)에는 걸지 않는다.

### ★ 왕복 불변식 — 무엇이 몇 회차까지 안 움직여야 하는가

부스에서 사람은 **여러 번** 왔다 갔다 한다. 1회차만 맞추면 2회차에서 금액이 갈린다.
`verify-speclink` §H·§K 가 **판형 11개 전부**에 대해 아래를 강제한다.

| 회차 | 불변식 | 왜 이 회차에서 깨졌었나 |
|---|---|---|
| **1회차** 견적서 → 쇼룸 | 판형·up·개당단가가 같다 | 지종이 안 넘어가 **204 vs 223** (이 절의 출발점) |
| **1↔2회차** 쇼룸 → 견적서 → 쇼룸 | 같다 | 쇼룸이 링크 up 을 무시하고 스스로 다시 풀어 **3up 247원 → 4up 204원** 으로 되돌아갔다 → `capToQuoteUp` |
| **2회차** 재진입 | 운영자가 확정한 up 이 **되살아난다** | `seedFromAuto` 가 없어 3up → 2up 으로 되돌아가며 **240 → 312원 (+30%)** |
| **3회차** 한 번 더 | 여전히 같다 | 2회차만 고치면 3회차에서 다시 흔들린다 — 그래서 §K 가 세 번 잰다 |

⚠ **「계산에 쓸 up 과 링크에 실을 up 을 갈라라」는 제안은 채택하지 않았다.** 가르면 화면이
못 그린 고정값을 링크에 계속 싣게 되고 = 「그린 2up(312원) · 청구 3up(240원)」 =
**청구가 그림보다 싸다** = 금지 방향이다. 되살아나면 `3≠2` 라서 `linkStateOf` 는 손대지
않아도 스스로 싣는다. 되살리기가 원리적으로 불가능한 경우(사람이 손으로 만든 배치)는
**덜 그리고 그린 수로 청구**하고 화면이 `pinLost` 로 말한다 — 비싸지는 쪽이라 안전하다.
§I 가 그 방향을 **부등호로** 관측한다(고정 9up 158원 → 그린 4up **204원**, +46원).

### 실측 — `verify-speclink` **348/348** (26-08-26 **2차** 재실행 · EXIT=0)

| 절 | 확인 | 결과 |
|---|---|---|
| §A | `toQuoteInput` 이 읽는 키 ↔ `SPEC_KEYS` | **59개 완전일치** · 중복 0 · 안 읽히는 키 0 (Proxy 실측, 토글 켠 판·끈 판 **합집합**) — ★ 이 수는 스위트가 **찍는다**(기본 1회 50개 / 합집합 59개). 종전에는 주석에 「48 / 57」로 박혀 있다가 노브 2개가 늘자 그대로 늙었다 |
| §B | ★ 소스코 140×43×130 · 295AB · 4×62 · 4,000 · 원색4 · IR · 단면접착 | 견적서 **204원** = 쇼룸 **204원** (종전 204 vs 223) |
| §B-2 | 도수 5종 | 191 / 204 / 212 / 233 / 238 — 전부 두 화면 일치, **서로 다른 값 5종**(=이빨 있음) |
| §B-3 | 지종·판형·수량·코팅·박·형압·부분UV·톰슨·접착·행거탭·override·주문생산·전개도직입 | **22건 전부 일치** (155~322원) |
| §C | 사양 줄이 실제 견적 라인과 같은 말을 하는가 | ✓ 「AB라이트 295g · 원색 4도 · IR코팅 · 톰슨 일반형 · 접착 단면」 |
| §D | 해시 왕복 | 59키 무손실 · 페이로드 **213자** · 이상한 페이로드 7건에 안 죽음 · 모르는 키 무시 |
| §E | 단독 진입 `#showroom` | 표준사양 **223원 · 4up** (종전과 같음) |
| §H | 판형 11개 왕복 | **11/11** 판형·up·개당단가 동일 |
| §K | 왕복 **2·3회차** | **11/11** 판형 전부 1=2=3회차 동일 · 자유해를 실제로 채택한 판형 **4개**(0이면 §K 가 이빨 없음) |
| §N | ★ 고객과 같이 넣는 사양 14칸 + 수량별 개당단가 (**§17**) | 목록 계약 7 · 하위호환(over 없으면 **바이트 동일**) 2 · 이빨 **13칸 전부 금액이 움직이고 두 화면이 같다** · 왕복 **1·2·3회차** 사양 14칸 무손실 · 수량 표(지금 수량 칸 = 큰 글씨 · 수량↑ 개당↓) · ★ 원가 유출 0(모델·거부·가림) · ★ 26-08-26 **2차**: 일본어 선택지 15개 · **cost 모드 표 0칸** · 판형 뒤집힘 13칸 전부 금액 생존 · `upForPrice`·`resetKindOf` 모델 + 소스 계약 · **105칸 스윕(예외 8칸 · 최악 −3.7%)** · 「인쇄 없음」 |
| §M | ★ 관리자 수식 → 고객 표시가 (**§16**) | 정상 24 / 거부 25 · ★ **0 부류 42건 × 통화 2벌 전부 거부** · 경계 `/408`·`-203.5` 는 ¥1 로 산다 · 통화별 밴드(엔 `*1.7` → ¥379 는 그리되 경고) · 전각 6건 = 반각과 같은 값 · 가림 비트 `nc=1` 왕복 + **비트 없으면 종전대로 원가**(이빨) · **렌더 게이트**(2줄 주입에서 빨개진다 · 원가 숫자 557 부재) |

도면 표기(§12) 쪽 정적 확인 — SVG `<text>` **0개**(소스의 3건은 전부 주석) ·
`ShowroomPage.jsx` 의 stroke 색 **2종**(`INK #1b2129` · 판 테두리 `#c3cad4`).
⚠ 26-08-18 의 브라우저 래스터 실측(1280×800 · 1920×1080 · 콘솔 에러 0 · 대비값)은
**이번 패스에서 다시 돌리지 않았다.** 대비 수치의 출처는 §12 표이고 날짜는 26-08-18 이다.


---

## 14. 날개 프로파일 재보정과 게이트 3개 (26-08-19 · 숫자는 26-08-25 **3차** 재실행값)

「치수로 그린 도면이 실물과 모양이 다르다」에서 시작해, 날개 프로파일을 실측으로 갈아엎고
그 과정에서 **게이트가 자기 공식으로 진실을 짓고 있던 것**을 발견해 고친 기록이다.

### 무엇이 틀려 있었나 (칼선 PDF 12벌 재측정)

| 자리 | 종전 코드 | 실측 | 결과 |
|---|---|---|---|
| 삼면 위 띠 | `[s,s,s,s]` 네 패널 전폭 | W 패널 `s` / D 패널 `D/2` | 위 띠가 통짜 사각형 |
| 삼면 아래 띠 | 깊은 날개를 **D 패널**에 | 깊은 날개는 **W 패널** | 4/4 표본과 어긋남(−30~−39mm) |
| 맞뚜껑 뚜껑 | 위=패널1 / 아래=패널3 **분리**(역텍) | **한 W 패널이 둘 다**(스트레이트텍) | 더파이러츠 잔차 **−86.01mm** |
| 맞뚜껑 혀 `t` | 16.5 | 산포 15.0 / 16.5 / **18.0** | 더파이러츠 실물 겹침 510mm² |
| 십자 접착탭 | 공용 `GLUE_TAB` 14.3 | 십자만 산포 24/17/15/14/12 | 니치어 **−9.03mm** → 1,269mm² 겹침 |

### ★ 깊은 날개 위치 — 실측이 **두 계열로 갈린다**. 근거는 3/5 다

| 계열 | 목형 | W:D |
|---|---|---|
| 탭에서 **먼** W 가 깊다 | 웨이크A 46×46 · 웨이크B 36×36 · 도솔 50×50 | W≈D |
| 탭에 **붙은** W 가 깊다 | 소스코 140×43 · 칼선-14 80×44.5 | W≫D |

⚠ 「3/4 · 반례 1건」이라고 적혀 있던 시기가 있었다. **틀렸다** — 칼선-14 를 `cross`→`glue3` 로
재분류하면서 그 표본이 소스코와 같은 계열인 것을 근거 집계에서 뺐다. 실제는 **3/5** 다.
「D 검증범위 36~50 밖」이라는 면책도 거짓이었다(D=44.5 는 범위 **안**).

**W≫D 가설은 소스코 자신의 실측이 반증한다.** 소스코 아래 띠는 `[29.0, 30.0, 29.0, 58.0]` 으로
W2 에도 30.0 이 있다 — 「한쪽만 깊다」가 아니다. 가설대로 넘기면 그 30.0 을 0 으로 그려
겹침 56.2mm² 가 남는다. 자유 문턱을 도입하는 5표본 적합인데 **적합 대상조차 못 덮는다.**

### 채택 — 안전 봉투. 그리고 그 대가

```
위 띠  [D/2, s, D/2, s]           → [s, s, s, s]
아래 띠 [min(W,b)/2, b, min(W,b)/2, 0] → [min(W,b)/2, b, min(W,b)/2, b]
```
두 부등식(`idx1 ≥ b` from 도솔 65.0 · `idx3 ≥ b` from 칼선-14 60.0)이 동시에 성립해야 하므로
`[.., b, .., b]` 가 **최소** 봉투다. 더 작게 그리는 안은 어느 쪽이든 −58~−65mm 부족을 남긴다.

**폐기한 안과 각각의 대가 — 지우지 마라**

| 안 | 대가 |
|---|---|
| 먼 쪽 고정 | LUXEN 견적서 2up 재현 ✓ / **소스코형 5,033mm² 실물 겹침** ✗ |
| 붙은 쪽 고정 | LUXEN 1up ✗ |
| **양쪽(채택)** | LUXEN 2→1up · 웨이크버니B 6→4up (견적서 재현 상실, **비싸게 부름**) |

⚠ 「`scorecard2` 는 원시 호출이고 앱은 `MAX_FOOT_PCT` 가 흡수한다」는 완화 논리를 실측했고
**봉투에는 적용되지 않는다**: 먼쪽안은 원시 8up → 앱 6up(흡수 ✓), 봉투안은 원시 4up → 앱 4up
(**흡수할 것이 없다**). 즉 봉투의 비용은 원시 점수만이 아니라 **실제 금액**이다.
그래도 채택한다 — 대안은 「싸고 물리적으로 불가능한 판」이고 up↓ 은 안전 방향이다.

### ★ 대가 중 가장 큰 것 — 삼면접착에서 맞물림이 사실상 사라졌다

**이 절은 다음 사람이 반드시 읽어야 한다.** 봉투는 「모른다」를 「크게 그린다」로 바꾼 것이지
「알아냈다」가 아니다.

**관측값 (26-08-25 `scorecard-unit` 실행 출력):**

| | 점수 | 배치 열 |
|---|---|---|
| §A 물림0 · **맞물림 허용** | **6/12** | 12건 전부 `칼선공유` |
| §B 물림0 · **맞물림 금지**(칼선공유만) | **6/12** | 12건 전부 `칼선공유` |

두 표가 **점수도 배치도 완전히 같다.** §A 와 §B 를 가르려고 만든 A/B 인데 지금은 갈리지
않는다 — 즉 **엔진이 맞물림을 하나도 못 만들고 있다**는 뜻이고, 이 표는 그 사실을 잡는
관측기로만 살아 있다. (§A ≠ §B 가 되면 맞물림이 돌아온 것이다.)

**그런데 맞물림은 실재한다** — 실측 대지 삼면E **20.8mm** · 칼선-05 **43.9mm**.
즉 이 봉투는 실물을 재현하지 못한다. **보수적으로 틀린다.**
방향이 안전(도형이 커짐 → up ↓ → 비싸게 부름)이라 채택했을 뿐이지, 맞는 것이 아니다.

**한 공식으로는 원리적으로 못 덮는다.** 실측이 두 계열로 갈리는 것이 확정 사실이기 때문이다
(바로 위 3/5 표). 어떤 단일 공식을 골라도 다른 계열에서 음수 잔차(=겹침)를 낸다.

**제대로 푸는 길은 하나뿐이다 — 봉투가 아니라 「깊은 날개 위치」를 구조 옵션 입력으로 받는다.**

* ⚠ **선행 작업이 있다.** `flaps()` 는 지금 `opt` 를 **받지 않는다** — `dieline/index.mjs:39`
  는 `st.flaps?.(W, D, H)`, `:59` 는 `st.flaps(W, D, H)` 다. `netSize(W,D,H,opt)` 와
  `pieces(W,D,H,{net,flaps,opt})` 만 `opt` 를 받는다. 그러니 첫 줄은 **`opt` 를 `flaps` 까지
  꿰는 것**이고, 그건 두 호출부 수정이다.
* 값의 모양: `deepFlap: "both" | "far" | "near"`
  (`both` = 지금의 봉투 = **기본값. 모르면 봉투다**).
  실측 5벌이 어느 쪽인지는 이미 안다 — 계량 때 쓴 표를 **여기 옮겨 둔다**
  (코드의 시제품 스위치는 지웠다. 아래 「지뢰 제거」 참조):

  | 목형 `W\|D\|H` | 깊은 날개 | 근거 |
  |---|---|---|
  | 웨이크버니A `46\|46\|138` | `far` (먼쪽) | 실측 |
  | 웨이크버니B `36\|36\|168` | `far` | 실측 |
  | 도솔 `50\|50\|150` | `far` | 실측 |
  | 소스코 `140\|43\|130` | `near` (붙은쪽) | 실측. ⚠ 이 한 벌이 `far` 를 쓰면 실물 겹침 5,033.8mm² 가 난다 — 두 계열로 갈리는 증거가 바로 이것이다 |
  | 칼선-14 `80\|45\|30` | `near` | 실측 (발주표기 `80\|44.5\|30`) |

  ⚠ 이 표를 **그대로 코드에 박지 마라.** 목형 치수를 키로 쓰는 하드코딩 표는 같은 치수의
  다른 목형에 조용히 오적용된다. 값은 **입력**(구조 옵션)으로 받고 이 표는 **기본 추정의
  근거 자료**로만 써라. 모르는 목형은 무조건 `both`(봉투) = 안전측이다.
* 붙일 조건: **맞물림으로 up 을 버는 목형의 견적서**가 확보되면. 지금 있는 것은 대지 사진뿐이고
  두 건 다 up 2 로 같아 **금액에 나타나지 않는다** = 채점할 수 없다.
* 붙이고 나서 재계량할 것: `verify-net` · `scorecard-nest` · `scorecard-unit` §A vs §B
  (§A > §B 로 갈라져야 맞물림이 돌아온 것이다) · `verify-imposition` 겹침 게이트.

#### ✔ 지뢰 제거 — `G3MODE` 환경변수 (26-08-25 삭제 완료)

`dieline/glue3.mjs:3-16` 에 **「계량 전용 임시 스위치 (측정 후 원복 — 커밋 금지)」** 블록이
있었다(`__TBL_D`/`__TBL_E`/`__g3mode` + 284행 호출부). **지웠다.** 봉투(A) 경로를 직접 쓴다.

**「기본값이 A 라 아무것도 안 하면 무해하다」는 틀린 진단이었다.** 실제로는 아무것도
안 해도 **dev 서버에서 삼면접착 견적이 통째로 안 나왔다**:

* `src/` 는 브라우저에서 그대로 실행된다 → 브라우저에 `process` 가 **없다**
* → 삼면접착 전개도를 그릴 때마다 `ReferenceError`
* → `App.jsx:142` 의 try/catch 가 삼켜서 `q=null` → 견적서가 「좌측 패널에 정보를
  입력하면…」 **빈 화면**. 쇼룸도 도면·up·단가가 전부 빈다. **사용자에게 보이는 에러는 0.**
* 프로덕션 빌드는 무사했다 — 롤업이 `process.env` 를 `{}` 로 접는다(dist 번들 `s1={}`).
  Node 에도 `process` 가 있으니 **16개 스위트가 전부 초록인 채 dev 실행만 조용히 죽는**
  형태였다. 위험이 「환경변수를 건드렸을 때」로 축소 서술돼 있었다 — 그게 아니라 **상시**였다.

★ 규약으로 남긴다: **`src/` 안에서 `process` 를 참조하지 마라.** 계량 분기가 다시
필요하면 환경변수가 아니라 **함수 인자**(목형별 구조 옵션)로 받아라. 위 `deepFlap` 이
바로 그 정식 경로다. 「테스트는 다 초록인데 앱만 죽는」 결함은 이 한 줄에서 나온다.

### ★ 게이트 3개 — 각각 무엇을 채점하고, **무엇을 채점하지 않는가**

`test/verify-imposition.mjs`. 진실은 셋 다 **원본 칼선 PDF 폴리라인**이다.
셋을 나눠 둔 이유는 **서로 못 잡는 것을 잡기 때문**이고, 그러므로 **하나만 믿으면 안 된다.**

★ 26-08-25 — 셋 다 **두 열**로 잰다. 목형별 구조 옵션(§15)이 같은 W·D·H 에서 **두 도형**을
내기 때문이고, 종전처럼 기본값만 재면 목형을 아는 사용자가 받는 도형이 무검증이다.

| 열 | 무엇인가 | 왜 채점하나 |
|---|---|---|
| **목형 지정** | `test/die-profiles.mjs` 가 그 목형에 실어 주는 값 (= 앱이 실제로 내는 도형) | ★ **절대선** — 실측 목형은 자기 옵션으로 실물 겹침 0. 견적서 up 재현보다 위다. 여기서 겹치면 견적서가 아니라 **모델의 다른 값**이 틀렸다는 신호다 |
| **기본값** | 옵션 없음 (= 목형을 **모르는** 사용자가 받는 도형) | 기본값이 실물에서 겹치면 앱이 물리적으로 불가능한 판을 판다. 예산으로 덮지 않는다 |

두 열이 **같은 숫자**를 내면 채점은 한 번만 한다(같은 사실을 두 번 세면 예산 건수만 부푼다).
갈리는 순간 자동으로 두 줄이 되고, 갈림 자체가 「옵션이 도형을 바꿨다」는 신호다.

| 게이트 | 채점하는 것 | **채점하지 않는 것** | 26-08-25 결과 |
|---|---|---|---|
| **실물 겹침** | 앱이 **모델 폴리곤**으로 고른 배치에 **실물 폴리라인**을 얹어 격자 0.3mm 샘플링. 「이 판이 물리적으로 가능한가」 | ① 앱이 **안 고르는** 배치. 격자 솔버가 지금 마침 안 쓰는 빈자리의 오차는 **원리적으로 안 보인다** ② 모델이 실물보다 **큰** 것(=up 손해)은 겹치지 않으므로 0.00 이다 | **193조합 신규 0** (두 열) · 자동선택 판형에서 겹치는 목형·모드 **1건**(칼선-14 22.23mm²) |
| **잔차** | 모델의 netW·netH·날개깊이 8칸 **≥ 실측**. **음수만** 실패 | ① 날개의 **옆면 모양** — 깊이 숫자만 본다 ② **양수 잔차**(과대)는 무제한 통과 = 「얼마나 비싸게 부르는가」를 안 잰다 | 12벌 통과 · 예산 2건 · **여유 정확히 0.00 인 칸 36개**(목형 지정 열 기준. 32→36 은 옵션이 봉투를 실측 크기로 좁힌 결과다) ← 표본 하나 더 들어오면 여기가 먼저 깨진다 |
| **커버리지** | 열 0.2mm 포함검사 — 「**실물에 있는데 모델에 없는 재료**」의 면적·최대깊이·최악 지점 | ① 「모델에 있는데 실물에 없는 재료」 — 안전 방향이라 **일부러** 안 센다 ② 배치·금액 | 12벌 **전건 예산**(= 안 재던 축을 재기 시작한 결과). 두 열이 **12/12 동값** — 옵션이 좁히는 곳(탭 폭 · 실측 0 인 얕은 날개)에는 실물 재료가 없기 때문이다 |
| **실측 자세** (신설) | 목형표가 **일부러 안 싣는** 옵션(소스코·칼선-14 의 `near`)의 겹침을 매번 다시 잰다 | 그 옵션의 잔차·커버리지 — 어느 목형도 near 를 싣지 않아 표본이 그 줄을 안 지나간다 | 소스코 **51.98mm²** @하4 2up · 칼선-14 **397.53mm²** @46 16up. **양방향 실패** — 커지면 「대가가 늘었다」, 0 이 되면 「목형표에 실어라」 |

* **NFP 를 쓰지 않는다.** NFP 로 NFP 를 검증하면 아무것도 검증하지 않는다.
* 계측기 자기검사 3종: ① 모델 폴리곤 재측정 = 0.00mm²(nest 가 보증한 0 과 일치) ·
  ② 2% **확대** 시 겹침이 검출되는가(소스코 0.00 → **921.75mm²**) ·
  ③ 2% **축소** 시 부족이 늘어나는가(**12/12벌**). 이빨 없는 계측기의 초록이 가장 위험하다.
* 복원은 `layout.boxes` 로 한다. **`layout.cells` 로 하지 마라** — `{i,j,x,y,flip}` 뿐이고
  자세(`rotated`)가 없어서 자유배치에 겨누면 조용히 틀린다(같은 배치가 boxes 0.00 / cells 9,357mm²).
* 세 게이트가 서로를 메운다 — 위 띠 `D/2` 돌연변이는 **겹침 게이트가 놓치고**(위 ①)
  잔차·커버리지가 잡는다. 반대로 끝단폭·테이퍼 4축은 잔차가 못 보고 커버리지만 본다.
* ★ **돌연변이도 두 열로 채점한다.** 노브가 생긴 뒤로 한 열만 보면 돌연변이가 노브 뒤에
  숨는다 — 실측으로 확인됐다: `CROSS_TAB 23.33 → 14.3` 과 `CROSS_TAB_W 기본 24.03 → 15.70`
  두 축은 **기본값 열에서만** 빨간불이 켜진다. 십자 5벌이 전부 자기 탭폭을 실어서
  목형 지정 열은 한 글자도 안 바뀌기 때문이다. 스위트가 「한 열에서만 잡힌 축」을 찍는다.

### ★★ 규칙 — **모델로 진실을 지으면 안 된다**

**기대값이 실측인지 코드 출력인지 항상 확인해라.** 이 저장소는 그 함정을 **두 번** 밟았다.

| 언제 | 무엇을 했나 | 왜 안 보였나 |
|---|---|---|
| `xEdges` 「실측 오차 0.0mm」 | 기대값을 **코드 출력**으로 잡았다 | 유일한 픽스처 바이오머가 `D=15 = 탭 15.0` 이라 참·거짓 두 프레임이 **같은 값의 순열**이 되어 0.0 으로 보였다. 실제로는 내부 접는선 4개가 전부 +0.70mm 어긋나 있었다 |
| 첫 겹침 게이트 | 「실물」을 모델과 **같은 netW 공식 · 같은 `TIP_W` · 같은 테이퍼 규약**으로 재합성했다 | netW/netH 오차 · 끝단폭 오차 · 테이퍼 모양 오차가 **정의상 0** — 원리적으로 검출 불가. 실제로 뚫려 있던 구멍: 칼선-14 실측 netW **264.30** vs 모델 263.30 → 표준판형 10종 **전부** 겹침 23mm² 인데 **두 게이트 다 통과**. 그리고 모델이 실물 외곽을 못 덮는 구간이 **12벌 전부**에 있었는데 아무 게이트도 안 봤다 |

**실무 규칙 셋**

1. 게이트의 기대값은 **저장소 밖 원본**(칼선 PDF · 견적서 · 엑셀)에서만 온다.
   코드가 낸 숫자를 픽스처에 적는 순간 그 축은 채점되지 않는다.
2. 픽스처가 **한 벌**이면 그 한 벌이 우연히 대칭·동값인지 의심해라(바이오머 사례).
3. 새 게이트를 만들면 **먼저 이빨을 확인해라** — 일부러 틀리게 만들고 빨간불이 켜지는지.
   그게 아래 돌연변이 게이트다.

### 돌연변이 게이트 14건 — 스위트가 **스스로** 돌린다. 그런데 무엇을 보증하나

상수를 **작게**(=도형이 작아지는 = 위험한 방향) 만들면 빨간불이 켜지는지 자동 검사한다.
축별 내역: netW 2 · netH 3 · 날개깊이 2 · 끝단폭(tip) 3 · 테이퍼 4 = **14축, 14/14 검출.**

**보증하는 것** — 이 14축은 **채점된다.** 누가 상수를 위험한 쪽으로 되돌리면
`npm run verify` 가 빨개진다. 되돌림이 조용히 통과할 수 없다.
이 게이트를 만들기 전에는 **5건**이 출력 숫자를 한 글자도 안 바꾸고 통과했다 —
즉 그 5축은 **아무도 채점하지 않고 있었다**(끝단폭·테이퍼기울기·전폭구간·접착탭모따기).

**보증하지 않는 것**
* 상수 **값이 맞다**는 것. 「되돌리면 빨개진다」는 「지금 값이 정답」과 다르다.
* **큰 쪽**으로의 변경. 일부러 안 잰다 — 도형이 커지는 것은 안전 방향이기 때문이다.
  그 대가(up ↓ = 비싸게 부름)를 재는 것은 `verify-net`·`scorecard-nest` 의 일이다.
* 14축 **밖**. 목록에 없는 축은 여전히 채점되지 않는다.

`find` 문자열이 정확히 1회 나오지 않으면 그 자체로 실패다(조용한 no-op = 가짜 초록 방지).
⚠ `fs.cpSync` 는 이 저장소 경로(한글 포함)에서 node v24 · Windows 를 **즉사**시킨다
(`0xC0000409`). 에러가 아니라 크래시라 try/catch 로도 못 잡는다 — `readdir`+`copyFileSync` 로 우회.

### 예산(KNOWN/RES/COV)은 「원인이 특정된 **미해결**」의 자리다

원인이 특정되고 조치가 **안전측**이면 예산이 아니라 **고칠 자리**다. 실제로 예산 뒤에
자동선택 판형의 실물 겹침이 숨어 있었다 — 니치어 1,269mm²(@국2 6up · 실경로가 고르는 판) ·
칼선-10 100mm². 둘 다 예산을 **삭제하고** 고쳤다(`CROSS_TAB` 23.33 · 혀 18.0).
되돌리면 그 줄이 다시 필요해지므로 되돌림이 즉시 드러난다.

### ★ 남은 예산 **전건** — 원인 · 크기 · 조건 · 되돌림 신호 (26-08-25 실행값)

「실측 대조 39/55 (+ 알려진 공식 오차 **16건**)」의 16 이 정확히 이 표다.
숫자 뒤의 괄호는 스위트가 들고 있는 **예산값**이다 — 실측이 예산을 넘으면 그 자리에서 FAIL 이다.

#### ① 실물 겹침 예산 — **1건** (`KNOWN`)

| 목형 | 크기 | 조건 | 원인 | 되돌림 신호 |
|---|---|---|---|---|
| ★ **칼선-14** | **22.23mm²** (예산 **22.5** — 여유 0.27mm²/1%) | @46 15up 3×5 · **그리고 ★자동 @4x62 12up 3×4** | **공식 문제가 아니라 표기차.** 발주 표기 `D 44.5` vs 목형 실측 `D 45.00`(패널 폭 93.66→138.66 = 45.00, 마지막 패널 44.30 = D−0.7). netW 공식 263.30 vs 실측 264.30 | `D=45` 를 넣으면 잔차 **0.00** · 겹침 **0.00mm²**. ★ 26-08-25 2차 — 그 주장을 `verify-imposition` **「표기차 검산」** 절이 **매번 실측한다**(주석 안의 숫자는 늙는다). 규격 표기 수정은 **견적 입력 쪽 일**이라 여기서 안 한다 — ⚠ 여유가 1% 뿐이지만 **예산을 올리지 마라**. 올리는 순간 「고칠 곳은 입력」이라는 말이 거짓이 된다 |

★ **이 한 건은 자동선택 판형에서 겹친다** — 즉 사람이 판을 고르지 않아도 앱이 그 판을 고른다.
수동 선택에서만 겹치는 건과 **급이 다르다.** 판형 조합 193건 중 자동선택에서 겹치는 목형은
이 **1벌뿐**이고(종전 4벌), 나머지 11벌은 최대 0.00~0.47mm² 다.

#### ② 잔차 예산 — **2건** (`RES_BUDGET`, 음수만 잡힌다)

| 목형 | 크기 | 원인 | 되돌림 신호 |
|---|---|---|---|
| 도솔 | **−0.20mm** (예산 −0.25) | 위 띠 `s = 0.67D + 0.03W + 2.8` 공식 잔차. **4표본 3계수라 자유도가 1** | 고치려면 `netSize` 를 건드려야 하고 표본이 더 필요하다 |
| 칼선-14 | **−1.00mm** (예산 −1.05) | 위 ① 과 **같은 한 건**(표기차). `b` 59.5/60.0 로 −0.50 · netW 263.3/264.30 로 −1.00 | `D=45` → 0.00 |

⚠ **여유가 정확히 `0.00` 인 칸이 32개** 있다(소스코:netW · 웨이크A/B 다수 · 더파이러츠 다수 …).
표본이 하나 더 들어오면 **여기가 먼저 깨진다.** 깨지면 그것은 회귀가 아니라 **새 정보**다.

#### ③ 커버리지 예산 — **12벌 전건** (`COV_BUDGET`)

점수가 내린 게 아니라 **안 재던 축을 재기 시작한** 결과다. 지배 원인은 **5가지**이고
전부 도형을 **크게** 만드는 조치 = 안전 방향이지만 up·금액 **재계량이 필요**하다.

| 원인 | 해당 목형 | 최악값(부족면적 / 최대깊이) | 조치 |
|---|---|---|---|
| **ⓐ `TIP_W 10.8` 이 넓은 패널을 못 덮는다** — 10.8 의 출처는 웨이크B 의 **두 갈래 혀 폭의 합**(5.3+5.5)인데 바깥 span 은 **30.1**이다. 「슬릿을 메워 보수적으로」라고 주석은 말하지만 실제 코드는 **합으로 줄여** 가운데 하나로 모았다 = 보수적의 **반대**. 게다가 웨이크는 패널 36·46mm, 소스코는 **140mm** 로 검증 범위 밖 | 소스코 | **941.7mm²** / 10.24mm @ 위띠·전면 | `TIP_W` 를 「바깥 span」으로 올리거나 **패널폭 비례**로 |
| **ⓑ 사다리꼴이 단조 감소라 실물의 「노치 후 재확대」를 원리적으로 못 그린다** — 웨이크A 는 70% 에서 두 노치로 갈렸다가 85% 에서 다시 넓어진다 | 웨이크A · 웨이크B · 도솔 | 342.0 / 300.8 / 208.4mm² | 사다리꼴 대신 **「전폭 + 끝단 사각」 2단** |
| **ⓒ 접착탭에 날개를 줄 통로가 없다** — `flaps.top/bot` 은 **4칸뿐**이고 탭은 `piecesFromFlaps` 에서 **몸통 높이 직사각형 하나**다. H=30 극단 납작 목형이라 아래 띠(59.5)가 몸통보다 두 배 깊고 탭이 그 띠를 따라 내려간다 | 칼선-14 | **733.9mm²** / **54.27mm** @ 아래띠·접착(idx4) | `flaps` 에 탭 칸을 늘린다 (①의 netW −1.00 이 여기 겹쳐 있다) |
| **ⓓ 십자 뚜껑이 패널 경계를 넘는다(잠금 귀)** — 모델 날개는 **자기 패널 안에서만** 그려지고 이웃 쪽으로 못 나간다. 최악 지점이 전면 가장자리 열인 것이 그 증거 | 니치어 · 다빈기획 · 뉴로티엑스 · 칼선-09 · 칼선-10 | **678.8mm²**(다빈기획 W=190 으로 최대) / 58.70mm | 구조 파일이 날개의 **x 범위**를 직접 주는 통로가 필요하다 (현재 깊이 1개만 준다) |
| **ⓔ 앵커 부작용** — netH **과대**(모델 216 vs 실물 210, 혀 18.0 = 5벌 상한 = 안전측)인데 겹침 규약대로 **좌상단 모서리**에 맞춰 얹으니 +6 이 전부 아래로 간다 | 바이오머 | 508.9mm² / 3.00mm | ⚠ **도형을 키워서 생긴 부족이라 키워서는 못 고친다.** 앵커를 몸통 기준으로 바꾸면 겹침 게이트의 자리 규약(nest 가 bbox 모서리에 앉힌다)과 어긋난다. 실물 겹침은 **0.00mm²** 라 실害 미확인 |
| **ⓕ `TIP_FULL` 이 아래 날개에 안 걸린다** — `piecesFromFlaps` 가 `tipW` 를 **위 날개에만** 넘긴다 (`addFlap(…, topOf(i), -1, flaps.tipW ?? null)` vs `addFlap(…, botOf(i), +1)`). 26-08-19 에 위쪽만 고친 버그가 아래에 남았다 | 더파이러츠 | 195.7mm² / 14.29mm | `flaps` 에 **`botTipW` 를 따로** 둔다 |

⚠⚠ **`TIP_FULL` 을 아래에도 그냥 넘기지 마라 (ⓕ 를 한 줄로 고치지 마라).**
삼면접착 아래 띠 인셋이 10.2 → **64.6** 으로 **작아져서**(W140 기준) 정반대로 위험해진다.
반드시 `botTipW` 를 따로 둬야 한다.

### 왕복 2회차 — `seedFromAuto`

운영자가 확정한 3up 이 왕복 한 번에 2up 으로 되돌아가며 **+30%**(240→312원) 오르던 버그.
원인은 링크가 아니라 **재진입에서 확정값을 되살리지 못한 것**이었다.
불변식과 「계산 up 과 링크 up 을 가르지 않은」 이유는 **§13 「왕복 불변식」**에 있다 —
두 곳에 적으면 갈린다.

⚠ 이 버그를 찾는 동안 `ShowroomPage` 가 **정의되지 않은 `capItems()`** 를 불러 「자동 배치」가
`ReferenceError` 로 죽고 있는 것을 발견했다. **`vite build` 는 린트가 없어 이걸 못 잡는다** —
빌드 성공은 「코드가 돈다」의 증거가 아니다. 지금 그 자리를 지키는 것은 `verify-speclink` §K 다.

### 점수 변동 — 내린 것을 내린 대로 적는다

현행 열은 **26-08-25 2차 실행값**이다(§7 과 같은 실행).

| 지표 | 종전 | 현행 | 왜 |
|---|---|---|---|
| `verify-net` 판걸이 up | 7/10 | **6/10** | ⓑ `CROSS_TAB` — 십자B 4→3up |
| `scorecard-nest` | 7/10 · 14.4% | **6/10 · 17.1%** | 같은 원인 |
| `verify-interlock` §D up | 7/10 | **6/10** | 같은 원인. ⚠ 이 줄은 문서에 없어서 **오래 7/10 인 채로 남아 있었다** |
| `scorecard-unit` §A | 7/12 | **6/12** | ⓐ 안전 봉투 — 맞물림 상실 (LUXEN 2→1) |
| `scorecard2` | 10/15 | **8/15** | ⓐ. ⚠ **원시 네스터 — 앱 점수 아님** (§7 경고 ②) |
| 패널별 날개 깊이 MAE | 16.07mm | **2.13mm** | ★ 이번 작업의 목적 |
| **자동선택 판형에서 겹치는 목형** | 4벌 | **1벌** | ★ 이것이 진짜 성과다. 남은 1벌 = 칼선-14 (표기차) |
| 채점되는 기하 축 | 3축 | **7축** | +끝단폭 +테이퍼기울기 +전폭구간 +접착탭모따기 |
| 돌연변이 검출 | 9/14 | **16/16** | 스위트가 스스로 돌린다. 그중 2축은 **노브의 기본값**을 겨눈다 |
| `verify-speclink` | 58/58 | **162/162** | +왕복 2·3회차 불변식(§K) · §J 거래처·품명. ⚠ 이 문서가 한때 **204/204** 라 적고 있었다 — **재현 실패.** 26-08-25 2차 실행값은 **162/162** 이다(§B 공정합계 1 · §L 범위 밖·허용범위·choice 22 신설) |

**up 점수가 내려간 것은 실패가 아니다.** 종전 7/10 은 실물보다 작은 도형으로 얻은 숫자이고,
그중 일부는 **물리적으로 불가능한 판**이었다. 「틀린 판을 안 만드는 것」이 up 점수보다 위다.

### 남은 것 (문서가 아는 **미해결** 전건)

* **칼선-14 실물 겹침 22.23mm² @자동선택 판형** — 표기차. 위 예산표 ①.
* **잔차 예산 2건 · 여유 0.00 칸 32개** — 위 예산표 ②.
* **웨이크버니A 46×46×138 @국2 — 견적서 6up, 앱 4up.** 원인은 모델이 아니라 **전역 정책**이다:
  원시 6up 의 발자국이 **93.3%** 이고 `MAX_FOOT_PCT=92` 가 깎는다. 92→94 로 올리면 재현되지만
  **전 견적에 파급**되므로 사람이 결정할 일이다. 방향은 안전측(비싸게 부른다).
  숫자는 `verify-net` 「표 밖 실측」이 매번 다시 잰다.
* **탈취제A 75×37.5×186 @4x62 — 견적서 4up, 앱 6up.** ⚠ 위 셋과 **방향이 반대다** —
  앱이 더 많이 걸어서 **싸게 부른다**(영업 위험). 원인 미특정이고 노브로 안 풀린다.
  같은 「표 밖 실측」 줄이 매번 찍는다.
* **`near` 프로파일은 잔차·커버리지가 **무검증**이다.** 실측 near 목형 둘(소스코·칼선-14)을
  픽스처가 일부러 안 실으므로 그 두 게이트가 그 코드 줄을 **원리적으로 안 지나간다**
  (겹침만 「실측 자세」 절이 잰다). 3지선다 중 하나가 무검증이라는 사실은 화면 라벨이
  직접 말한다(§15 안전 결함 ④). near 를 싣는 목형이 생기면 그때 자동으로 두 게이트에 들어온다.
* **커버리지 12벌 전건 (원인 6가지)** — 위 예산표 ③. 전부 「크게 그리기」라 안전측이지만
  up·금액 **재계량 필요**. 특히 ⓕ 는 한 줄로 고치면 정반대로 위험해진다.
* **삼면접착 맞물림 상실 — 아직 미해결이다.** 통로(`flaps(W,D,H,opt)`)는 26-08-25 에
  꿰었다(§15). 그런데 **26-08-25 2차에 이 줄을 정정한다** — 1차에 「`scorecard-unit`
  §A-die 8 > §B 6 = 맞물림이 돌아왔다」고 적었는데, **그 8 은 추정 3행이 만든 값**이었다.
  추정을 철회하니 §A-die 는 §B 와 같은 **6** 이다 = **실측만으로는 맞물림이 돌아오지 않는다.**
  ★ 26-08-25 **3차 정정** — 「실측 far 3벌은 up 을 한 칸도 안 움직인다」는 **거짓이었다.**
  웨이크A·도솔은 안 움직이지만 **웨이크버니B 36×36×168 @국2 는 기본 4up → 목형 지정 6up**
  이고 견적서가 **6up** 이다(원시 8up 98.0% 를 발자국 상한 92 가 6 으로 깎아 정확히 맞는다).
  즉 **실측 노브가 앱 실경로에서 견적서를 재현하는 자리가 실재한다.** 그런데도 위 문장이
  살아 있었던 이유는 **웨이크버니B 가 어느 채점표에도 없기 때문**이다 — 「점수 기여 0」은
  노브에 대한 진술이 아니라 **채점 범위에 대한 진술**이었다. 지금은 `verify-net`
  「표 밖 실측」이 두 열을 매번 다시 재서 찍는다(점수로는 안 센다 — 채점 정의를 바꾸는
  일은 사람이 근거와 함께 해야 한다).
  삼면접착 **맞물림**은 그와 별개로 여전히 안 돌아왔다.
  남은 것은 그대로 「미상 목형을 어떻게 추정할 것인가」이고, W:D 자동 판정안은 기각됐다.
  1-feature 규칙이 **둘 다 표본을 완벽히 가르면서 반대 답을 낸다**는 것이 2차의 추가 발견이다(§15).
* ✔ ~~`G3MODE` 환경변수~~ — **26-08-25 삭제 완료.** 같은 절 「지뢰 제거」.
  그 블록은 dev 서버에서 삼면접착 견적을 통째로 빈 화면으로 만들고 있었고(브라우저에
  `process` 없음), 테스트 16종은 Node 에 `process` 가 있어 전부 초록이었다.
* **CI(오라클 없음 + `PDF_ALLOW_SKIP=1`)에서 `verify-imposition` 이 exit 1** — 겹침 계측기
  자기검사가 진실 0벌이면 `check(1,0)` 로 FAIL 을 넣고 그 FAIL 이 면제를 안 받는다.
  「SKIP 은 실패다」 방침과 얽혀 있어 손대지 않았다. 새 게이트 2개는 진실 0벌이면
  「미실행(점수 아님)」을 찍고 채점하지 않는다(가짜 초록 방지).
  ⚠ **개발 PC(오라클 있음)에서는 EXIT=0 이다** — 26-08-25 확인. CI 는 이 패스에서 안 돌렸다.

---

## 15. 목형별 구조 옵션 — 봉투를 여는 통로 (26-08-25 · 1~3차 전부 반영)

§14 가 「제대로 풀려면 **구조 옵션 입력**이 필요하고, 그 전에 `flaps()` 가 `opt` 를
받도록 꿰야 한다」고 남긴 그 자리다. 이번에 꿰었다.

### 왜 필요했나 — 한 공식으로 못 덮는 것이 **확정 사실**이다

| 갈리는 것 | 실측 | 봉투로 닫은 대가 |
|---|---|---|
| 삼면접착 아래 띠 **깊은 날개 위치** | far 3벌(웨이크A·웨이크B·도솔 · 전부 W/D=1.00) / near 2벌(소스코 3.26 · 칼선-14 1.78) | LUXEN 2→1up · 웨이크버니B 6→4up · **삼면접착 맞물림 상실** — ★ 이 중 **웨이크버니B 는 실측 노브로 갚아졌다**(기본 4up → 목형 지정 6up = 견적서). LUXEN·맞물림은 그대로 |
| 십자 **접착탭 실폭** | 24.03 / 15.70 / 14.70 / 13.70 / 12.03 (삼면·맞뚜껑 7벌은 전부 15.0) | 십자B 4→3up · 다빈기획 하4 **견적 미산출** |

봉투는 「크게 그린다」라 안전하지만 **견적서보다 비싸게 부른다.** 목형을 아는 사용자가
그 목형의 값을 넣으면 그 목형의 겹침은 **0 이다**(실측 12벌 전건 확인).
⚠ **「견적서 up 이 그대로 나온다」는 일반 법칙이 아니다** — 실측으로 그렇게 된 자리는
웨이크버니B(4up→6up)와 다빈기획(하4 「견적 미산출」 소멸) **둘**이고, 웨이크A·도솔은
폴리곤만 갈리고 up 은 그대로다. 나머지를 맞히려고 값을 **추측하면** 아래 사고가 재발한다.

### ★ 노브 목록 — 각각의 **실측 표본 수**

**살아 있는 노브는 2개다**(`deepFlap` · `glueTabW`). 표의 셋째 줄은 「**만들지 않은** 노브」의
기록이다 — 지우면 다음 사람이 같은 산포를 보고 노브를 또 만든다.

노브를 만든 근거는 「값이 흩어진다」는 실측이다. 표본이 몇 벌인지 숨기면 다음 사람이
한 벌짜리 근거를 다섯 벌짜리로 읽는다.

| 노브 | 구조 | 형 | 기본값 | 실측 표본 | 표본이 말하는 것 |
|---|---|---|---|---|---|
| `deepFlap` | `glue_3side` | choice `""`/`far`/`near`/`both` | `both`(양쪽 다 깊게 = 봉투) | **5벌** — far 3(웨이크A·웨이크B·도솔) / near 2(소스코·칼선-14) | 두 계열로 갈린다. ⚠ far 3벌 중 웨이크A·B 는 **같은 PDF 2페이지 = 같은 목형족**이라 독립 표본은 **2족**뿐이다 |
| `glueTabW` | `cross` | number 5.0~60.0mm | **24.03**(실측 5벌 **상한**) | **5벌** — 24.03 / 15.70 / 14.70 / 13.70 / 12.03 | 십자만 산포한다. 삼면·맞뚜껑 **7벌은 전부 정확히 15.0** 이라 공용 `GLUE_TAB` 은 그대로 둔다 |
| (없음) | `tuck_both` | — | 혀 t = 18.0 | 3벌 — 15.0 / 16.5 / 18.0 | 산포는 있으나 **세 값 전부 점수를 한 칸도 안 움직였다** → 노브를 만들지 않았다 |

`near` 는 **라이브 표본이 0** 이다 — 실측 near 목형 둘을 픽스처가 일부러 비워 두기 때문에
`verify-imposition` 의 **잔차·커버리지 게이트가 그 코드 줄을 원리적으로 안 지나간다**
(겹침만 「실측 자세」 절이 잰다). 3지선다 중 하나가 무검증 경로라는 사실을 화면
라벨이 직접 말한다 — `탭에 붙은 쪽만 깊다 (실측 2벌 · ⚠ 잔차·커버리지 미검증)`.

### ★ 목형별 설정 표 — 지금 무엇이 실려 있나 (`test/die-profiles.mjs`)

| 목형 | 구조 | 실린 값 | `src` | 왜 |
|---|---|---|---|---|
| 웨이크버니A 46×46×138 | glue3 | `deepFlap:"far"` | 실측 | 깊은 W 가 탭에서 먼 쪽(idx1) |
| 웨이크버니B 36×36×168 | glue3 | `deepFlap:"far"` | 실측 | 같은 목형족(같은 PDF 2페이지) |
| 도솔 50×50×150 | glue3 | `deepFlap:"far"` | 실측 | idx1 실측 65.0 = b |
| 소스코 140×43×130 | glue3 | **(비움)** | 실측 near | 켜면 얕은 쪽 dust 1.0mm 부족 → 겹침 51.98mm². up 은 4모드 전부 4up 이라 얻을 것이 0 |
| 칼선-14 80×44.5×30 | glue3 | **(비움)** | 실측 near | 켜면 @46 16up 맞물림이 열려 겹침 397.5mm². 원인은 탭 날개 결손 |
| LUXEN 130×130×55 | glue3 | **(비움)** | **미실측** | 아래 「미상 기본값과 그 위험」 |
| 니치어 70×70×151.5 | cross | `glueTabW:24.03` | 실측 | 세로 크리스 `[0,24.0,94,164,234,303.33]` |
| 칼선-10 49×49.5×144 | cross | `glueTabW:15.70` | 실측 | |
| 다빈기획 190×100×109.5 | cross | `glueTabW:14.70` | 실측 | 이 값이면 하4 netW 594.0 ≤ 597 (「하4 고정 시 견적 미산출」 회귀 소멸) |
| 칼선-09 42×42×117 | cross | `glueTabW:13.70` | 실측 | |
| 뉴로티엑스 80×20×48.5 | cross | `glueTabW:12.03` | 실측 | 실측 최소 |
| 십자A 47×47×176 | cross | **(비움)** | **미실측** | 아래 |
| 십자B 70×70×55 | cross | **(비움)** | **미실측** | 아래 |

### ★★ 이번 사고 — 추정 3행을 **철회했다** (26-08-25 2차 · 3차에 단서 하나 추가)

**사고의 모양을 먼저 적는다. 다음 사람이 같은 길로 들어오기 때문이다.**

> 사용자 지시는 「**견적서에 맞춰 달라**」였다. 나는 목형별 노브에 **견적서에서 역산한 값**을
> 넣어 맞췄고 점수가 올랐다. 그런데 **오른 점수 전부가 추정 행에서 나왔고 실측 행 기여는
> 0** 이었다. 그게 신호다 — **견적서로 값을 정하고 그 견적서를 맞히면 점수는 오른다.
> 그건 재현이 아니라 순환이고, 상승분이 추정 행에서만 나오면 과적합이다.**

「견적서에 맞춘다」가 성립하는 조건은 **목형을 아는 것**이다. 목형을 **추측해서** 맞추면
그 추측이 다른 목형에서 실물을 겹치게 한다. 그래서 노브 **기제는 남기고 추측 값만 뺐다.**
원본 칼선 PDF 가 들어오면 그 자리에서 켜진다.

⚠ **가장 위험한 오독은 「점수가 안 움직였으니 안전하다」다.** 니치어 70×70×151.5 는
탭을 24.03 → 15.70 으로 줄여도 **up 이 6 그대로**다. 그런데 피치가 8.33mm 줄어
**앱 자동선택 판형에서 1,178.96mm² 가 겹친다.** up 은 안전 지표가 아니다 —
안전 지표는 **겹침**이다(안전 방향 규약).

한때 이 표에 **추정** 3행(LUXEN `far` · 십자A·십자B `glueTabW 15.70`)이 있었다.
적대검증이 그 3행을 무너뜨렸고, 무너진 방식이 그대로 「미상 기본값을 어떻게 정해야
하는가」의 규칙이 됐다.

| 무너진 근거 | 무엇이 드러났나 |
|---|---|
| **점수 기여가 100% 순환이었다** | 네 채점표에서 목형 노브가 올린 점수 **전부**가 추정 3행에서 나왔다. 실측 노브 행은 **한 점도 못 올렸다**. 즉 노브 「값」의 점수는 견적서 역산이었다. 노브 「기제」는 정당하다 — 실측이 두 계열/5값으로 갈리는 것이 증명돼 있고 부등식 논증과 돌연변이 게이트가 그걸 지킨다. ⚠ **3차 단서** — 「실측 기여 0」은 **채점 범위의 진술**이다. 채점표 밖에서는 웨이크버니B 가 실측 `far` 로 앱 실경로 4up→**6up = 견적서 재현**을 낸다(아래 변동표) |
| **추정 규칙을 실측에 되먹이면 겹친다** | 「십자 미실측 = 다수결 15.70」 → 니치어 70×70×151.5 에서 **1,178.96mm²**(국전 6up 3×2 = **앱 자동선택 판형**). 니치어 up 은 15.70 에서도 **6 그대로**다 — 피치만 8.33mm 줄어 겹친다. **「점수가 안 움직였다」는 안전의 증거가 아니다.** 「glue3 미실측 = far」 → 소스코 2,817.88mm² · 칼선-14 401.32mm². ⚠ **계측기가 둘이라 숫자가 두 벌이다** — 여기 적은 값은 적대검증의 **독립 2D 격자(0.2mm)** 이고, 스위트는 0.25mm 행 스캔이라 같은 자세에서 1,169.30 / 2,824.74 / 397.53 을 읽는다(수 % 차). **판정은 「지금 이 계측기로 재도 RED 인가」 하나**이고, 독립 격자값은 두 계측기가 같은 결론인지 눈으로 대조하라고 옆에 찍는다 |
| **최근접 실측이 정반대를 가리킨다** | 십자B 70×70×55 와 **W·D 가 같은 유일한 실측 십자 목형**이 니치어 70×70×151.5 이고, 그 실측 탭은 **24.03** 이다. 15.70 을 주는 근거는 「다수결」뿐인데 최근접 이웃은 상한을 가리킨다 |
| **표본이 결정하지 못한다 (LUXEN)** | 근거는 「W/D=1.00 이라 far 계열」 하나뿐. 그런데 같은 5표본을 **H/W** 로 갈라도 완전 분리되고(far 3.00/4.67/3.00 vs near 0.93/0.375) LUXEN 은 H/W **0.42 로 near 쪽**이다. 1-feature 규칙 둘이 표본을 둘 다 완벽히 가르며 **반대 답**을 낸다 |
| **우리 게이트가 이미 그 추론을 RED 로 판정한다** | 돌연변이표의 「glue3 기본 both → far(다수결 3/5)」 행이 소스코 −29.00mm 로 실패한다. 같은 추론을 기본값에서는 위험하다고 증명해 놓고 한 목형에는 점수용으로 허용하고 있었다 |

**⟹ 규칙: 미실측은 미지정(= 앱 기본 봉투)으로 둔다.**
소스코·칼선-14 를 「실측이 near 인데 일부러 비운다」로 처리한 것과 **같은 규약**이다 —
근거가 그보다 훨씬 약한 추정이 반대로 켜져 있는 것이 앞뒤가 안 맞았다.

**★ 「도면이 들어오면 켠다」 — 켜는 조건 (추측으로 채우지 마라)**

노브 **기제는 남겨 뒀다.** 값만 뺐다. LUXEN·십자B 의 **원본 칼선 PDF** 가 들어오면
아래 네 단계로 그 자리에서 켜지고, 견적서 재현이 즉시 돌아온다.

1. PDF 를 `verify-imposition` 의 `ORACLE` 에 넣는다(진실 = 폴리라인 실측).
2. 그 도면에서 값을 **읽는다**(십자 = 세로 크리스 간격 → 탭 실폭 / glue3 = 아래 띠 깊은 날개가
   접착탭에서 먼 쪽인지 붙은 쪽인지). **견적서 up 에서 역산하지 않는다** — 그게 이번 사고다.
3. `test/die-profiles.mjs` 에 `src:"실측"` 으로 한 줄 넣는다.
4. `npm run verify` — 잔차 ≥ 0 · 커버리지 · **겹침 0**(자동선택 판형 포함) ·
   「추정 되먹임 금지」 ①이 0건을 유지하는지 본다. 네 개가 다 초록이어야 켠 것이다.

도면 없이 켜려면 「추정 되먹임 금지」 ②를 먼저 통과해야 한다 — 그 값을 **같은 구조의
실측 목형 전부**에 먹여 겹침 0 을 증명하는 것이고, 지금 철회한 두 규칙은 거기서 RED 다.

### 되먹임 게이트 — 무엇을 막나 (`verify-imposition` 「추정 되먹임 금지」)

★ **도착했을 때 이 절은 이름만 있고 잠들어 있었다.** `src==="추정"` 행이 0건이라
루프가 한 번도 안 돌고 「0건」만 찍었다 — 즉 이번 사고의 근거 숫자가 **주석 안에만**
남아 있었고, 그건 이 저장소 자신의 교훈 **「주석 안의 숫자는 늙는다」** 위반이다.
**두 겹으로 실제 작동시켰다.**

| 겹 | 무엇을 재나 | **무엇을 막나** |
|---|---|---|
| ① **진입 필터** — 「점수에 실린 **비실측** 노브 0건」 | `DIE_TABLE` 에서 `src !== "실측"` 인데 `opt` 가 채워진 행을 센다. 있으면 그 값을 같은 구조의 실측 목형 전부에 먹여 **겹침 0** 을 요구한다 | 견적서 역산값이 점수판에 조용히 실리는 것. ⚠ 필터가 `=== "추정"` 이 아니라 **`!== "실측"`** 인 이유 — `dieOptOf` 는 `src` 를 **안 본다.** 라벨만 `"미실측"` 으로 바꾸고 `opt` 를 채우면 `measuredOnly` 분모에서 빠져 「점수 아님」으로 **위장**하면서, 겹침 게이트의 「목형 지정」 열과 앱 실경로(`box.dieOpt`)에는 그 도형이 그대로 들어간다. 그 샛길을 막는다 |
| ② **철회 규칙 상설 시연** — 「철회 후보 2건 · 되먹임 RED 2건」 | 철회한 두 규칙(십자 `15.70` · glue3 `far`)을 **매번 다시** 실측 12벌에 먹여 아직 RED 인지 본다 | ①이 **잠드는 것**. 추정 행이 0건이면 ①은 아무것도 재지 않고, 그러면 반증 숫자가 다시 주석으로 내려간다. ⚠ **RED 가 사라지면 그건 채택 신호가 아니라 조사 신호다** — 모델이 바뀌어 반증이 없어졌다는 뜻이므로 스위트가 그렇게 찍는다 |

**이빨을 실측했다**(손 돌연변이 2건 · 재현법이 그 절 주석에 있다) — 십자B 를
`{glueTabW:15.70}, src:"추정"` 으로 되돌리면 ①이 니치어 1,169.30mm² 를 짚고 exit 1,
같은 값에 라벨만 `src:"미실측"` 으로 바꿔도 **똑같이 exit 1**.

### ★ 견적서 재현 점수 변동표 — 무엇이 왜 올랐고, 무엇이 **안 오르나**

| 무엇이 | 어떻게 | 근거 등급 |
|---|---|---|
| 다빈기획 190×100×109.5 하4 「견적 미산출」 회귀 | `glueTabW 14.70` → netW 594.0 ≤ 597 로 **소멸** | **실측** |
| 니치어 70×70×151.5 자동선택 판형 겹침 1,269mm² | 기본 봉투 24.03 이 원래 막고 있다. 노브로 그 값을 **명시**하면 여유 0.00 인 채로 정확히 덮는다 | **실측** |
| 웨이크버니A `far` · 도솔 `far` | 폴리곤은 갈리지만 **up 은 안 움직인다** → 점수 기여 **0** | 실측 |
| ★ **웨이크버니B 36×36×168 @국2 6up** | 실측 `far` 로 **앱 실경로에서 재현된다** — 기본 4up → 목형 지정 **6up** = 견적서 6up (원시 8up 발자국 98.0% 를 상한 92 가 6 으로 깎아 정확히 맞는다). ⚠ **어느 채점표에도 이 건이 없어서 점수에는 안 잡힌다** — `verify-net` 「표 밖 실측」이 매번 다시 잰다. 26-08-25 2차에 이 줄을 「추정이라 철회 = 재현 안 됨」으로 적은 것은 **오기**였다(웨이크버니B 의 `far` 는 철회 대상이 아니었다) | **실측** |
| LUXEN 2up · 십자B 4up | 추정값을 넣으면 재현됐다. **철회했으므로 지금은 재현 안 된다** | ✗ 추정 |

**재현 안 되는 건과 그 천장** — 지금 견적서 up 을 못 맞히는 건은 두 부류다.

| 부류 | 건 | 천장(무엇이 있어야 풀리나) |
|---|---|---|
| **자료 부재** (점수 아님) | LUXEN 130×130×55 2up→1up · 십자B 70×70×55 4up→3up · 십자A(값에 안 민감, 6up 유지) | **원본 칼선 PDF**. 도면이 오면 노브 한 칸으로 즉시 풀린다. 추정으로 채우면 위 표의 순환·겹침이 되살아난다 |
| **모델 요인** (실패) | 삼면D 82×7×126 (2→6up · D=7 은 검증범위 D≥13 밖) · 맞뚜껑B 150×20×150 (2→4up · 500ea 소량, 기존 목형 재사용) · 삼면B 210×90×180 (3→2up · 46전지 990×720 클램프) | 각각 다른 원인이고 노브로 못 푼다. 현재 조치는 「판걸이(up) 직접 입력」 |
| **전역 정책 요인** (채점표 밖) | 웨이크버니A 46×46×138 @국2 6up→**4up** — 원시 6up 의 발자국이 **93.3%** 인데 `MAX_FOOT_PCT=92` 가 깎는다 | ★ **26-08-25 사용자 결정 — 올리지 않는다.** 94 면 재현되지만 전역 정책이라 전 견적에 파급된다. 이 한 건은 「판걸이(up) 직접 입력」으로 보정한다. 근거는 `sheets.mjs` MAX_FOOT_PCT 주석 |
| **초과 배치** (⚠ 방향이 나쁘다) | 탈취제A 75×37.5×186 @4x62 4up→**6up** | 위 셋과 반대로 앱이 **더 많이** 건다 = **싸게 부른다** = 영업 위험. 원인 미특정. `verify-net` 「표 밖 실측」이 매번 다시 찍는다 |

그래서 점수판은 **「실측 전용 점수」**를 따로 찍는다(`die-profiles.measuredOnly`) —
자료 부재 건을 분모에서 빼고, **몇 건을 뺐는지·그중 몇 건이 어긋나는지 항상 같이 찍는다.**
「점수 아님」은 면죄부가 아니라 **분리 집계**다.

### ⚠ 기본값으로 계량했을 때 **겹치는 목형이 하나 있다**

| 목형 | 조건 | 크기 | 원인 · 조치 |
|---|---|---|---|
| 칼선-14 80×44.5×30 | **기본값 열** · 46전지 15up 3×5 (**앱 자동선택 판형**) | **22.23mm²** | 공식이 아니라 **발주 표기차**다 — 발주 D **44.5** vs 목형 실측 D **45.00**(패널 폭 93.66→138.66). `verify-imposition` 「표기차 검산」 절이 매번 실측한다: **D=45.00 을 넣으면 겹침 0.00mm² · 잔차 0.00** 이다. ⟹ 조치는 예산 조정이 아니라 **입력**이다. 이 목형을 견적할 때 D 는 45.00 을 넣어라 |

예산 `KNOWN_OVERLAP` 은 22.5 로 잡혀 있고 여유가 0.27mm²(1%)뿐이다. **예산을 더 올리지
마라** — 위 검산이 「고칠 곳은 입력」이라고 말하고 있고, 예산을 올리는 순간 그 말이 거짓이 된다.

### 관통 경로 (한 줄씩)

```
BoxSpec  s.dieDeep / s.dieTabW           ← DIE_OPTS 표를 읽어 위젯을 뿌린다(구조 id 로 if 안 씀)
  │  ui/state.mjs  toQuoteInput → box.dieOpt {deepFlap?, glueTabW?}   (빈칸은 **빼고** 넘긴다)
  │  ui/state.mjs  SPEC_KEYS 에 "dieDeep","dieTabW"  → 해시 → 쇼룸(quoteInputOf 가 상태를 통째로 편다)
  ▼
domain/quote.buildDieline
  │  pickDieOpt(structure, dieOpt)  ← **그 구조가 읽는 노브만** 거른다
  │  key 에 `optKey` 를 넣는다      ← ⚠ 안 넣으면 판걸이 캐시가 옛 배치를 돌려준다
  ▼
dieline/index.dielinePieces → st.netSize(W,D,H,opt) · **st.flaps(W,D,H,opt)** ← 이번에 꿴 한 줄
  ▼
glue3.flaps  opt.deepFlap "both"(기본) | "far" | "near"
cross.netSize/flaps  opt.glueTabW = 탭 **실폭**mm (기본 24.03) → netW 가산분 = 탭폭 − 0.7
tuck-both  노브 없음 (혀 t 는 3값 전부 점수를 한 칸도 안 움직여 **만들지 않았다**)
```

* **`process` 금지는 그대로다.** 이 노브는 환경변수가 아니라 **함수 인자**다 —
  종전 `G3MODE` 가 dev 를 조용히 죽인 이력은 `glue3.mjs` flaps 주석이 소유한다.
* **기본값 = 현행 안전 봉투.** 값을 안 주면 종전과 **한 칸도 안 다르다**(하위호환).
  기본을 다수결(십자 탭 15.70)로 내리자는 계량 권고가 있었으나 채택하지 않았다 —
  근거와 그 대가(니치어형 미지정 시 국전 6up 실물 겹침 1,169.3mm²)는 `cross.mjs` 주석.

### 화면이 **말한다** — 자동인지 목형 지정인지

`BoxSpec` 는 입력칸이 아니라 **도메인이 실제로 받은 값**(`dieline.dieOpt`)을 보고 줄을 찍는다.

* `✓ 목형 지정 — 이 값으로 계산했다: 아래 띠 깊은 날개 = 탭에서 먼 쪽만 깊다 (실측 3벌)`
* `⚠ 목형 미지정 — 표준 가정으로 계산했다 (…). 가정은 안전측이라 판걸이가 실제보다 적게 나올 수 있다 = 견적이 비싸진다.`
* `⚠ 범위 밖 — 표준 봉투로 접었다. 접착탭 실폭 999mm (허용 5~60mm) — 이 값은 견적에 쓰이지 않았다.`

숨기지 않는 이유는 정직성 규약이다 — 견적이 **가정 위에 서 있다**는 사실을 사용자가
확인할 기회를 잃으면 안 된다.

### ★ 안전 결함 5건 — 화면·도메인이 **거짓말하던 자리** (26-08-25 2·3차)

노브를 꿰면서 **화면이 「넣은 값」을 「쓴 값」이라고 진술하는** 구멍이 같이 들어왔다.
전부 실측(화면·DOM·프로브)으로 확인하고 고쳤다. 방향 등급은 이 문서 머리의 **안전 방향 규약** 표다.

| # | 증상 (실측) | 원인 | 방향 | 고친 자리 |
|---|---|---|---|---|
| ① | 범위 밖 값(999)을 넣으면 도메인은 기본 봉투로 접는데 화면은 「✓ 목형 지정 — 접착탭 실폭 **999mm** 으로 계산했다」라고 진술. **상한 초과(61)가 더 나쁘다** — 모델이 실물보다 36.97mm **작아지고**(netW 303.33 vs 340.30) up 이 늘어 최대 **−36.6% 싸게** 청구된다 | 접기가 `cross.netSize` **안에서만** 일어나고 `pickDieOpt` 는 빈값만 걸러서 `dieline.dieOpt` 에 **원값이 남았다** | **최악**(모델 < 실물 = 겹침 + 싸게) | **접는 지점을 한 곳으로 올렸다** — `pickDieOpt`(`dieline/index.mjs`)가 구조의 허용범위(`cross.isValidTabW` / choice 의 `options`)를 통과한 **유효값만** 넣는다. `dieline.dieOpt` 에는 도메인이 실제로 쓴 값만 남으므로 화면은 한 줄도 안 고쳐도 접힌 건이 「⚠ 미지정」이 된다. 재현 확인: 999·61·3 전부 「⚠ 목형 미지정 — 표준 가정(24.03mm)」 + 「⚠ 범위 밖 …이 값은 견적에 쓰이지 않았다」 · netW 303.33 유지 · 국2 4up · 개당 189원(미지정과 동일) |
| ①′ | 입력칸이 범위를 **안 말한다** | 위젯이 `min`/`max`/`step` 을 안 받았다 | 보조 | `ui/primitives.jsx` `Input` 이 통과시키고 `ui/panels/BoxSpec.jsx` 가 `DIE_OPTS` 의 `min`/`max`/`valid` 를 넘긴다 (DOM 실측 `type=number min=5 max=60 step=0.01`). ⚠ **브라우저 힌트일 뿐이다** — 붙여넣기는 우회한다. 정본은 ①의 도메인 |
| ② | **음수 수량으로 견적이 나왔다** — `qty=-100` 이 4up · 지대R 0.275 · **개당 0원**. `qty=0`·`W=0`·`H="abc"` 는 전부 접히는데 음수만 빠져나갔다 | 두 진입점(`buildContext`·`compareSheets`)이 각자 `input.qty \|\| 0` 을 복제했고, `\|\|` 는 음수를 **참으로** 읽는다 | **최악**(0원 = 가장 싸게, 경고도 없음) | `domain/quote.mjs` `normQty` 한 곳에서 접는다(`Math.trunc(Number(qty)) > 0`). 프로브 4000→4000 · 0→0 · −100→**0** · "abc"→0 · −1→0 · 1→1. 화면 실측: `qty=-100` 이 `qty=0` 과 **완전히 같은 화면**(판형·판걸이·지대R·개당단가 전부 미표시). `ui/panels/BasicInfo.jsx` 수량 `min=1 step=1` |
| ③ | 「✓ 목형 지정」 줄이 **내부 id** 를 찍었다(「… far 으로 계산했다」) + 조사 오류 | `DIE_OPTS` 에 `options[].label` 이 있는데 안 썼다 | 표시 | `BoxSpec.jsx` 가 라벨을 찍는다. 값이 숫자·영문·괄호로 끝나 받침 판정이 불안정하므로 **조사를 쓰지 않는 어순**(「이 값으로 계산했다: …」)으로 바꿨다. 실측 문구: `✓ 목형 지정 — 이 값으로 계산했다: 아래 띠 깊은 날개 = 탭에서 먼 쪽만 깊다 (실측 3벌)` |
| ④ | 3지선다 중 **무검증 경로**(`near`)가 다른 둘과 똑같이 생겼다 | 라벨이 표본 수·검증 상태를 안 말했다 | 표시(그러나 오도) | `dieline/index.mjs` `DIE_OPTS` 라벨을 `탭에 붙은 쪽만 깊다 (실측 2벌 · ⚠ 잔차·커버리지 미검증)` 로. 드롭다운 option 텍스트 실측. **왜 미검증인가** — 실측 near 목형 둘을 픽스처가 일부러 비워 두므로 잔차·커버리지 게이트가 그 코드 줄을 원리적으로 안 지나간다 |
| ★ | **3차에 새로 잡은 것** — choice 노브가 거부되면 「허용 **undefined~undefined**」가 찍혔다 | 화면이 `min~max` 를 조립했는데 choice 는 둘 다 없다 | 표시 | `rejectedDieOpt` 가 `kind` 와 **레지스트리가 완성한 `allow` 문장**을 돌려주고 `BoxSpec` 은 그걸 그대로 찍는다(화면이 조립하지 않는다). ⚠ **닿는 경로가 실재한다** — `decodeSpec` 은 바깥 해시라 `dieDeep~zzz` 가 상태에 그대로 앉는다(Select 로는 못 만들지만 URL 은 만든다). 실측: `⚠ 범위 밖 — … 아래 띠 깊은 날개 zzz (허용 탭에서 먼 쪽만 깊다 (실측 3벌) / 탭에 붙은 쪽만 깊다 (실측 2벌 · ⚠ 잔차·커버리지 미검증) / 양쪽 다 깊다 (표준 가정과 같음))` |

⚠ ②는 노브와 무관한 **기존** 구멍이다. 노브를 꿰다가 같은 화면을 훑어서 나왔다.
「이번 변경이 만든 것만 고친다」로 남겨 두면 개당 0원 견적이 그대로 고객 앞에 뜬다.

회귀 감시는 `verify-speclink` §L 이다 — 「접혔는가」뿐 아니라 **「접힌 값이 `dieline.dieOpt`
에서 사라졌는가」**(= 화면 문구에 안 나오는가)를 0.5/999/61 세 값으로 잰다. 그리고 그
게이트가 「전부 버린다」로 통과하지 못하게 **허용범위 4값(5/12.03/24.03/60)이 반드시
실리는지**도 같이 잰다.

### 실측이 있는데 **일부러 안 쓰는** 자리 둘 (이것이 이번 계량의 핵심 발견)

| 목형 | 실측 | 채택 | 왜 |
|---|---|---|---|
| 소스코 140×43×130 | near | **미지정(both)** | near 를 켜면 얕은 쪽 dust(29.0)가 실측 W2 30.0 에 **1.0mm 부족** → 실물 겹침 51.98mm². 0.70D(30.1)로 올리면 겹침 0 이지만 커버리지 941.7 → **1,001.3 > 예산 942.5** 로 신규 적신호. up 은 4모드 전부 4up 이라 **얻을 것이 0** |
| 칼선-14 80×44.5×30 | near | **미지정(both)** | near 를 켜면 @46 8×2↺ 16up 맞물림이 열려 실물 겹침 397.5mm². D 를 목형 실측 45.00 으로 고쳐 잔차를 8칸 전부 0.00 으로 만들어도 **343.5mm² 남는다** — 표기차가 아니다. 진짜 원인은 **모델이 접착탭(idx4)에 아래 날개를 못 그린다**(`flaps.top/bot` 이 4칸뿐) |

⟹ 절대선 「실측 목형은 **자기 옵션으로** 실물 겹침 0」을 지키는 값이 이 둘에서는
「옵션을 안 주는 것」이다. **탭 날개를 그릴 수 있게 되기 전까지 near 를 켜지 마라.**

### 노브가 살아 있는지 누가 지키나 — 죽은 손잡이 금지

`scorecard2` 「목형별 노브 배선 **13/13**」이 노브 값마다 **폴리곤 서명**(정렬된 꼭짓점
목록)이 달라지는지 본다. 배치(up)가 아니라 도형을 보는 이유는 `테이퍼 노브 배선` 절과 같다.

⚠ **면적만 재면 far/near 를 못 가른다** — 둘은 같은 날개를 다른 패널에 단 거울상이라
면적이 같다. 첫 구현이 이걸로 2/13 으로 죽었다. 그래서 서명으로 비교한다.
같은 절이 `DIE_OPTS` 가 광고하는 노브와 이 게이트가 검사하는 노브가 **같은지**도 센다
(광고만 하고 아무것도 안 바꾸는 노브 = 사용자가 만지는 헛바퀴 = 삭제한 `TONGUE_INSET` 과 같은 함정).

두 화면 연동은 `verify-speclink` **§L** 이 지킨다 — 「키가 실렸는가」가 아니라
**「값이 금액을 움직이고, 그 움직임이 해시 왕복에서 산다」**를 잰다.
미지정이 왕복만으로 조용히 켜지지 않는 것도 같이 못박는다(켜지면 앱이 조용히 싸게 부른다).

### 목형표는 `test/die-profiles.mjs` 한 곳이다

네 스위트(`verify-net` · `scorecard-nest` · `scorecard-unit` · `scorecard2`)가 **같은
견적서 건**을 채점한다. 목형표를 네 벌로 두면 한 목형의 실측이 갱신될 때 세 벌이 조용히
낡는다. 그 파일이 행마다 `src`(**실측** / 추정 / **미실측**)를 들고 있고 점수표가 그걸 그대로 찍는다.

* `dieNote(cases)` — 각주 한 줄. 「목형 지정 N건 (실측 a · 추정 b) · 미실측 c건은 기본 봉투로 계산 = **점수 아님**」
* `measuredOnly(rows)` — **대표 숫자.** 미실측 건을 분모에서 빼고, **몇 건을 뺐는지 · 그중 몇 건이 견적서와 어긋나는지**를 같이 찍는다. 「점수 아님」이 면죄부가 되지 않게 하는 장치다.

현재 값(26-08-25 2차) — `verify-net` **5/8** · `scorecard-nest` **5/8** ·
`scorecard-unit` §A-die **5/9** · `scorecard2` **7/12**. 추정 **0건**이다.

---

## 16. 쇼룸 고객 표시가 — 관리자 수식으로 마진·환율을 얹는다 (26-08-26)

`src/showroom/price-formula.mjs` (표시 레이어 · 도메인 import **0줄**) ·
`src/showroom/ShowroomPage.jsx` (관리자 패널) · 게이트는 `verify-speclink` **§M**.

> ⚠ **26-09-04 — 환율 부분은 §18 로 갈라졌다.** 이 절이 적은 `*1.7/10` 은 마진과 환율이
> 한 상수에 섞인 모양이고, 지금은 수식에 `환율` 토큰이 있다(`*1.7/환율`). 이 절의 수식
> 문법·0 차단·가림 비트·저장 판단은 **그대로 유효**하고, `*1.7/10` 도 바이트 동일하게
> 계속 돈다(§18 의 하위호환 문단). 환율을 어디서 받고 언제 갱신하는지는 §18 이 갖는다.

### 무엇이 문제였나

전시회 부스의 고객 화면(`#showroom`)이 **한국 원화 원가를 그대로** 띄우고 있었다.
일본 고객 앞에서 그럴 수는 없다 — 마진을 얹고 엔으로 환산한 값이어야 한다.
그런데 마진율·환율은 그날 그날 다르고 **코드에 박을 수 있는 값이 아니다.**
그래서 관리자가 **수식 문자열**을 넣는다: `*1.7/10` = 마진 1.7배 후 ÷10(환율).

★ 이것은 **표시 레이어다.** `buildQuote`·도메인·원가 계산을 한 줄도 안 만진다.
원가는 늘 도메인이 낸 `totals.perEA` 그대로이고 여기서는 그 수를 **읽기만** 한다.
섞으면 §B 의 204원 게이트가 깨지는 것으로 끝나지 않고, 화면 숫자와 실제 원가가
한 계산에 섞여 **나중에 어느 쪽이 진짜인지 알 수 없게** 된다.

### 문법 — 왜 접두 연산자 사슬인가

**사용자가 실제로 치는 형태가 그것이다.** 「원가에 1.7 곱하고 10 으로 나눠」를
사람은 `*1.7/10` 이라고 적지 `x*1.7/10` 이라고 적지 않는다. 그래서 앞에 피연산자가
없으면 **원가가 암묵 좌변**이다. 괄호로 감싼 형태(`(*1.7/10)`)도 받아야 해서
선행 `(` 를 건너뛴 자리에 `x` 를 끼운다.

```
expr    := term  (('+'|'-') term)*
term    := factor (('*'|'/') factor)*
factor  := ('-'|'+')? primary          ← 괄호 안의 진짜 단항 부호
primary := num | var | '(' expr ')'          var = x | X | 원가
```

* 좌결합 · `* /` 가 `+ -` 보다 먼저 · 괄호가 우선순위를 이긴다.
* ⚠ 선행 `-` 도 같은 규칙을 탄다 — `-500` 은 **「원가 − 500」**이지 「음수 500」이 아니다.
  이 화면에서 상수 음수는 뜻이 없고(결과 음수는 어차피 거부다), 네 연산자를 다르게
  대우하면 그 예외를 관리자가 외워야 한다.

**받는 것** — 숫자(소수 포함) · `+ - * / ( )` · `x` / `X` / `원가` · 공백.
**전각(全角)은 정규화해서 받는다** — 일본 전시회 노트북의 IME 가 전각 모드일 때
`＊1.7／10` · `*１.７/１０` · `ー`(長音符 = 「-」 키) 를 낸다. 종전에는 그대로 거부했고
메시지가 「쓸 수 없는 글자입니다 — «＊»」였다. **11.5px 에서 ＊ 와 * 는 구별되지 않는다** —
거부는 안전했지만 부스에서 고칠 수가 없었다. 거부의 안전함은 **고칠 수 있을 때만** 뜻이 있다.
정규화는 「같은 글자의 다른 표기」를 모으는 것이지 문법을 넓히는 것이 아니다.

**거부하는 것**(전부 이유 문자열과 함께 · 예시) —
빈 문자열 · 공백뿐 · `*` `/`(연산자로 끝남) · `1.2.3` · `.` · `(1+2` · `1+2)` · `()` ·
`/0` · `x/(1-1)` · `-1000`(음수) · `*-2` · `y*2` `cost*2` `원가2`(모르는 이름) ·
`1e9`(지수표기 미지원) · `2^3` `1,7`(쓸 수 없는 글자 — **코드포인트를 같이 말한다**) ·
`*1.7;alert(1)` · `*1.7/10 && 1`(주입) · `*99999999`(상한 1e9) · 120자 초과 · 괄호 20겹 초과 ·
`x x`(토큰이 남음) · 보이지 않는 글자(제로폭 공백 → 「U+200B」로 이름을 말한다).

★ 「모르는 글자는 조용히 버린다」로 하면 안 된다 — `*1.7원/10` 이 `*1.7/10` 으로 통과하면
관리자는 자기가 친 것이 그대로 쓰인다고 믿는다.

### ★ 파서를 직접 짠 이유 — `eval` / `new Function` 을 안 쓴다

이 문자열은 부스에서 **사람이 손으로 치고** 브라우저 저장소에서 되읽힌다.
그 문자열이 코드가 되는 순간, **입력 한 줄로 화면에서 아무 코드나 돈다.**
토크나이저 + 재귀하강 파서는 400줄이 안 되고, 그 대가로 「부스 입력과 URL 이
코드가 되지 않는다」가 **구조적 사실**이 된다(방어 코드로 지켜야 할 성질이 아니라).

게이트 = `verify-speclink §M ⑧` 이 `src/` 전체를 훑는다.
⚠ **이 게이트가 무엇을 보증하지 않는지** 같이 적는다. 종전 정규식(`\beval\s*\(` 등)은
**별칭 한 번에 뚫렸다** — `const EV = (0, eval); EV(str)` 는 호출 괄호가 안 붙어 안 걸리고,
`Object.getPrototypeOf(function(){}).constructor` 경로에는 `Function` 이라는 글자가 없다.
백도어를 심고 돌렸더니 초록이었다. ⟹ 이름만 보도록 넓혔다
(`\beval\b` · `\bFunction\b` · `\.constructor\b` · `\bimport\s*\(`).
**그래도 `globalThis["ev"+"al"]` 같은 조립은 원리적으로 못 잡는다** — 그 한계를
스위트가 통과하는 단언으로 적어 두었다. 보증 범위는 「그 이름들이 소스에 안 나온다」까지다.

### ★ 실패하면 원가로 폴백하지 않는다 — 「—」다

관리자가 수식을 켜 뒀다는 것은 **원가를 감추겠다는 뜻**이다. 거기서 조용히 원가로
되돌아가면 고객 앞 화면에 한국 원화 원가가 뜬다 — **이 기능이 막으려던 바로 그 화면이다.**
실패는 시끄럽게, 그러나 **관리자에게만** 시끄럽게: 금액 자리는 「—」이고 이유는
관리자 패널에만 뜬다(빨간 글씨 + 해당 통화 입력칸 빨간 테두리).

같은 이유로 **통화를 명시로 골랐는데 그 통화의 수식이 비면 거부**다.
다른 통화 수식으로 대신 계산하면 관리자가 고른 것을 우리가 뒤집는 것이다.
예외는 통화가 **「자동」**일 때뿐이다 — 그때는 남은 수식 쪽으로 넘어가고 **넘어갔다고 말한다**.
실측한 함정이 그것이었다: 기본값 auto + 한국어 화면에서 관리자가 엔화 칸에 **첫 글자
`*` 를 치는 순간** 고객 화면이 「—」로 죽고, 통화를 바꿀 때까지 그대로였다.

### ★ 0 을 고객 화면에 띄우지 않는다 — 게이트는 **반올림 뒤** 한 곳

적대검증이 찾은 결함: `*0` 이 거부되지 않고 **¥0** 을 그렸다. 산술은 맞다 —
틀린 것은 「화면에 0 을 띄워도 된다」는 가정이다. 부스에서 오타 하나가 고객 앞에
¥0 을 놓으면 「싸게 불렀다」가 아니라 **그 자리에서 발주를 받을 수 없는 화면**이다.

게이트는 `resolve` 의 `roundFor` **직후** 한 줄이다 — `if (!(n > 0)) return blocked(…)`.

* **왜 `evalFormula` 가 아니라 여기인가** — 정확값으로 재면 두 부류를 통째로 놓친다.
  ① 반올림해서 0 이 되는 것: `*0.001`(0.204 → ¥0) · `/1000000000` · `-203.6` · `/500` …
  ② 원가가 작을 때: 원가 1원의 `*0.4` 는 정확값 0.4 로 「0 이 아닌데」 화면은 ¥0 이다.
  **화면에 뜰 값으로 판정하는 것이 요점이다.**
* **왜 `!(n > 0)` 인가** — 0 · **-0** · NaN 을 한 자리에서 막는다. `n === 0` 은 NaN 을
  놓치고 `n <= 0` 도 NaN 을 놓친다.
* **-0 은 따로 손봤다.** `*-0` · `*0*-1` · `*(0*-1)` 는 IEEE 음의 0 을 만드는데
  `v < 0` 은 **-0 < 0 === false** 라 음수 검사를 통과했고, `Math.round(-0) === -0` ·
  `(-0).toLocaleString() === "-0"` 이라 화면에 **「¥-0」**이 떴다(브라우저 실측).
  부호는 `evalFormula` 가 지우고(값 판정은 안 한다) 값은 0 게이트가 막는다.
* 경계는 **반열림 [0, 0.5)** 이다 — `/408`(정확히 0.5)은 살아서 ¥1 이 된다.

게이트 = §M ⑫ 가 **0 부류 42건 × 통화 2벌**을 매번 돌린다(정확한 0 20 · 반올림 0 13 ·
음의 0 9). 브라우저 실측으로도 확인했다 — `*0` · `*-0` · `*0.001` 전부 「—」다.

### 상한 쪽에는 **통화별 상식 밴드** — 거부가 아니라 관리자 경고

0 의 반대쪽 끝이 상업적으로 더 위험하다. ¥0 은 누가 봐도 틀렸지만 `*1.7/10` 에서
`/10` 이 빠진 **¥379 는 진짜처럼 보인다**(정상값 ¥38). 종전 상한은 `MAX_VALUE` 1e9
뿐이라 ¥999,999,840 까지 경고 없이 통과했다.

★ **밴드가 통화마다 다른 것이 요점이다.** 배율 1.7 은 원화에서 정상 마진이고
엔화에서는 「환율을 안 나눴다」는 뜻이다 — 공통 밴드 하나로는 그 구별을 못 한다.

| 통화 | 밴드(원가 대비) | 근거 |
|---|---|---|
| `JPY` | 0.01 ~ 1 | 1엔 ≈ 9~10원 → 정상 배율은 0.1~0.5 자리. 1 을 넘으면 「엔 숫자 > 원 숫자」 = 고객이 10배를 낸다 |
| `KRW` | 1 ~ 10 | 원가보다 낮으면 손해 · 10배를 넘으면 오타 |

거부가 아닌 이유 — 환율은 그날 그날이고 다음에 붙을 통화가 어느 자리에 올지 모른다.
거부로 두면 부스에서 맞는 수식이 막히고, 그때 운영자가 하는 일은 「게이트를 지우는 것」이다.
화면은 그리되 관리자 패널이 붉게 말한다. ⚠ **모르는 통화가 붙으면 밴드가 없다** = 경고 없음.

### 반올림 — **코드가 소유한다**(화면이 아니라)

`roundFor(cur, v) = Math.round(v)`. 지금은 두 통화가 같은 규칙이지만 `cur` 를 인자로
받는다 — 「엔은 10엔 단위로」가 오면 고칠 자리가 한 곳이어야 하고, 부르는 쪽이 통화를
넘기고 있어야 그 변경이 화면 코드를 안 건드린다.

* JPY — 소수점 반올림해 1엔 단위 (`34.68 → 35`). 실무 최소 단위가 1엔이다.
* KRW — 소수점 반올림해 1원 단위. **도메인 개당단가와 같은 규칙**(`quote.summarize` 가 round).
* 표기 — `¥1,234` / `1,234` + 단위키 `won`. 「원」을 문자열에 박지 않는다(일본어 화면에서
  「ウォン」이 되어야 하고 그 사전은 `showroom-core.T` 가 소유한다).
* 관리자 패널이 이 규칙을 **글로 보여준다**(`ROUND_NOTE`) + 반올림 전후를 같이 찍는다.
  숨은 반올림은 부스에서 「왜 계산이 안 맞죠」가 된다.

### 관리자 진입 — `Ctrl+Alt+M` (열기) / `Esc` · 같은 조합 (닫기)

* 부스에서 화면을 만지는 것은 **고객의 손가락**이다. 수식자 조합은 터치로 원리적으로
  만들 수 없다 — 눈에 안 띄는 버튼(모서리 3연타 같은 것)은 우연히 열린다.
* 주소창은 **고객 앞에 떠 있다.** `?admin=1` 은 숨은 모드가 있다고 광고하는 셈이고,
  이 화면은 300ms 마다 `replaceState` 로 주소를 자기가 다시 쓴다 — 플래그가 그 동기화와 싸운다.
* ★ **닫기는 비대칭이 맞다.** 우연히 닫히는 것은 무해하고 **우연히 열리는 것이 유출**이다.
  종전에는 빠져나가는 길이 수식자 두 개 조합과 33×18px 짜리 글자 링크뿐이라, 패널이
  「원가(개당) 345원」을 띄운 채 366×397px 로 떠 있는데 **고객이 다가오면 급히 닫을 방법이
  사실상 없었다.** 지금은 `Esc` 한 손가락이다(브라우저 실측 확인).
* 패널은 `position:fixed` 다 — 흐름에 넣으면 열 때마다 판·도면이 작아진다. 관리자가
  수식을 만지는 동안 **고객이 보는 그림은 한 픽셀도 움직이면 안 된다.**
* ★ 인쇄에서는 빠진다 — `@media print{[data-admin]{display:none}}`. 실측으로 이 페이지의
  모든 스타일시트에 `@media print` 가 **0건**이었고, fixed 라 첫 페이지에 원가와 수식이
  그대로 찍혔다. 이 화면은 스타일이 전부 인라인이라 `<style>` 한 조각을 둔다 —
  **거기에 다른 규칙을 늘리지 마라.**

### ★ 링크 — 수식은 안 싣는다. 그런데 그것만으로는 **거꾸로 유출됐다**

수식을 해시에 안 싣는 판단은 §13 이 거래처·품명에 내린 것과 같은 종류다. 수식은 그보다
나쁘다 — 거래처명은 남의 이름이지만 수식은 **우리 마진율이 그대로 적힌 문자열**이다.
`?q=…;fx~*1.7/10` 을 고객에게 보내면 1.7 배를 우리가 손으로 알려주는 셈이다.
대가(링크로 못 옮긴다)는 §13 과 **같은 방식**으로 갚는다 — 브라우저 저장소.
⚠ 다만 `sessionStorage` 가 아니라 **`localStorage`** 다. 두 값의 원하는 수명이 반대다:
거래처명은 탭이 닫히면 **사라져야** 하고(다음 사람이 남의 고객사명을 물려받으면 안 된다),
수식은 **살아야** 한다(노트북이 한 번 재시작되면 화면이 조용히 원가 원화로 돌아간다).

**★★ 그런데 「수식을 안 싣는다」만으로는 원가가 새 나갔다** (적대검증 critical · 실측).
해시는 **사양 전부**를 싣는다. 부스 화면이 ¥59 를 띄우던 **바로 그 주소**를 `localStorage`
가 없는 브라우저(= 링크를 받은 **모든 고객**)에서 열면 `isPriceOn` 이 거짓이 되어
`mode:"cost"` 로 접히고 **「345 원」 + `data-perea=345`** 가 그대로 그려졌다.
그 수가 이 파일이 유일하게 감추려던 것이고, `package.json` 의 `homepage` 가 gh-pages
공개 배포라 그 주소는 부스 밖으로 나간다. 게다가 관리자 패널이 「링크·주소창에는 싣지
않습니다 (마진 유출 방지)」라고 말했다 — 운영자는 그것을 **「이 링크는 보내도 안전하다」**로
읽는다.

**고친 방식 — 수식은 계속 안 싣고, 「가렸다」는 사실 한 비트만 싣는다.**

```
#/showroom?q=<사양>&nc=1      nc = no cost   (코덱: ui/state.mjs noCostOfHash / specHash 3번째 인자)
```

* 받는 쪽이 그 비트를 보면 원가로 접지 않고 「—」를 그린다(`resolve` 의 `hideCost` ·
  `mode:"hidden"`). 관리자 패널에는 **왜 「—」인지** 뜬다.
* **왜 `q` 안이 아니라 형제 파라미터인가** — `q` 는 `SPEC_KEYS` 와 1:1 이고 그 목록은
  「`toQuoteInput` 이 읽는 키」와 같아야 한다(§A 가 Proxy 로 실측 대조). 표시 레이어의
  값을 거기 끼우면 그 계약이 깨진다. `payloadOfHash` 는 `q=` 만 읽으므로 안 건드려도 돈다.
* 비트를 안 주면 해시는 **종전과 바이트 동일**하다 — 기존 링크·왕복 게이트가 안 움직인다.
* 비트에 마진은 없다. 주소에 `1.7` 도 `1.35` 도 안 나온다.
* ★ 관리자가 수식을 **전부 비우면** 이 비트도 같이 내린다 — 「이제 안 가린다」는 명시
  조작이다. 안 내리면 화면이 「—」에 갇힌다.
* ⚠ **막는 것은 실수이지 고의가 아니다.** 주소에서 `&nc=1` 을 지우면 원가가 보인다.
  원리적으로 그렇다 — 사양이 실려 있으면 그 브라우저가 같은 계산을 다시 할 수 있고,
  계산이 **전부 클라이언트에 있다**. 정말 못 보게 하려면 계산을 서버로 올려야 하고
  그건 이 앱의 형태가 아니다. 그래서 관리자 문구는 **안심시키지 않는다** —
  무엇이 실리고, 무엇이 안 실리고, **지우면 어떻게 되는지**까지 적는다.
* 같은 이유로 **「견적 앱으로 →」 버튼을 고객 화면에서 치웠다**(가리는 중일 때만).
  개당가 하나를 감추려고 파서를 짜 놓고 그 옆에 **원가 전체로 가는 라벨 달린 문**을
  열어 두면 감춘 것이 아니다 — 실측: 누르면 「공정 합계」·「개당 단가」·
  「단가 399,651원/R」이 한국어로 뜬다. 없애지 않고 `Ctrl+Alt+M` 뒤로 옮겼다.
  ⚠ 주소를 손으로 고쳐 `#/` 로 가는 사람은 못 막는다 — 위 「실수이지 고의가 아니다」와 같은 한계다.

### 원가 유출 게이트 — 무엇을 보고, **무엇을 못 보나**

계약은 하나다: **고객 화면에는 `shownPriceOf` 의 반환 객체만 그린다.**
그 객체에는 `mode` · `cur` · `main` · `unitKey` 뿐이고 **원가·수식·거부 사유·경고가 없다.**
원가는 `adminViewOf`(관리자 몫)에만 있고 관리자 패널 안에서만 그려진다.

| 게이트 | 무엇을 보나 |
|---|---|
| §M ④ | `shownPriceOf` 반환값을 `JSON.stringify` 해서 원가 문자열이 없는지 (**모델**) |
| §M ⑪ | `ShowroomPage.jsx` 소스 — 금액 칸이 `shown.main` 만 그리는가 · `data-perea` 가 수식 미설정일 때만 원가를 싣는가 · `perEA` 를 만지는 줄이 **6줄 이하**인가 · `Esc` 핸들러 · `@media print` · 가리는 중 back 버튼 (**정규식**) |
| §M ⑯ | ★ **`react-dom/server` 로 페이지를 실제로 그려 그 문자열을 훑는다** (**렌더 결과**) |

★ ⑯ 이 왜 필요했나 — ④ 는 모델을 재고 ⑪ 은 소스를 정규식으로 본다. **둘 다 관리자
패널의 렌더 경로를 지나가지 않는다.** 실증된 구멍: `adminOpen ? adminViewOf(…) : null`
의 조건을 없애고 `{adminOpen && av && (` 를 `{av && (` 로 바꾸는 **2줄**이면 원가·수식·
마진이 고객 화면에 영구히 박히는데 스위트는 **209/209 초록**이었다(브라우저로도 확인).
「화면에 원가 문자열이 없다」를 재려면 **화면 문자열이 있어야 한다.**
⑯ 은 그 2줄 주입을 **테스트가 직접 넣고 빨개지는지** 매번 확인한다(§H 이빨과 같은 규율).

⚠ **⑯ 이 못 보는 것** — SSR 은 `useEffect` 를 안 돌리므로 자동 배치가 안 앉는다
(`items` null → up 0 → 원가 자체가 없다). 그래서 원가 **숫자**를 재는 (c) 케이스에서는
`items` 씨앗 한 줄을 **명시적으로 심는다** — **배치만** 손대고 금액 경로는 원본 그대로다.
심지 않으면 「원가가 없다」가 공허해진다. 마우스·키보드 거동(패널이 실제로 열리는지)도
못 본다 — 그쪽은 브라우저 실측으로 확인했다.

### 남은 위험 — 알고 방치하는 것

* **React alternate fiber 잔상.** 패널을 닫아도 이전 렌더 fiber 에 원가·수식이 남는다
  (`root.child.child.child.alternate.pendingProps.children.2.props.v.cost = 345` — 실측).
  DOM 에도 화면에도 없고 devtools/콘솔로만 닿는다. React 가 이전 렌더를 들고 있는
  정상 동작이라 코드로 지우기 어렵다. 실질 위험이 낮아 **방치가 합리적**이다.
  줄이려면 `AdminPanel` 에 `v` 객체 통째가 아니라 포맷된 문자열만 넘긴다.
* **`nc=1` 을 지우거나 주소를 `#/` 로 고치면 원가가 보인다** (위 두 곳에 적었다).
* **`grep` 은 조립한 이름을 못 잡는다** (위 「파서를 직접 짠 이유」).
* 통화가 **두 벌뿐**이다(KRW·JPY). 세 번째가 붙으면 `BAND` 에 한 줄, `ROUND_NOTE` 에
  한 줄, `CUR_CHOICES` 에 한 줄이 **같이** 들어가야 한다.

---

## 17. 고객과 **같이** 사양을 넣는 화면 (26-08-26 · **2차 반영**)

`src/showroom/showroom-core.mjs` (`SHOWROOM_SPEC_KEYS` · `specOf` · `qtyRowsOf` ·
`pairs(list, lang)` · **`upForPrice`** · **`resetKindOf`** · `QTY_PIN_MIN`) ·
`src/showroom/ShowroomPage.jsx` (왼쪽 「사양」 절 + 결과 상자의 수량 줄 + `reseat` 효과) ·
`src/showroom/price-formula.mjs` (`shownRowsOf`) ·
`src/domain/data/papers.mjs`·`process-prices.mjs` (`labelJa`) · 게이트는 `verify-speclink` **§N**.

⚠ **2차(26-08-26)에 이 절의 두 문장이 거짓으로 밝혀져 정정했다.** 아래 「⚠⚠ 2차 정정」
표시가 붙은 곳이 그 자리다 — ① 「고정의 대가는 늘 안전 방향」(105칸 중 8칸이 반대다) ·
② 「수식 미설정이면 종전대로」(종전에는 표가 없었고, 원가 노출면이 1개 → 5개가 됐다).
새로 붙은 절 넷은 이 절 끝에 있다: 판이 바뀌는 순간 · 과도기 프레임 · 수량 칸 문턱 ·
인쇄 0도.

### 무엇이 문제였나

사용자 지적 원문: 「고객이랑 같이 화면 보면서 이거 사양을 넣어야하는데?」

쇼룸에서 바꿀 수 있는 것은 **구조 · W/D/H · 판형 · 수량**뿐이었다. 그런데 단가를 움직이는
것은 `SPEC_KEYS` **59칸**이고, 부스 대화에 꼭 필요한 지종·도수·코팅·후가공·접착·톰슨이
거기 없었다. 그걸 바꾸려면 견적 앱으로 돌아가야 하는데 **그 화면은 원가 전체를 보여준다** —
§16 이 개당가 하나를 감추려고 파서를 짜 놓은 화면 옆에서, 고객 앞에서 열 수 없는 문이었다.

### 무엇을 넣고 무엇을 뺐나 — **부분집합이 요점이다**

`SHOWROOM_SPEC_KEYS` **14칸** ⊂ `SPEC_KEYS` **59칸**. 50칸을 그대로 옮기면 고객 앞 화면이
견적 앱이 된다(「항목을 최소로」 — `SHEET_CHOICES` 가 주문생산 판형을 뺀 것과 같은 판단).
**여기 없는 칸은 실려 온 값·기본값이 그대로 산다**(`baseOf`) — 빠진 것이 아니라 안 만지는 것이다.

| 넣은 것 | 왜 |
|---|---|
| 지종 `paperId` | 부스 대화 1순위이자 이동폭 최대(실측 162~477원) · **판형 추천을 뒤집는다**(아코팩350 → 4×62 4up 이 4×64 2up 으로 — 그 순간 화면이 무엇을 하는지는 아래 「판이 바뀌는 순간」 절) |
| 도수 `fpColor·fpSp·fpBk` / `bpColor·bpSp·bpBk` | 「몇 도 인쇄」가 §13 의 출발점이었던 바로 그 지적이다 |
| 코팅 `fcId·bcId` | 「무광으로 하면?」이 부스에서 매번 나온다 |
| 후가공 `foil·emb·puv` | 켜고 끄기만. 종류·면수는 도면이 확정된 뒤의 이야기다 |
| 접착 `glueId` · 톰슨 `thomId` | 「있으면 좋음」. 톰슨은 **구조를 바꾸면 `nextThomId` 로 같이 바뀐다**(견적 앱과 같은 함수) |

| 뺀 것 | 왜 |
|---|---|
| 단가 직접입력 `printU·sobooU·spotRprV·mPrice·mR·lossSheets·admin·foilRpr·embRpr` | **원가 구조 그 자체**다. 입력칸으로 두는 순간 §16 이 무의미해진다 |
| 개발비 8칸 | 개당단가에 안 들어간다. 화면이 이미 「개당단가는 개발비 제외」라고 적는다 |
| UV 인쇄 `fpUv·bpUv` | 브리핑의 도수 목록에 없어서 뺐다. ⚠ **뺀 것 중 이동폭 최대**(실측 223 → 271원 = +21%) — 넣으려면 배열에 두 줄, 화면에 알약 하나 |
| 별색 계산방식 `spotMode·beda·spotRprV` | 우리 청구 방식이지 고객과 하는 대화가 아니다 |
| 박 종류·면수 `foilType·foilS·puvS` | 켜기만 하면 도메인이 1면 기본으로 계산한다 |
| 행거탭·목형 노브 `hang·hangV·dieDeep·dieTabW` | **전개도를 바꾼다.** 목형 노브는 목형이 확정된 재주문에서만 뜻이 있다(§15) |
| 주문생산 판형 `cusW·cusH·cusCut` · 판걸이 `mUp·mUpV` | 앞은 `SHEET_CHOICES` 와 같은 이유, 뒤는 **화면의 배치가 곧 그 값**이다 |

⚠ 배열이 **유일한 출처**다 — 계산(`quoteInputOf`)과 링크(`linkStateOf`)가 같은 목록을
`specOf` 로 걸러 얹는다. 나눠 적으면 「계산에는 반영되는데 링크에는 안 실리는 칸」이 생기고,
그 증상이 §13 의 204 vs 223 이다. §N 이 부분집합·중복·금지목록을 매번 센다.

### ★ 수량별 개당단가 — 판형과 판걸이를 **화면 그대로 고정한다**

「1,000개면 얼마, 5,000개면 얼마」가 부스 대화의 본체다. 눈금은 `[1000, 3000, 5000, 10000]`
+ **지금 수량**(안 끼우면 큰 글씨의 값이 표 어디에도 없다 · ⚠ 문턱 `QTY_PIN_MIN`=100 —
아래 「수량 칸」 절). 계산은 `buildQuoteRange` —
견적 앱의 수량별 비교표(`ui/viz/QtyCompareTable`)가 쓰는 **바로 그 함수**다(복제 0줄).

★ 판형·판걸이를 고정한 채 **수량만** 바꾼다. 화면에는 판 한 장과 배치 한 벌이 그려져 있고,
표는 「이 배치로 수량만 바꾸면」을 답해야 한다. 수량마다 판형을 다시 고르게 두면 표의 한 칸이
**화면에 없는 판**의 금액이 되고, 그 순간 한 화면이 스스로와 갈린다.

#### ⚠⚠ 26-08-26 **2차 정정** — 「고정의 대가는 늘 안전 방향」은 **거짓이었다**

이 자리에는 「1,000개 표 564원 vs 자동 516원 → 우리 표가 더 비싸다 = 안전 방향」이라고만
적혀 있었고, 게이트(`§N ⑥`)도 **첫 줄 한 칸만** 재고 있었다(`rows[0].perEA >= autoLow`).
전 칸을 재면 부등호가 깨진다 — 기준 사양(삼면 140×43×130 · **AB라이트295** · 4×62 4up):

| 수량 | 이 표 | 그 수량으로 견적을 **다시 짜면** | 방향 |
|---|---|---|---|
| 1,000개 | 531원 | 469원 (4×64 2up) | +13.2% 안전 |
| **3,000개** | **237원** | **239원** (4×64 2up) | **−0.8% 위반** |
| 4,000 · 5,000 · 10,000개 | 204 / 196 / 167원 | 같은 판 | 0.0% |

7구조×3지종×5수량 **105칸** 스윕: 안전 97칸 · **예외 8칸** · 최악 **−3.7%**
(맞뚜껑 100×60×150 · AB라이트295 · 3,000개 — 표 263원 vs 재견적 273원 = 4×63 2up).
원인은 `findBestSheet` 의 랭킹이 **최종 개당가 최소화가 아니라는 것**이다
(`EST_PRICE_PENALTY` · `SHEET_PRIORITY` · `TIE_PCT`) — 「자동이 늘 더 싸다」가 성립하지 않는다.

**그래도 고정은 유지한다.** 셋을 견줬다:

| 안 | 대가 |
|---|---|
| 고정을 푼다 | 표 한 칸이 **화면에 없는 판**의 금액이 된다 — 한 화면이 스스로와 갈린다(이 절의 요지) |
| 위반 칸을 표에서 뺀다 | 칸마다 자동 재견적을 한 번 더 돌려야 한다 = 렌더마다 `findBestSheet` **5회**. 수량을 칠 때마다 도는 비용이라 못 낸다. 게다가 빠지는 칸이 하필 1,000·3,000개(부스에서 제일 자주 묻는 수량)다 |
| **부등호를 계약에서 내리고 계량한다** ← 채택 | 사실을 적고, `§N ⑥`·`⑪` 이 **전 칸 + 105칸 스윕**을 매번 재서 「예외 8칸 · 최악 −3.7%」를 **예산으로** 잡는다. 나빠지면 그 스위트가 실패한다 |

⚠ 실제 **청구** 경로에서는 갈리지 않는다 — 돌아가는 링크가 **그린 판의 id 를 싣기** 때문에
(`linkStateOf` ①) 견적서도 같은 판으로 계산한다. 위 「재견적」은 **사양을 처음부터 다시
넣었을 때**의 값이고, 그 경우에만 최대 3.7% 비싸진다.
⚠ **누르는 칩이 아니다.** 눌러서 수량을 바꾸면 판형 추천이 뒤집힐 수 있고(1,000개는 4×64 2up 이
싸다) 그러면 지금 그린 배치와 다른 판이 된다. 수량은 왼쪽 칸 하나로만 바꾼다.
(2차 정정 — 종전에 「그때 앉힌 배치가 지워져 금액이 「—」로 간다」고 적은 부분은 이제 다르다:
 판만 바뀐 경우 화면이 **말하고** 새 판에 **다시 앉힌다**. 아래 「판이 바뀌는 순간」 절.)

### 원가 유출 — 표는 화면에서 **가장 여러 번 읽히는 숫자**다

계약은 §16 과 같다: 화면은 `shownPriceOf`/`shownRowsOf` 의 반환 객체만 그린다.
`qtyRowsOf` 가 낸 원가 배열은 `ShowroomPage` 에 **닿기 전에** `shownRowsOf` 를 지난다 —
그래서 §M ⑪ 의 「원가를 만지는 줄 **6줄 이하**」가 표가 붙은 뒤에도 그대로다.

| 게이트 | 무엇을 보나 |
|---|---|
| §N ⑦ | `shownRowsOf` 반환에 원가·`why`·`warn`·`src` 가 없다 (**모델**) · 거부 시 전 칸 「—」 · `nc=1` 전 칸 「—」 · ★ **수식 미설정이면 표가 0칸**(아래 2차 정정) |
| §M ⑪ | 소스 — 표가 `r.main` 만 그리는가 · `perEA` 를 만지는 줄이 6줄 이하인가 (**정규식**) |
| §M ⑯ (c-2) | ★ 렌더 결과 — 다섯 칸이 전부 표시가인가 · **원가 다섯 벌**이 화면 글자·`data` 속성 어디에도 없는가. 표면은 「태그는 `data-*` 값만 남기고 태그 밖 글자는 그대로」로 만든다 — 원본 마크업을 훑으면 세 자리 원가가 SVG 좌표와 우연히 같아 오탐이 나고, 「네 자리만」으로 접으면 1up 원가가 전부 세 자리라 **재는 것이 없는 죽은 단언**이 된다(실제로 그랬다) |

브라우저 실측(1280×800 · 수식 `*1.7/10` · JPY): 화면의 원가는 **관리자 패널 안의 한 개뿐**이고,
그 패널을 DOM 에서 떼면 다섯 수량의 원가가 **0건**이다.

#### ⚠⚠ 26-08-26 **2차 정정** — 수식이 **꺼져 있을 때**의 노출면이 5배였다

위 표는 수식이 **켜졌을 때**만 재고 있었다. 꺼져 있으면(`mode "cost"`) 표도 「종전대로 원가
원화」로 그렸고, 그 순간 고객 화면의 원가가 **1개 → 5개**가 된다. 브라우저 실측
(localStorage 비움): 「4 up · 개당 **204** 원」 + 「1,000개 **531** / 3,000개 **237** /
4,000개 **204** / 5,000개 **196** / 10,000개 **167**」.

1,000개 531원은 4,000개 204원의 **2.6배**다 — 이건 단가 하나가 아니라 **고정비/변동비 구조
(원가 곡선) 자체**를 고객에게 그려 보이는 것이고, 단가 한 개보다 훨씬 많은 정보다.
§16 은 큰 글씨 **하나**를 가리려고 파서·0 차단·가림 비트까지 지은 기능이다. 그 기능이 꺼져
있을 때 노출면이 5배가 되는 것은 「미설정이면 **종전대로**」가 아니다 — **종전에는 표가 없었다.**

⟹ **cost 모드에서는 `shownRowsOf` 가 빈 배열을 돌려준다 = 표를 안 그린다.** 큰 글씨 하나만
남으므로 종전 노출면이 정확히 유지되고(하위호환), 화면 코드는 한 줄도 안 만졌다
(`ShowroomPage` 는 `qtyShown.length` 로만 표를 낸다).
· 상대값(「1,000개 +160% · 10,000개 −18%」)으로 그리는 안은 **버렸다** — 가리려는 것이 곡선인데
  상대값이 그 곡선 그 자체다.
· 부스에서 표가 필요하면 수식을 켜면 된다. 원화 원가 그대로가 필요하면 `*1` 이면 되고,
  그때는 관리자가 **보여주기로 명시로 조작한** 것이다.
· `§N ⑦` 의 이빨을 **뒤집었다**: 종전 「미설정이면 표는 종전대로 원가 원화다」가 이 확대를
  정상으로 못박고 있었다. 지금은 「표가 0칸이다」 + 「큰 글씨는 여전히 원가 원화다(하위호환
  이빨)」 + 「수식을 켜면 표가 다시 5칸 뜬다(공허하지 않다)」 셋을 잰다.

### 배치 — 도면과 판이 주인공이라는 것을 안 깬다

* **사양 칸은 왼쪽 `aside`** 다. 그 칸은 `overflowY:auto` 라 늘어나도 **판이 한 픽셀도 안 줄어든다.**
  오른쪽에 두면 줄마다 판 높이가 빠진다. 1280×800 실측: aside 내용 883px / 보이는 높이 747px =
  **136px 스크롤**(마지막 톰슨 줄과 사양 문구가 접힌다). 판·도면은 무손상이다.
* **수량 줄은 결과 상자 안**의 한 줄이다(41px). 별도 표로 만들면 ~150px 이고, 큰 글씨에서 멀어진다.
  · 결과 상자의 `gap: 34` 를 `columnGap 34 / rowGap 10` 으로 **갈랐다** — 안 가르면 줄바꿈 간격
    34px 이 통째로 판 높이에서 빠진다(실측 판 465 → 387px).
  · 오른쪽 정보 블록의 「수량」 줄은 **지금 수량이 표에 있을 때만 접는다** — 그때는 강조된 칸으로 이미
    크게 떠 있어 같은 값을 두 번 말하는 셈이고, 그 한 줄이 22px 이다.
    ★ 26-08-26 2차 정정 — 종전 조건은 `qtyShown.length <= 1` 이었는데 **표준 눈금 4칸이 늘 있어서**
    수량이 무효(빈칸·0·음수·문자)여도 거짓이었다. 실측: 큰 글씨는 「—」인데 표는 자신 있게 네 값을
    말하고 강조된 칸이 하나도 없어 **지금 무엇을 견적한 것인지 화면이 안 말했다.** 조건을
    「지금 수량이 표에 있는가」로 바꿨다 — 무효면 「수량 0 개」가 떠서 왜 「—」인지가 읽힌다.
  · 최종: 판 높이 **465 → 434px (−6.7%)**. 색은 이미 쓰는 두 벌(`acc/accBg` · `soft/line`)뿐이고
    SVG `<text>` 0개 · stroke 색 2종은 그대로다.

### ⚠ 일본어 — 칸 이름 · **공정명 15개** · 그리고 **일부러 안 지은 31개**

칸 이름은 전부 `showroom-core` 의 KO/JA 사전에 있다:
仕様 · 用紙 · 印刷 · コーティング · 後加工 · 貼り · 打ち抜き · 表/裏 · プロセス4色 · 特色 ·
スミ · 箔押し · エンボス · 部分UV · 数量別の単価 · **印刷なし**.

★ 26-08-26 **2차** — 종전에는 여기까지였고 **선택지 52개가 전부 한국어**였다(지종 28 ·
코팅 6×2 · 접착 7 · 톰슨 5). 종전에 한국어가 남던 자리는 사양 요약 **한 줄**이었는데,
고객과 **같이 보는** 왼쪽 칸 전체가 한국어인 것은 규모가 다르다. 그래서 §17 이 지정해 둔
**정본 경로를 실제로 팠다**: 도메인 항목에 `labelJa` 를 붙이고 `pairs(list, lang)` 가 고른다.
**화면에서는 문자열을 여전히 안 짓는다**(짓는 순간 견적 앱과 갈린다).

| 번역했다 (15) | 안 했다 (31) | 왜 |
|---|---|---|
| 코팅 5 — 無し · マットPP · グロスPP · IRコート · ベルベット | **글로스코팅** | 유광코팅과 무엇이 다른지 데이터에 근거가 없다(단가만 다르다: 4절 65,000 vs 55,000). 高光沢/UV 중 무엇인지 지어낼 수 없다 |
| 접착 6 — 無し · 片面貼り · 三面貼り · 手提げ型 · スリーブ · PP貼り | **풀발이** | 우리 현장 용어이고 대응하는 일본 실무어를 확인 못 했다. `のり貼り` 로 적으면 단면·삼면과 구분이 사라진다 |
| 톰슨 4 — 単純型 · 標準型 · 複雑型 · G型 標準 | **측면 풀발이 12단** | 위 풀발이와 같은 말이다. 한 용어를 한 곳에서만 번역하면 화면이 스스로와 어긋난다 |
| — | **지종 28** | 전부 제품명·등급명이다. 「AB 350g」을 「アートボール350g」라고 적는 순간 **우리가 만든 제품명 주장**이 되고, 고객 앞 화면에서 근거 없는 확언이 가장 나쁘다 |

· `matte`/`gloss` 를 **PP** 로 적은 근거는 `process-prices.mjs` 자신의 26-08-06 주석이다
  (「무광라미 → 무광코팅과 같은 것이다」 = 라미네이트 = 일본 실무의 PP 貼り).
· `일반형 → 標準型` 은 이 목록의 **기본 티어**라서다(`thomId` 기본값이 `n`). 一般型 도 뜻은
  같으나 일본 실무는 標準 을 쓴다.
· **전부 아니면 전무가 아니다** — `labelJa` 가 없으면 자동으로 한국어로 떨어진다. 근거가
  생기면 데이터에 한 칸씩 더하면 화면은 자동으로 따라온다(코드 수정 0줄).
· 사양 줄(`specSummaryOf`)은 여전히 **견적 라인의 한국어 `spec` 문자열**을 잇는다 —
  그건 라벨 목록이 아니라 도메인이 만든 **문장**이라 같은 통로로 못 고친다(§13 의 같은 판단).
  실측: 「見積仕様 · AB라이트 295g · **印刷なし** · IR코팅 · 톰슨 일반형 · 접착 단면」.

### ★ 판이 바뀌는 순간 — 부스에서 제일 자주 밟는 경로 (26-08-26 2차)

`showroom-core.resetKindOf` · `ShowroomPage` 의 `reseat` 효과 · 게이트 `verify-speclink` §N ⑨.

**증상.** 고객 앞에서 지종 드롭다운을 한 번 만지면(AB라이트295 → 두성 디프매트308) 자동판형이
4×62 → 4×64 로 뒤집힌다. 종전에는 그 한 번에 **앉힌 배치가 통째로 지워졌다**:
¥35 → 「—」 · 판은 빈 사각형(도형 0개) · 「「자동 배치」를 누르면 …」 온보딩 문구 복귀 ·
수량표 5칸 소멸. 그리고 `msg` 도 같이 `""` 로 지워져 **화면이 이유를 말하지 않았다.**
운영자가 「자동 배치」를 다시 눌러야 했다.

**이게 표준 경로인 이유.** 단독 진입은 늘 `carried?.sheetId || "auto"` 이고 견적 앱 기본값
(`INITIAL_STATE.sheetId`)도 `"auto"` 다. 그런데 종전 §N 은 `SOSCO.sheetId="4x62"` 고정이라
이 경로를 **한 번도 안 밟았다.** 이 문서 §17 은 그 뒤집힘을 표에 적어 놓고
(「아코팩350 → 4×62 4up 이 4×64 2up 으로」) 「그때 금액이 「—」로 간다」를 **수량 칩을 안 만든
이유**로까지 썼는데, 정작 새로 넣은 지종 드롭다운이 같은 문을 열고 있었다.

**고친 방식 — 리셋 키를 둘로 가른다.**

| `resetKindOf` | 언제 | 무엇을 하나 |
|---|---|---|
| `"shape"` | 도형이 바뀌었다 (치수·구조·PDF 후보/페이지) | 지우고 **끝**. 좌표가 뜻을 잃었으므로 다시 앉히는 것이 정당하지 않다 — 종전 그대로 |
| `"sheet"` | 도형은 그대로 · **판만** 바뀌었다 | ① `t.sheetChanged` 로 **말하고** ② 같은 도형을 새 판에 **다시 앉힌다**(400ms 디바운스) ③ 끝나면 `t.sheetReseated(n)` |
| `"none"` | 지울 배치가 없었다 (첫 화면) | 아무 것도 안 한다 — 단독 진입의 온보딩이 그대로 산다 |

**「자동 재계산 금지」 규율과의 관계.** 자동 배치를 거는 자리는 이제 **셋**이다(버튼 ·
사양이 실려 온 마운트 1회 · 이 다시 앉히기). 그 규율이 막으려던 것은 「치수 칸에 글자 한 자 칠
때마다 최대 200만 회 탐색(실측 153ms)」이고, 셋째는 그 경우가 **아니다**:
· **도형이 그대로일 때만** 돈다 — 치수를 치면 `"shape"` 라 안 돈다.
· **400ms 디바운스**(`RESEAT_MS`) — 수량을 「5 → 50 → 500 → 5000」으로 쳐도 탐색은 **한 번**이다
  (`reseat` 카운터가 바뀔 때마다 앞선 타이머를 지운다).
· 겹침 게이트는 그대로다 — `autoPlace` 안의 `auditItems` 를 지나고, 그리는 자리에서 한 번 더 잰다.

**브라우저 실측**(1280×800 · 수식 `*1.7/10` · 판형 「자동」 · MutationObserver 로 커밋 전부 관측):
지종을 AB라이트295 → 두성 디프매트308 로 바꾸면

| t | up | 큰 글씨 | 표 | 화면이 하는 말 |
|---|---|---|---|---|
| +0ms | 0 | 「—」 | (없음) | — |
| +3ms | 0 | 「—」 | (없음) | 「판형이 바뀌었습니다 — 같은 도면을 새 판에 다시 앉힙니다」 |
| +446ms | **2** | **¥81** | ¥139 ¥85 ¥81 ¥78 ¥72 | 「판형이 바뀌어 새 판에 2장으로 다시 앉혔습니다」 |

¥81 = 4×64 2up 의 진실값이다(474원). 종전에 이 자리에서 한 프레임 스쳐 가던 **¥49(290원)**
— 4×64 판에 up 4 를 강제한, 물리적으로 불가능한 배치의 금액 — 은 아래 절이 원리적으로 막는다.

### ★ 과도기 프레임 — 배치와 「그 배치가 놓인 판」은 **한 짝**이다 (26-08-26 2차)

`showroom-core.upForPrice` · 게이트 `verify-speclink` §N ⑩.

판이 뒤집히는 렌더에서 `sheetBase` 는 **이미 새 판**인데 `items` 는 아직 **옛 판의 4장**이다.
그 한 렌더에서 `qShow`·`qtyRows` 가 「4×64 판에 up 4」로 계산한다 — MutationObserver 실측
**290원(¥49) · 표 [¥115 ¥56 ¥49 ¥47 ¥41]**. 그 판의 진실은 2up 474원(¥81)이므로 **−39% 저가**이고,
4×64 에 4up 은 애초에 **안 앉는다.** `requestAnimationFrame` 샘플러로는 안 잡혔다(9.5ms 안에
「—」로 덮인다) — 즉 **화면에는 안 떴다.** 다만 그 무해함이 `useEffect` 가 페인트 전에 도는지에
걸려 있었다: 느린 기기 · 동시성 렌더 · `useEffect`→`useLayoutEffect` 변경 중 하나면 그대로
−39% 가 고객 앞에 뜬다. 방향이 「앱이 싸게 부른다」라 등급을 낮출 수 없다.

⟹ `items` 옆에 `itemsSheet`(그 배치가 놓인 판의 id)를 **한 짝으로** 든다. 갱신은 `setItems`
**한 문**을 지나고, 금액에 쓸 up 은 `upForPrice(items, itemsSheet, sheetBase.id)` 가 정한다 —
짝이 어긋나면 **0**, 즉 금액이 원리적으로 「—」다. 그린 수(그림)와 금액용 up 이 갈리지 않도록
`up` 변수 자체를 이 값으로 뒀다.
⚠ `setItemsRaw` 를 직접 부르지 마라. 그 순간 짝이 어긋난다.
⚠ `verify-speclink` §M ⑯ (c) 의 SSR 씨앗도 **두 개**가 됐다(배치 + 그 판). 하나만 심으면
  `upForPrice` 가 0 을 돌려줘 렌더 게이트가 통째로 공허해진다 — 그 사실을 그 자리에 적어 뒀다.

### 수량 칸 — 타이핑 중간 상태를 고객 화면에 칩으로 만들지 않는다 (26-08-26 2차)

`showroom-core.QTY_PIN_MIN` (= 100 · 눈금 최소 1,000 의 1/10) · 게이트 `§N ⑥`.
수량을 다시 칠 때 `qtyStepsOf` 가 **지금 수량을 늘 눈금에 끼우고** 있어서, 「4000 → 전체선택 →
5」에서 표에 「5個 **¥15,034**」라는 칩이 실제로 페인트됐다(rAF 샘플러 실측: 5 ¥15,034 → 50 ¥1,515
→ 500 ¥164 → 5000 ¥33). 종전에는 큰 글씨 하나만 요동했는데 요동하는 자리가 둘이 된 것이다.
· 문턱 아래는 **표에만** 안 끼운다 — 큰 글씨는 종전대로 즉시 따라간다.
· 그때 결과 상자의 「수량」 줄이 「수량 5 개」라고 말한다(위 배치 절의 조건 변경과 한 자리에서 만난다).
· ⚠ 「100개 미만은 안 받는다」는 뜻이 **아니다.** 우리 최소수량은 이 저장소 어디에도 없다.

### 사양 줄 — 인쇄가 **0도**면 그렇게 적는다 (26-08-26 2차)

`showroom-core.specSummaryOf(lines, t)` · 게이트 `§N ⑫`.
사양 줄은 견적 라인에서 뽑으므로 「안 한 공정은 줄이 없다」= 조용하다. 종전에는 도수를 쇼룸에서
못 만졌으니 무인쇄 상태를 이 화면에서 만들 수 없었는데, 이제 「원색4」 알약 **1탭**이면 만들어진다.
실측: 「견적서 사양 · AB라이트 295g · IR코팅 · 톰슨 일반형 · 접착 단면」 · 개당 ¥29(170원) —
고객은 그 단가가 **무인쇄** 값이라는 것을 화면 어디서도 못 읽는다.
⟹ 인쇄 라인이 0건이면 **지종 바로 뒤**에 「인쇄 없음 / 印刷なし」를 명시로 적는다. 맨 끝에 붙이면
덧붙인 말처럼 읽혀서 자리를 정했다. ⚠ 라인이 **0건**인 견적(들어가는 판형이 없는 전개도)에는
아무 말도 안 한다 — 설명할 단가 자체가 없는 화면이다.

### 왕복 불변식 — 이번에 새로 든 키는 **없다**

`SHOWROOM_SPEC_KEYS` **14칸**은 그대로다(§17 첫 표). 이번 2차에서 늘어난 것은 **화면 상태**이지
사양이 아니다:
· `itemsSheet` — 배치의 짝. **URL 에 안 싣는다**(배치 자체가 안 실린다. 실리는 것은 `mUp`/`mUpV`).
· `reseat` — 다시 앉히기 예약 카운터. 값에 뜻이 없다.
둘 다 `SPEC_KEYS` 밖이고, `§N ③` 의 「over 를 안 만지면 QuoteInput·링크 해시가 **바이트 동일**」이
그대로 초록이다. 왕복 1·2·3회차 사양 14칸 무손실도 그대로다(`§N ⑤`).

## 18. 환율을 **망에서** 받는다 — 마진과 환율을 가른다 (26-09-04)

`src/showroom/fx-rate.mjs` (신설 · 받기·검사·저장·낡음) ·
`src/showroom/price-formula.mjs` (수식의 `환율` 토큰) ·
`src/showroom/ShowroomPage.jsx` (**갱신 시점**과 관리자 칸) · 게이트는 `verify-speclink` **§M ⑰⑱**.

### 무엇이 문제였나

§16 의 수식 `*1.7/10` 은 **마진 1.7 과 환율 10 이 한 상수에 섞여** 있었다. 그래서
① 환율이 움직여도 화면은 조용히 그대로였고 ② 환율을 고치려면 마진이 적힌 문자열을
손대야 했고 ③ 화면이 「환율 얼마로 환산했는지」를 원리적으로 말할 수 없었다
(그래서 §17 의 참고견적 줄은 자리만 두고 숫자를 비워 뒀다 — 모르는 것을 안 적었다).

### 어디서 받나 — **문서가 아니라 브라우저가 정했다**

요건은 키 없음 · CORS 허용 · KRW→JPY · https. 후보 셋을 **실제로 호출**했다.

| 후보 | node | 브라우저 | 판정 |
|---|---|---|---|
| `api.frankfurter.app` | 200 · 헤더에 `ACAO: *` 가 **보인다** | **CORS 로 막힘** | ✗ 못 쓴다 |
| `api.frankfurter.dev` | 200 | 200 | ✓ 둘째 출처 (ECB) |
| `open.er-api.com` | 200 | 200 | ✓ 첫째 출처 |
| `api.exchangerate.host` | 200 이지만 본문이 `missing_access_key` | 같음 | ✗ 키를 요구한다 |

★ `.app` 은 node 에서 CORS 헤더가 보이는데 브라우저에서는 안 온다. 실측 문구:
`No 'Access-Control-Allow-Origin' header is present on the requested resource.`
캐시버스터를 붙여도 같다. **문서만 읽고 골랐으면 부스에서 처음 알았을 자리다.**
두 출처가 낸 값은 0.115068 대 0.11495 로 **0.10%** 차이 — ¥ 한 자리가 안 바뀐다.
그래서 평균 내지 않고 **순서대로 시도해 처음 통과한 하나**를 쓴다(고객이 검산할 수 있는
수여야 하므로, 화면의 환율은 어느 출처가 실제로 준 수여야 한다).

### 수식 — `10` 자리에 `환율` 한 낱말

`*1.7/환율` (별칭 `rate`). 뜻은 **1엔이 몇 원인가**(원/엔) — 종전 `10` 이 있던 그 자리다.
곱셈으로 뒤집지 않은 이유: 운영자가 이미 그 자리에 숫자를 적어 왔고, 낱말 하나만
바꾸는 것이 가장 안 틀리는 이사다.

★★ **하위호환이 문법 차원에서 성립한다.** `rate` 는 파서에서 `var` 와 같은 자리의
**잎**일 뿐이고 우선순위·결합은 한 줄도 안 바뀌었다. 순수 숫자 수식은 트리에 환율 잎이
없으므로 `usesRate(node)` 가 거짓이고, 그 뒤로 환율을 **아예 안 읽는다** — 환율이 8.69든
0 이든 NaN 이든 결과가 같다. `§M ⑰` 이 수식 75건 × 원가 5벌 × 환율 8벌 = **3,000건**을
바이트 대조하고(정상·거부·0 부류·전각을 §M ①②⑫⑭ 의 표에서 그대로 끌어온다),
이빨로 「환율을 쓰는 수식은 실제로 움직인다」를 같이 잰다.

### 틀린 값을 안 내놓는 다섯 겹

1. **밴드** — KRW→JPY 0.08~0.15(= 1엔 6.67~12.50원) 밖이면 **거부**한다. §16 의 BAND 는
   사람이 친 수식이라 경고였지만, 이쪽은 기계가 준 수다 — 범위 밖이면 응답을 잘못 읽은
   것이고 쓰면 안 된다. 방향이 뒤집힌 값(8.69 를 JPY 로 받는 경우)이 여기서 걸린다.
   ⚠ **이 밴드는 역사적 전 구간이 아니다 — 일부러 좁다.** 2008~09 에 1엔이 15~16원까지
   간 적이 있어 상한 12.50 은 그 시절을 안 덮는다. 그래도 좁게 두는 이유는 ① 막으려는
   것이 환율 변동이 아니라 **우리의 오독**(자릿수·통화 뒤바뀜)이고 ② 넓히면 다른 통화
   값이 통과해 **조용히 틀린 가격**이 되기 때문이다. 좁아서 지는 비용은 조용하지 않다 —
   거부하고 관리자 패널이 이유를 띄우며, 운영자는 수동 입력으로 즉시 넘어간다.
   진짜로 밴드를 벗어난 시세가 며칠 가면 넓히지 말고 수동으로 운용하다가, 그때의 실제
   시세를 보고 `fx-rate.mjs` 의 두 상수와 그 옆 문단을 **같이** 고쳐라.
2. **모양** — `result !== "success"` · 기준통화가 KRW 가 아님 · `rates.JPY` 없음 ·
   숫자가 아님 · 0 이하. HTTP 상태도 따로 본다(`§M ⑱` 이 **이유가 상태코드를 말하는지**까지
   잰다 — 모양 검사에 어쩌다 걸리는 것으로 층을 대신하면 그 층이 조용히 지워진다).
3. **못 받으면 원가로 안 간다** — 마지막으로 받은 값(기준일을 화면에 적는다)이나 수동
   값을 쓰고, 둘 다 없으면 금액이 「—」다. §16 이 수식 거부에 내린 판단과 같다.
4. **저장소도 다시 검사한다** — 되읽을 때 밴드를 다시 건다. 밴드 밖이면 **버린다**
   (낡은 값보다 나쁘다).
5. **표시와 계산이 같은 수** — 받은 값을 소수 둘째에서 끊어 그 값으로 계산하고 그 값을
   적는다. 고객이 화면의 8.69 로 검산하면 화면의 ¥44 가 나온다. 잃는 정밀도 0.11%.

### 갱신 시점 — 조용히 바뀌지 않는다

고객에게 ¥38 을 보여준 뒤 환율이 갱신돼 ¥39 가 되면 **그 자체가 사고**다. 그래서 값이
바뀌는 경로는 둘뿐이다: **화면 진입 시 1회**(그것도 캐시가 6시간보다 낡았을 때만) ·
**관리자의 「지금 갱신」.** 타이머·폴링은 없고 `§M ⑱ ⓘ` 가 `setInterval` 0건을 강제한다.
캐시가 신선하면 새로고침해도 망을 **안 탄다**(실측: 요청 0건 — 태블릿에서 이 차이가 크다).

### 화면이 거짓말하지 않게

참고견적 줄의 「환율 1엔 = 8.69원 (2026-09-04 기준)」은 **수식이 환율 토큰을 실제로 썼을
때만** 나온다. `*1.7/10` 에 대고 적으면 그 10 은 환율이 아니므로 화면이 거짓말이 된다 —
그때는 종전 문구(「당사 설정 환산값」) 그대로다. 반대로 관리자 패널은 그 상태를 **붉게**
알린다(「지금 수식은 환율을 쓰지 않습니다 — 위 환율 8.69 가 반영되지 않습니다」): 환율은
받아 뒀는데 금액에 안 닿는 것이 이 기능에서 가장 값싼 사고다.

⚠ 고객 몫 객체(`shownPriceOf`)에 **필드를 하나 늘렸다**(`fx`). §16 이 「늘리지 마라」고
적어 둔 자리다. 환율은 공개된 수이고, 표시가와 환율을 알아도 마진을 모르면 원가가
복원되지 않는다 — 그 논증이 `price-formula` 의 `fxShown` 주석에 있다.
